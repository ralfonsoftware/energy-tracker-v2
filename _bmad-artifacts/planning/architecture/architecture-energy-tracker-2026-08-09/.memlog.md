---
scope: Whole-system architecture for Energy Tracker v2 (greenfield): Pattern Detective, Tariff Savings Radar, Context Capture, Extensible Platform, Data Export/Import, Household & Access
purpose: Build substrate for downstream epics/stories + a human-facing solution overview doc
altitude: initiative
updated: 2026-09-02T21:44
---

- (direction) User: container-based approach for local dev/test; production hosting on Azure for now.
- (constraint) [ADOPTED] Frontend stack fixed by UX spec (DESIGN.md): React + shadcn/ui + Tailwind CSS, system font stack, mobile-first responsive web.
- (constraint) [ADOPTED] PRD NFR: single deployment artifact must run on modest self-hosted hardware (NAS/SBC class) AND on low-tier/scale-to-zero cloud tiers -- no separate cloud edition.
- (constraint) [ADOPTED] PRD NFR: OIDC auth provider swappable via config only, no code change; every route authenticated except OIDC callback.
- (constraint) [ADOPTED] PRD NFR: tenant (Household) isolation enforced at data-access layer, not just UI.
- (constraint) [ADOPTED] PRD Constraints: no paid third-party service required for a basic self-hosted instance; AI Wattage Plausibility (FR-17) backend is a household-level swappable choice (local LMStudio vs cloud API), must degrade gracefully when disabled.
- (constraint) [ADOPTED] PRD FR-14: Tariff Savings Radar and Pattern Detective pace threshold share one Bonus-Decay Normalization implementation -- must not diverge.
- (assumption) PRD addendum offers a candidate Azure shape (Static Web App + Container App scale-to-zero + Azure SQL Basic + Auth0/Entra ID) explicitly as non-binding input for this architecture phase, not a locked decision.
- (direction) User selected Coaching path and wants Spine + a human-facing doc as deliverables (audience/doc-type to be scoped at Finalize).
- (decision) Backend language/runtime: C# on .NET 10 (LTS, supported to Nov 2028). Verified current via Microsoft Learn.
- (decision) Design paradigm: Ports & Adapters (Hexagonal). Binds: whole system. Prevents: OIDC, AI backend, job queue, and persistence provider each growing a bespoke one-off swap mechanism instead of one consistent port/adapter shape. Driven directly by user's own framing of the job-queue requirement (one abstraction, pluggable implementations) plus PRD's repeated 'swappable via config, no code change' requirements (OIDC, AI backend).
- (decision) Async job processing: IBackgroundJobQueue port. Adapters: in-process System.Threading.Channels-backed adapter (default, zero extra containers/infra, satisfies self-host single-instance case) and Azure Storage Queue adapter (cloud path, enables future KEDA queue-depth scaling). A dedicated self-host broker container is deferred, not built now -- in-process adapter covers that case already.
- (decision) Deployment topology: single container/process serves both the HTTP API and background job processing (hosted BackgroundService), for both self-host and Azure Container Apps. Splitting the worker into a separately-scaled Container App (queue-depth KEDA rule) is Deferred until import volume justifies it.
- (decision) Frontend hosting: ASP.NET Core backend serves the built React/shadcn SPA's static assets directly from the same container (classic ASP.NET Core SPA-hosting pattern), rather than the addendum's separate Azure Static Web App. Reason: PRD NFR demands one deployment artifact for both self-hosted and cloud; a two-service split has no self-host equivalent. Deviates from PRD addendum's candidate shape -- flagged for addendum update.
- (decision) Auth: ASP.NET Core's built-in OpenIdConnect authentication handler, fully config-driven (authority/client id/secret via env vars) -- no vendor-specific SDK. Works against Entra ID, Auth0, Authentik, Keycloak etc. interchangeably.
- (decision) Data access: EF Core 10, chosen over Dapper for productivity/correctness across ~28 FRs and solo development; global query filters give data-layer tenant isolation (PRD NFR) largely for free.
- (decision) Database engine: dual-provider, configurable via settings/env var -- PostgreSQL (self-host default; ARM+x86 compatible, low footprint) and Azure SQL Basic DTU (Azure production; near-zero cost per user's explicit choice). Both via EF Core providers (Npgsql, Microsoft.EntityFrameworkCore.SqlServer) selected at the composition root.
- (constraint) Dual DB-provider support cost: EF Core migrations must be maintained as two separate migration histories (one per provider); domain/application code must avoid provider-specific SQL, functions, or column types (e.g. no raw jsonb operators, no rowversion/xmin-specific concurrency tokens) to keep both providers behaviorally identical. This is real ongoing engineering surface for a solo developer -- accepted explicitly by the user given the cost benefit (near-free Azure SQL Basic DTU) and self-host ARM compatibility (Postgres).
- (version) PostgreSQL 17.x pinned for self-host (mature, broad Docker/ARM support); Azure SQL Basic DTU for cloud. EF Core 10 + Npgsql.EntityFrameworkCore.PostgreSQL 10.0.3 + Microsoft.EntityFrameworkCore.SqlServer 10.x. Verified current via NuGet/Microsoft Learn, 2026-08-09.
- (decision) Dual-provider EF Core shape (verified against Microsoft Learn 'Migrations with Multiple Providers'): ONE shared DbContext type (not per-provider subclasses), with migrations split into two separate assemblies/projects -- Infrastructure.Migrations.Postgres and Infrastructure.Migrations.SqlServer -- each wired via MigrationsAssembly(...). Provider chosen by a single config value at the composition root (mirrors OIDC/AI-backend/job-queue adapter-selection convention).
- (constraint) Dual-provider migration drift risk: schema changes require running 'dotnet ef migrations add' once per provider migrations project; nothing prevents doing it for only one and having the other silently drift. Mitigation: a single wrapper script (scripts/add-migration.sh) runs both invocations together so a migration can't be added to only one provider by habit.
- (reference) Source: https://learn.microsoft.com/en-us/ef/core/managing-schemas/migrations/providers -- confirms one-DbContext/multi-migrations-assembly pattern and config-driven provider selection via Generic Host.
- (version) Azure SQL Database Basic DTU tier confirmed current (not deprecated): 5 DTU, 2GB storage, no stop/start capability (unlike Postgres Flexible Server Burstable which can stop). Verified via Microsoft Learn 2026-08-09.
- (decision) Status (FR-6/7) and Tariff Check Reminder due-ness (FR-15) are computed lazily at request time (dashboard/API read), never via a recurring background scheduler. Reason: Azure Container Apps scale-to-zero means no in-process timer/IHostedService survives between requests -- a naive cron approach would silently stop firing once idle. FR-18's proactive weekly recap (deferred, post-MVP) will need an externally-triggered scheduler (Container Apps scheduled Jobs or a KEDA cron scaler) when built, not an in-process timer.
- (decision) Historical tag integrity (FR-9, FR-16, FR-28): Room/PowerPoint/Device are soft-deleted (never hard-deleted), keeping historical FK references valid and reassignable. Smart Plug readings and Events additionally snapshot the Room/PowerPoint/Device identity by value at write time (denormalized), since a PowerPoint's live Room assignment can change later and must not retroactively rewrite historical views.
- (decision) Audit trail on corrections (Meter Reading, Tariff edits) implemented via one shared AuditCorrection table/service, not two divergent per-feature implementations.
- (decision) AI Wattage Plausibility (FR-17): single IAiPlausibilityClient port implemented as one OpenAI-compatible HTTP adapter, configured per household with a base URL + API key -- covers local LMStudio and any cloud provider identically without separate adapter classes. Disabled cleanly when unset (graceful degradation per Constraints).
- (decision) Meter Reading Regression (FR-25): at most one open MeterRegressionPrompt per Main Meter; ordering is by reading timestamp (chronological), not entry order; an open prompt blocks FR-3 baseline computation for readings after it until resolved.
- (event) Coaching round complete; proceeding to distill full ARCHITECTURE-SPINE.md.
- (decision) Reconcile pass (brief/PRD/UX subagents) found real gaps: (1) no structural guard against re-summing SmartPlugReading/Event against MeterReading as authoritative -- the v1 Residual anti-pattern the brief explicitly retired; (2) no rule against hardcoded household-specific values (genericity for self-hosters); (3) AD-7's 'compute live, never schedule' rule conflicts with FR-2/FR-8's requirement that historical Status/trend never get silently rewritten by a later baseline/threshold edit -- needs a persisted StatusSnapshot, not pure live recomputation of history; (4) offline-queued Meter Reading writes (PRD NFR) never addressed -- no PWA/local-queue mechanism, and no idempotency-key contract for safe retry; (5) Smart Plug import completion notification transport unspecified -- must be polling, not WebSocket/SSE, to stay consistent with AD-7's scale-to-zero reasoning; (6) session/token persistence for 'stays logged in on phone' unspecified; (7) i18n display/translation mechanism (vs. storage) unspecified.
- (decision) AD-14 Main Meter authority: MeterReading is the sole authoritative consumption total; no code path sums SmartPlugReading or Event data into a total compared against or reconciled with MeterReading (no Residual). Domain layer has no type representing 'total measured vs meter total'.
- (decision) AD-15 Generic-by-default: no household-specific value (threshold default, currency, kWh preset) is hardcoded in code; all such values are Household-level config rows with offered-not-applied presets (per FR-2).
- (decision) AD-16 Offline-safe idempotent writes: Meter Reading creation carries a client-generated idempotency key (GUID); the frontend queues unsent readings locally (IndexedDB) and flushes on reconnect; the API upserts-by-idempotency-key so a retried sync never double-inserts, while a genuine second same-timestamp-differing reading is still accepted per FR-1.
- (decision) AD-7 amended: current Status/Reminder stay live-computed at request time (unchanged). Historical trend points are persisted as an immutable StatusSnapshot written at the moment of computation (on every new Reading or completed import, per FR-6) -- never recomputed retroactively from later-edited settings. This resolves the conflict with FR-2/FR-8's no-silent-rewrite requirement.
- (decision) AD-6 amended: Smart Plug import completion notification is delivered via client polling of a job-status endpoint, not WebSocket/SSE -- a persistent push connection would either block Container Apps scale-to-zero or break on cold start, contradicting AD-7's own reasoning.
- (decision) AD-17 Session persistence: server-side cookie-backed OIDC session (ASP.NET Core cookie auth handler chained to the OIDC handler), not SPA-managed tokens in localStorage -- satisfies 'stays logged in on phone' across restarts and avoids XSS token-theft exposure from client-side token storage.
- (decision) AD-18 i18n mechanism: Household.Locale field selects both display formatting and UI translation; frontend uses an additive translation-resource-file library (e.g. i18next) and backend uses .NET's built-in resx/IStringLocalizer for any server-rendered strings (email/notification copy) -- a new Locale is a resource-file addition, never a code change, per PRD NFR.
- (constraint) Consistency Convention added: the dashboard's Status API response stays minimal (status value + one headline/supporting sentence) -- drill-down data (trend, per-plug) is served by separate endpoints, never merged into the Status response, structurally protecting the brief's 'says less, on purpose' discipline against feature creep.
- (event) Reviewer Gate ran: lint_spine.py (2 false-positive placeholder flags on literal '{id}' route params, no real issues), rubric-walker, version-currency reviewer, adversarial-divergence reviewer. All findings triaged and fixed directly in ARCHITECTURE-SPINE.md (Finalize step 3).
- (decision) Reviewer fixes applied: AD-2 tightened with concrete portable-pattern allowlist + ARM/x86 self-host nuance for SQL Server; AD-3 strengthened to ban Find()/raw SQL/IgnoreQueryFilters() and to cover background-job tenant-context resolution explicitly; AD-6 pinned a concrete JobEnvelope<TPayload> contract (JSON-serializable only, no delegates) so two features can't build incompatible payload shapes; AD-7 assigns StatusSnapshot writes to exactly one IStatusRecomputeService called from both trigger paths; AD-11 explicitly excludes FR-23's wholesale restore (no partial merge) from the audit-correction path; AD-14 broadened from 'domain or application code' to the whole system including frontend rendering; AD-16 clarified scope to Meter Reading creation only, not edits; AD-17 adds Data Protection key persistence via PersistKeysToDbContext (prevents cookie invalidation on every Container Apps cold start); new AD-19 covers the previously-silent operational envelope (health endpoint, structured logging, env-based secrets).
- (version) Version-currency reviewer corrections: Vite corrected from 6.x to 8.x (Vite 7 shipped June 2025, Vite 8 Rust/Rolldown rewrite shipped March 2026; verified via npm dist-tags). React 19.x confirmed still current (19.2.8, no v20). Azure SQL Basic/DTU cost description corrected: flat ~5 DTU/~$5/month, no auto-pause capability (DTU purchasing model has no serverless tier) -- was previously implied to be elastic like Postgres Flexible Server's Burstable stop/start; corrected to avoid an inaccurate claim, decision itself unchanged (user's explicit choice).
- (event) Solo-dev SOLUTION-OVERVIEW.md companion doc written. Spine finalized: status set to final, companions: [SOLUTION-OVERVIEW.md].
- (decision) AD-13 re-examined and confirmed (user revisited it explicitly): single container remains the call. Verified via Microsoft Learn: linking a Container App as an SWA backend requires the SWA Standard plan ($9/month flat, Free tier only proxies to managed Azure Functions) -- while Container Apps Consumption plan's free monthly grant (180,000 vCPU-seconds, 360,000 GiB-seconds, 2M requests) already covers this project's expected personal-household traffic entirely. So single-container is $0/month; splitting to SWA+Container App is a guaranteed +$9/month for capability (global CDN, instant-shell-load) that doesn't address a real problem at this project's scale -- the dashboard's actual data still cold-starts the same backend either way. Combined with the single-deployment-artifact NFR (which SWA has no self-host equivalent for), single-container wins on cost, simplicity, and NFR-compliance. No AD text change needed -- AD-13 already specified this; this entry records the confirmed decision and the verified reasoning.
- (event) User requested full artifact sync: PRD addendum.md's candidate Azure shape (Static Web App + separate Container App) still contradicted the finalized AD-13. Updating addendum.md to record the superseded/confirmed status per architecture decision, keeping data/auth candidate notes since those were adopted as-is.
- (event) Artifact sync complete: addendum.md updated to mark the Azure Static Web App candidate shape as superseded (with resolution notes), spine's AD-13 text tidied to reflect resolution, SOLUTION-OVERVIEW.md gained a dedicated frontend-hosting cost-analysis section. All current artifacts (brief, PRD, addendum, DESIGN/EXPERIENCE, spine, solution overview) now consistent.
- (change) prd.md was sharded (bmad-shard-doc) into prd/ (index.md + 12 section files); original prd.md deleted. Updated ARCHITECTURE-SPINE.md's sources: reference from prd.md to prd/index.md. Mechanical path fix only -- no AD, Rule, or content decision changed; Reviewer Gate re-run skipped as not load-bearing.
- (event) spine finalized (source-path fix only, no AD content changed)
- (direction) User requested formalizing, as an architecture decision, switching Azure SQL DB access from SQL authentication to Microsoft Entra ID (Azure AD)-only authentication -- an in-place change against the existing deployed server, not a fresh deployment. Prior research this session (Winston/architect persona) already surveyed the Bicep, app code, and CI workflow; captured below as the basis for the new AD.
- (constraint) [ADOPTED] Investigation findings, verified in-repo: infra/modules/database-sqlserver.bicep only sets administratorLogin/administratorLoginPassword (SQL auth), no AAD admin resource, no azureADOnlyAuthentication flag. Container App (infra/modules/container-app.bicep) already has a SystemAssigned managed identity, currently used only for AcrPull. Program.cs:140's UseSqlServer(connectionString, ...) is 100% driven by ConnectionStrings__Default (AD-2) -- zero app-code changes needed, no branching on auth mode. Microsoft.Data.SqlClient resolves to 6.1.1 in the built lockfile (project.assets.json), which already transitively carries Azure.Identity 1.14.2 -- Entra connection-string auth modes work today with zero new NuGet references.
- (version) Verified via Microsoft Learn: Microsoft.Sql/servers supports an inline administrators block (login/sid/principalType/tenantId/azureADOnlyAuthentication) to set the AAD admin and flip Entra-only together; equivalently a Microsoft.Sql/servers/azureADOnlyAuthentications child resource (name: 'Default') can flip the flag separately. No Bicep/ARM resource exists to create a database-level user (CREATE USER ... FROM EXTERNAL PROVIDER is T-SQL only, a real provisioning gap). Also confirmed: Microsoft.Data.SqlClient 7.0 will split Entra auth support into a separate Microsoft.Data.SqlClient.Extensions.Azure package -- not applicable at the current pinned 6.1.1, but a watch item for the next SqlClient bump.
- (decision) AD-21 Azure SQL access via Microsoft Entra ID-only authentication (Azure only; local self-host keeps SQL auth). Binds: AD-2's SqlServer adapter, the Container App's runtime identity, and the CI migration identity (energy-tracker-devops-uami). Prevents: the Container App and the CI migration runner each growing an ad hoc, divergent way of authenticating to the database once passwords are gone; a premature azureADOnlyAuthentication flip locking out an identity that has no database user yet. Rule: the existing SQL Server resource gets an AAD Admin (a human Entra ID account, not a service identity -- see rationale below) plus azureADOnlyAuthentication: true, applied only after both the Container App's system-assigned identity and energy-tracker-devops-uami have been granted contained database users (CREATE USER ... FROM EXTERNAL PROVIDER) with least-privilege roles (db_datareader+db_datawriter for the app identity, db_datareader+db_datawriter+db_ddladmin for the CI identity to run migrations). Cutover order is fixed: (1) add AAD Admin while SQL auth still works, (2) connect as that admin (or the still-live SQL admin) to create both database users and grant roles, (3) only then flip azureADOnlyAuthentication. Reversing (2) and (3) locks every identity out before it has a database user. The Container App's connection string moves to Authentication=Active Directory Managed Identity (system-assigned, no User Id needed); the CI migration step's connection string moves to Authentication=Active Directory Default, riding the az login OIDC session app-deploy.yml already establishes earlier in the same job via AzureCliCredential fallback. DATABASE_ADMIN_PASSWORD (GitHub secret) becomes retirable once cutover completes. Local self-host (docker-compose.sqlserver.yml) is a plain containerized SQL Server, not Azure SQL -- Entra-only auth doesn't apply to it at all, and it keeps sa/password unchanged; this is not a divergence from AD-2 since the connection string was already 100% environment-supplied with no code branching.
- (decision) AD-21 rationale, AAD Admin principal: a human Entra ID account (the project owner), not energy-tracker-devops-uami or the Container App's own identity -- keeping database administration separate from the two service identities that only need scoped data-plane/migration roles, appropriate least-privilege even at this project's solo/personal-household scale (consistent with AD-13's 'personal-household deployment' framing). Deferred: promoting the AAD Admin to an Entra ID group is the correct move if this project ever grows beyond a single owner/admin -- not needed now.
- (constraint) AD-21 CI rework: app-deploy.yml's 'Apply pending EF Core migrations' step currently builds Server=...;User ID=etadmin;Password=${DATABASE_ADMIN_PASSWORD};... . Under AD-21 this becomes Authentication=Active Directory Default; with no User Id/Password -- dotnet ef database update then authenticates as energy-tracker-devops-uami via the azure/login@v3 OIDC session already established earlier in the same job (DefaultAzureCredential's AzureCliCredential fallback shells out to the already-authenticated az CLI). The SQL firewall whitelist/retry-with-backoff/cleanup steps around it are unrelated to auth mode and are unchanged -- firewall and authentication are separate layers.
- (decision) AD-21 provisioning mechanism clarified (user direction): the one-time T-SQL that creates the two contained database users and grants their roles is run manually (e.g. via sqlcmd/Azure Data Studio against the AAD Admin's own Entra session), not automated via a Microsoft.Resources/deploymentScripts resource. The .sql script itself is committed to source control (e.g. infra/sql/grant-entra-db-users.sql) purely so the exact grants are repeatable if the SQL server is ever redeployed from scratch -- it is not wired into any Bicep/CI pipeline step.
- (event) Reviewer Gate ran on AD-21 (3 parallel subagents: version-currency, adversarial-divergence, security/least-privilege). Version-currency: all technical claims (Active Directory Managed Identity/Default keywords, CREATE USER FROM EXTERNAL PROVIDER, administrators/azureADOnlyAuthentications resources) verified current -- no hallucinations; 2 refinements folded in (ManagedIdentityCredential IMDS-probe-before-AzureCliCredential-fallback latency note; corrected SqlClient 7.0 from a 'future' watch item to already-GA). Security: 1 High finding (DATABASE_ADMIN_PASSWORD is not immediately retirable -- main.bicepparam/database-sqlserver.bicep still require it as a non-optional param on every infra-deploy.yml run; retiring it needs its own follow-up Bicep change) and 1 Medium (keep the admin password one deploy cycle post-cutover as a break-glass path) -- both folded into AD-21; role grants (db_datareader+db_datawriter app, +db_ddladmin CI) confirmed correctly minimal, a net reduction in CI blast radius vs. today's server-admin-login baseline -- added to AD-21 as a stated benefit. Adversarial-divergence: 3 Critical findings, all folded into AD-21 -- (C1) the AD asserted a Container App connection-string change with no file/owner named; fixed by explicitly binding database-sqlserver.bicep's connectionString output, main.bicep, and container-app.bicep and stating the rewrite ships in Deploy A; (C2) nothing gated the azureADOnlyAuthentication flip on the manual grant script having run, and both could land in one Bicep deploy/PR; fixed by splitting the rule into an explicit Deploy A / manual step / Deploy B sequence that must never collapse into one deploy; (C3) a from-scratch SQL server redeploy reproduces the exact lockout with nothing documented operationally; fixed by adding a cross-reference to docs/local-vs-azure-deltas.md and infra/README.md (both to be updated) alongside the existing databaseProvider-switch warning. Also folded in: H1 (generalized the Rule to cover any future DB-connecting identity, cross-referencing Deferred.md's Worker/API split), H2 (added a cold-start token-acquisition latency note against AD-7/AD-19).
- (decision) AD-21 finding triaged as intentionally NOT spine text: adversarial reviewer's M1 (token expiry vs. connection pooling under low-traffic profile) and M2 (AD-19's blanket 'DB connection string is a secret' claim goes stale for the Azure-SQL-post-cutover case specifically) and L1 (the CI job's az-login OIDC vs. Story 1.5's end-user sign-in OIDC share a name) are real but left for the implementation story to note inline rather than further expanding the spine text -- none of them are divergence risks between independently-built units (M1/M2 are single-behavior nuances, L1 is a naming/readability note), so they don't meet the spine's own inclusion bar.
- (event) Cross-referenced docs synced per AD-21's own text: added D6 to docs/local-vs-azure-deltas.md (quick-reference table + full prose section, mirroring D5's two-phase-deploy pattern) covering the from-scratch-redeploy lockout risk; added a runbook note to infra/README.md ('Azure SQL Entra-only auth cutover: never redeploy the flip before the manual grant script has run'), alongside the existing databaseProvider-switch warning it sits next to in spirit. Both cross-reference Story 1.11 (next story slot in Epic 1, after 1.10) as the governing story, and ARCHITECTURE-SPINE/invariants-rules.md#ad-21.
- (event) spine finalized (AD-21 added + reviewer-gate fixes applied; index.md TOC updated for AD-20/AD-21; lint_spine.py clean apart from the 2 known {id}-route-param false positives). Next: bmad-create-story for Story 1.11 under Epic 1 (Foundation, Deployment, Household & Access) to implement AD-21.
- (event) AD-21 amended post-finalization during Story 1.11 drafting: a fresh-context story validator flagged that AD-21's Rule text treated the inline administrators block and the Microsoft.Sql/servers/azureADOnlyAuthentications child resource as interchangeable ways to flip azureADOnlyAuthentication. Verified against current Microsoft Learn (learn.microsoft.com/azure/templates/microsoft.sql/servers) rather than trusting either the validator's specific claim (which was itself wrong -- principalType IS a valid administrators field) or the original AD-21 text: the administrators block's schema does include an azureADOnlyAuthentication field, but Microsoft's own docs state it 'can be utilized during server creation and for server updates, except for the azureADOnlyAuthentication property -- to update [it], an individual API must be used.' AD-21's Rule tightened: Deploy A's administrators block omits azureADOnlyAuthentication entirely (never relies on it); Deploy B is now specified as a separate azureADOnlyAuthentications child resource (name 'Default'), gated by a new bool param so one template serves both deploys via a param-value change. Also folded in from the same validator pass: a what-if validation step before either deploy, and a note to decide (not invent ad hoc) the bicepparam value-sourcing pattern for the new Entra Admin params.
- (event) Story 1.11 (azure-sql-entra-id-only-authentication) drafted under Epic 1, validated by a fresh-context subagent, corrected per the Microsoft-Learn-verified administrators/azureADOnlyAuthentications distinction above, and set to ready-for-dev. epic-1 reopened to in-progress in sprint-status.yaml. File: _bmad-artifacts/implementation/1-11-azure-sql-entra-id-only-authentication.md.
