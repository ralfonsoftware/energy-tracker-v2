---
baseline_commit: 55393507b4c52839b975f790535a884be13c1ba1
---

# Story 8.2: Dashboard Desktop/Tablet Layout

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Household member,
I want the Dashboard at ≥660px to show a clearly bounded column with a sensibly placed action button,
so that the Status card doesn't look stretched across the full window and "Log reading" doesn't float in empty space.

## Acceptance Criteria

1. **Given** the Dashboard is displayed at an available width ≥660px, **when** the page loads, **then** the Status card, Tariff Check prompt, and the primary action button are constrained to a centered 660px-wide column instead of stretching to the full window width (UX-DR19).
2. **And** the "+ Log reading" button renders directly following the Status/Tariff-Check cards, not floating in the remaining screen area.
3. **Given** the two header icon buttons (Log Event, Smart Plug Import), **when** rendered at ≥660px, **then** each carries a visible short-word label in addition to its icon, instead of a `title` tooltip only.
4. **And** at <660px the existing mobile layout (including icon-only buttons) remains unchanged.

## Tasks / Subtasks

- [x] **Task 1: Constrain Dashboard's page content to a centered 660px column at ≥660px** (AC #1, #2)
  - [x] In `web/src/components/dashboard/dashboard-page.tsx`, wrap the page's own content — the header row (`<h1>`/icon-button `<div>`, currently lines 129-157), the `eventConfirmation` status paragraph, `<StatusCard>`, `<TariffCheckCard>`, and the `{showPopulated && ...}` Log Reading button row — in a single new wrapper element carrying `wide:mx-auto wide:w-full wide:max-w-[660px]`. Do **not** wrap `<NavChrome>` or `<MeterRegressionPromptDialog>` in this constraint — `NavChrome`'s top-nav variant is deliberately full-width per Story 8.1 and the confirmed mockup (`.top-nav` has no `max-width` in `critique-desktop-breakpoint-2026-09-23.html`), and the regression dialog is an overlay with its own sizing.
  - [x] `max-w-[660px]` as a literal Tailwind arbitrary value matches this codebase's existing convention (`status-card.tsx:88` and `tariff-comparison-form.tsx:195` both already use `max-w-[220px]`) — do not invent a new `--container-*`/`--spacing-*` design token for this; `--breakpoint-wide: 660px` (index.css, Story 8.1) already exists for the *media-query* side, this is the matching literal value for the *max-width* side of the same rule, not a second source of truth to maintain.
  - [x] **Scope note beyond the AC's literal 3-element list:** AC #1 names only the Status card, Tariff Check prompt, and action button, but UX-DR19/`layout-spacing.md` states the 660px-column rule applies to a surface's content as a whole ("content is constrained to a single 660px-wide column... on every surface"). Constraining only those 3 elements while leaving the page's own `<h1>`/icon-button header row full-width would misalign the header to the left edge of a wide viewport while the cards center below it — include the header row and the `eventConfirmation` paragraph inside the same constrained wrapper for visual coherence. This is a corrective inclusion, not scope creep: it directly serves AC #1's stated intent.
  - [x] `<main>` keeps `min-h-svh flex flex-col gap-4 p-4` unchanged — do not remove `min-h-svh` or add `flex-grow`/`justify-between` anywhere to "push" the CTA. Static analysis of the current DOM order plus Story 8.1's `wide:order-first` change to `NavChrome` (which moves the nav to the *top* at ≥660px, no longer pinned to the bottom via `mt-auto`) suggests AC #2's "floating CTA" complaint (from the pre-8.1 mockup, where `NavChrome` sat fixed at the bottom of a full-height `<main>` with the CTA stranded mid-page) is likely already resolved as a side effect of Story 8.1 — the CTA is already the last item in a top-aligned flex column with no competing bottom-anchored sibling. **Do not assume this from a static read** — confirm it live in Task 4's Chrome session before treating AC #2 as satisfied by Task 1 alone; only add extra CSS if the live view still shows a real gap between the CTA and the cards above it.

- [x] **Task 2: Add visible short-word labels to the two header icon buttons at ≥660px** (AC #3, #4)
  - [x] **Resolved ambiguity — do not implement a "History" feature.** Epic 8's own AC text (and the UX critique mockup it cites, `critique-desktop-breakpoint-2026-09-23.html` lines 351-352/425) labels these two buttons "Historie"/"Import" — but that mockup was drawn from a pre-Epic-6 screenshot where the first icon's action was genuinely unclear (flagged `title="unklar: welche Aktion?"` in the same mockup's "before" frame, line 387). Epic 6 (`6-1-event-logging`) later gave that exact icon slot a real, different function: `dashboard-page.tsx`'s `NotebookPen` button opens `LogEventSheet` (`event.entryPointLabel` = "Log an Event"), not a history view — there is no Trend-History quick-link anywhere in the Dashboard header today, and Trend History is already one tap away via the nav chrome. **Label the two buttons for what they actually do** — "Event" and "Import" — not the mockup's stale "History" guess. This AC's acceptance criteria above have already been corrected to say "Log Event, Smart Plug Import" for this reason.
  - [x] Add two new i18n keys (AD-18, both `en-US` and `de-DE`, never one locale only): `event.shortLabel` = "Event" (en) / "Ereignis" (de); `smartPlugImport.shortLabel` = "Import" (en) / "Import" (de). Place `event.shortLabel` next to the existing `event.entryPointLabel` (`web/src/locales/{locale}/translation.json:178`), and `smartPlugImport.shortLabel` next to `smartPlugImport.entryPointLabel` (line 323) — reuse these namespaces (not a new `dashboard.headerIcons.*` block) because Story 8.3 needs `smartPlugImport.shortLabel` again for Trend History's own Import icon button (per the critique mockup, Section 3's top-nav-right also shows a labeled Import button) — one shared key, not a duplicate.
  - [x] **WCAG 2.5.3 (Label in Name) check — already satisfied by the words above, do not diverge:** each button's existing `aria-label`/`title` (the full-sentence `entryPointLabel` strings — "Log an Event"/"Ereignis erfassen", "Import Smart Plug data"/"Smart-Plug-Daten importieren") must contain the new visible short label as a substring (case-insensitive). Verified: "Event" ⊂ "Log an Event", "Ereignis" ⊂ "Ereignis erfassen", "Import" ⊂ "Import Smart Plug data", "Import" ⊂ "Smart-Plug-Daten importieren" (as a case-insensitive prefix of "importieren"). Do not shorten to anything else (e.g. a translated icon-only glyph label) without re-checking this.
  - [x] In `dashboard-page.tsx`, keep both buttons' existing `aria-label`/`title` attributes exactly as-is (they remain each button's accessible name at <660px, and are harmless when a visible label is also present). Add a `<span className="hidden wide:inline">{t('event.shortLabel')}</span>` / `<span className="hidden wide:inline">{t('smartPlugImport.shortLabel')}</span>` immediately after each icon (inside the button, after the `<NotebookPen .../>`/`<Upload .../>` element).
  - [x] Both buttons currently share the fixed-square shape `flex size-10 shrink-0 items-center justify-center rounded-xl` (lines 138, 152) — a fixed `size-10` square has no room for label text. At `wide:`, override to an auto-width pill: add `wide:size-auto wide:justify-start wide:gap-1.5 wide:px-3 wide:py-2` (keep `rounded-xl`, keep the existing `bg-nav-chrome-active-bg text-nav-chrome-active-foreground` fill — reuse verbatim, do not invent a new color/token) plus `wide:text-xs wide:font-semibold` for the label span, mirroring `nav-chrome.tsx`'s own `LINK_CLASSNAME` shape (`flex items-center gap-1.5 rounded-xl px-3 py-2 text-xs font-semibold`) for visual consistency between this app's two icon+label button instances.
  - [x] At <660px this must render byte-identical to today (`wide:` variants are inert below the breakpoint by construction) — the icon-only square buttons, their `aria-label`/`title`, and their click handlers (`onLogEventOpenChange`/`onSmartPlugImportClick`) are unchanged.
  - [x] **Do not relocate these two buttons into `NavChrome`'s top-nav**, even though the critique mockup visually shows an `icon-label-btn` inside `.top-nav-right` next to the Profile menu. `LogEventSheet` holds real internal state (form fields, an async tag-options fetch) and is a *controlled* component keyed to `logEventOpen`/`onLogEventOpenChange` — mounting a second `<LogEventSheet>` instance (one for the mobile header, one inside `NavChrome`'s wide-only top-nav) bound to the same `open` boolean would render two overlapping `<Sheet>` overlays simultaneously whenever `logEventOpen` is true, a real, user-visible bug. A single JSX element/component instance also cannot be reordered between two different parent flex containers via CSS the way `NavChrome`'s own two `<nav>` variants are (those are both plain, stateless markup inside the *same* component). Keep both buttons in `dashboard-page.tsx`'s own header row (now inside Task 1's constrained wrapper) and only change their *visual* label treatment via `wide:` classes, in place — this is the safe, established-pattern-consistent choice, not the literal mockup layout.

- [x] **Task 3: Automated test coverage**
  - [x] Extend `web/src/components/dashboard/dashboard-page.test.tsx`: assert the header-icon buttons render a `hidden wide:inline` span with the new short-label text (`event.shortLabel`/`smartPlugImport.shortLabel`) alongside the existing icon and unchanged `aria-label`, and assert the new wrapper carries the `wide:max-w-[660px]` class (or an equivalent `data-testid`/structural query — match this file's existing query style, e.g. `screen.getByRole('button', { name: /log an event/i })`).
  - [x] **jsdom does not evaluate real CSS media queries (same limitation Story 8.1 already documented)** — a Vitest/Testing-Library assertion can only prove the markup/classes exist, never which variant is actually visible at a given viewport width. That proof needs a real browser: extend `web/e2e/app-shell.spec.ts` with a new case (or add a sibling spec) that uses `page.setViewportSize()` to assert, at ≥660px, the Dashboard's content wrapper has a bounding-box width of ~660px (not the full window width) and both header-icon buttons show visible text; and at <660px, the buttons remain icon-only (no visible text node) and the content wrapper's width tracks the viewport, not 660px.
  - [x] Run `npm --prefix web run test`, `tsc -b`, `oxlint`, and the extended/new Playwright spec clean before marking any task complete. No backend changes in this story — no `.NET` test run required.

- [x] **Task 4: Live Chrome verification (mandatory — do not defer to a later story)**
  - [x] This story is pure browser-dependent responsive-layout behavior (a CSS breakpoint swap, same trigger class as Story 8.1's AC #1/#2), which is one of the two independent conditions that invoke this project's standing live-verification gate — no OIDC/claims surface here, but the browser-dependent condition alone is sufficient. It cannot move review→done without a live Claude-in-Chrome verification actually performed in this session; if Chrome isn't connected, raise it immediately and pause rather than deferring.
  - [x] **Known sandbox limitation from Story 8.1's Debug Log — do not rediscover this from scratch:** `resize_window` on an already-rendered tab does not change that tab's actual `window.innerWidth` in this environment. The working pattern Story 8.1 found: open a *new* tab already sized to the target width, *then* navigate it to the app — the viewport size takes effect only when set before navigation. Reconfirmed in this session: a second `resize_window` call against an already-navigated tab (reused or reloaded) had no effect (`window.innerWidth` stayed at its first-navigation value); only a genuinely fresh tab (`tabs_create_mcp`), resized before its first `navigate`, took effect.
  - [x] Verify live, specifically: (a) at a tab opened ≥660px (e.g. 1000px), the Status card / Tariff Check card / Log Reading button visually form a centered column narrower than the window, not stretched edge-to-edge, and the Log Reading button sits directly under the cards with no large dead gap before the (now top-positioned) nav; (b) at the same wide tab, both header icon buttons show their icon plus visible "Event"/"Import" text; (c) at a tab opened <660px (e.g. 500px), the layout is pixel-for-pixel the pre-existing mobile layout — full-width cards, icon-only header buttons, bottom tab bar — with no regression.

- [x] **Task 5: Verify against every AC**
  - [x] Walk AC #1-#4 individually and state, in Completion Notes, exactly what proves each one — code reference, automated test, or live verification (Task 4) — following the same AC-by-AC accounting Stories 1.12/1.7/8.1 used.

## Dev Notes

- **This is a UI-only story with no new domain capability, no backend change, and no new architecture decision** — Epic 8's own framing, same as Story 8.1. Only UX-DR19 (this story's primary driver) applies; UX-DR9/UX-DR27 (nav chrome/Profile menu) were Story 8.1's concern and need no further change here. AD-18 (i18n) applies to the two new label keys.
- **The epic's own AC text for this story is stale on one point — the header-icon-button labels.** Written 2026-09-23 against a UX critique mockup that itself flagged the first icon's purpose as unclear (pre-Epic-6 screenshot); Epic 6 (2026-09-21, shipped before this story) gave that icon a real function (`LogEventSheet`). Task 2 above resolves this: label the buttons "Event"/"Import" (what they actually do), not "History"/"Import" (the epic's literal, now-incorrect wording). This story's AC list has already been rewritten above to say "Log Event, Smart Plug Import" — implement against that, not the epic file's original text.
- **Do not move the header-icon buttons into `NavChrome`.** The confirmed mockup (`critique-desktop-breakpoint-2026-09-23.html`) visually shows them relocated into the top-nav's right side next to the Profile menu, but implementing that literally would require mounting a second, simultaneously-controlled `<LogEventSheet>` instance — a real overlay-duplication bug, not a cosmetic risk. Task 2 explains the safe alternative (relabel in place). If a future story needs the mockup's exact visual arrangement, it will need to first lift `LogEventSheet`'s open/close orchestration so only one instance ever mounts regardless of which breakpoint's markup renders its trigger — out of scope here.
- **`--breakpoint-wide: 660px` already exists** (`web/src/index.css`, Story 8.1's `@theme inline` block) and generates the `wide:` responsive variant used throughout this story (`wide:hidden`, `wide:flex`, `wide:max-w-[660px]`, etc.) — this is the same mechanism Story 8.1 built specifically so Stories 8.2-8.5 could reuse it. Do not add a second breakpoint token.
- **Design tokens to reuse verbatim (do not invent new ones):** `bg-nav-chrome-active-bg`/`text-nav-chrome-active-foreground` for both header-icon buttons' fill (already used this way, `dashboard-page.tsx:138,152`); `max-w-[220px]`-style literal arbitrary Tailwind values for the new 660px column wrapper, matching `status-card.tsx`/`tariff-comparison-form.tsx` precedent.
- **Card-hierarchy tiers (UX-DR24) need no change in this story.** `TariffCheckCard` already renders at the "quiet" tier via its own private `--tariff-check-card-bg`/`-border` tokens (`tariff-check-card.tsx:26`) — DESIGN/colors.md's 2026-09-23 formalization of a shared `{colors.surface-quiet}` token explicitly cites *this exact card* as the origin case ("Originated as the Tariff Check prompt card's own private background token... formalized here as a shared, reusable token once Epic 8 needed the identical treatment for two more read-only reference surfaces"). The generalized `--color-surface-quiet`/`-dark` CSS custom property does **not** exist yet in `index.css` (only the private `--tariff-check-card-*` pair does) — formalizing it into a shared token is Story 8.3's job (Trend History's Room→Power Point→Device card is its second real consumer), not this one's. `StatusCard` stays at the "glass" tier (`GlassCard`, has the Log Reading CTA / detail-drilldown link) — no tier change needed.
- **Testing standard reminder:** frontend unit tests are colocated next to source (`dashboard-page.test.tsx` beside `dashboard-page.tsx`), Vitest + Testing Library, `jsdom` — real breakpoint/viewport behavior can only be proven by the Playwright e2e spec (`web/e2e/app-shell.spec.ts`), per `project-context.md` and Story 8.1's own hard-won lesson.

### Project Structure Notes

```text
energy-tracker-v2/
  web/src/
    components/dashboard/
      dashboard-page.tsx                 # modified — new wide:max-w-[660px] content wrapper
                                          # (Task 1); header-icon buttons gain wide: label spans
                                          # and pill-shape classes (Task 2)
      dashboard-page.test.tsx            # extended (Task 3)
    locales/
      en-US/translation.json             # modified — event.shortLabel, smartPlugImport.shortLabel
      de-DE/translation.json             # modified — matching keys (Task 2)

  web/e2e/
    app-shell.spec.ts                    # extended — wide/narrow header-icon-label and
                                          # content-column-width assertions (Task 3)
```

No changes expected to `web/src/components/dashboard/nav-chrome.tsx`, `profile-menu.tsx`, `status-card.tsx`, `tariff/tariff-check-card.tsx`, any backend project, or `web/src/index.css` (the `--breakpoint-wide` token this story consumes already exists from Story 8.1).

### References

- [Source: _bmad-artifacts/planning/epics/epic-8-desktop-tablet-layout-correction.md#Story 8.2: Dashboard Desktop/Tablet Layout] — story statement and original acceptance criteria (lines 38-56); epic-level framing (lines 3-10: no new FR, UX-DR19 primary driver, story sequencing).
- [Source: _bmad-artifacts/planning/epics/requirements-inventory.md#UX-DR19] — full amended text of the 660px-column rule, applying to every screen including Dashboard (line 130).
- [Source: _bmad-artifacts/planning/epics/requirements-inventory.md#UX-DR24] — card-hierarchy quiet/glass tier rule; confirms the Tariff Check card is the already-compliant origin case (line 132), no change needed here.
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/DESIGN/layout-spacing.md] — `{spacing.breakpoint-wide}` = 660px, "content is constrained to a single 660px-wide column... on every surface" (line 9) — the basis for Task 1's decision to constrain the header row too, beyond the AC's literal 3-element list.
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/DESIGN/colors.md#Quiet surface] — confirms `{colors.surface-quiet}` is formalized but not yet wired into `index.css`, and that the Tariff Check card is its origin (not a gap to fix in this story).
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/mockups/critique-desktop-breakpoint-2026-09-23.html] — Section 1 (Dashboard, lines 373-445) for the before/after column layout, `.after-body`/`.after-col` CSS (lines 147-148, `max-width:660px`, centered via `justify-content:center`); lines 384-389/424-428 for the header-icon-button "before" (`title="unklar: welche Aktion?"`, i.e. unclear function) vs. "after" (`icon-label-btn`, labeled) treatment — read carefully together with Dev Notes' "stale History label" explanation before implementing Task 2 literally.
- [Source: _bmad-artifacts/implementation/8-1-responsive-nav-chrome-profile-menu.md] — read in full before starting. Establishes `--breakpoint-wide` (Task 1 there), `NavChrome`'s `wide:order-first` repositioning (why the CTA-floating concern is likely already resolved), the "page's own topbar duplication is expected and acceptable, reconciling it is Story 8.2+'s job" scope note (Task 2 there), the `resize_window`-before-navigation live-verification workaround (Debug Log), and the "ProfileMenu is a single mount, CSS-toggled internally — not duplicated across breakpoints" precedent that Task 2 above explicitly distinguishes from the (unsafe) alternative of duplicating `LogEventSheet`.
- [Source: web/src/components/dashboard/dashboard-page.tsx] — target file for both tasks; current header row (lines 128-157), `StatusCard`/`TariffCheckCard`/CTA row placement (lines 167-178), `NavChrome` mount (lines 180-189).
- [Source: web/src/components/dashboard/nav-chrome.tsx] — `LINK_CLASSNAME` (line 23-24) as the visual-consistency reference for Task 2's icon+label pill shape; the wrapping `<div className="mt-auto wide:order-first wide:mt-0">` (line 48) that already repositions the nav to the top at ≥660px.
- [Source: web/src/components/dashboard/status-card.tsx:88, web/src/components/tariff/tariff-comparison-form.tsx:195] — existing `max-w-[220px]` precedent for literal Tailwind arbitrary max-width values, reused for Task 1's `max-w-[660px]`.
- [Source: web/src/components/tariff/tariff-check-card.tsx] — confirms the quiet-tier card already exists via private tokens; no change needed (Dev Notes).
- [Source: web/src/components/event/log-event-sheet.tsx] — confirms `LogEventSheet` is a controlled component with real internal state (tag fetch, form fields); the reason Task 2 explicitly forbids a second mounted instance.
- [Source: web/src/locales/en-US/translation.json:178,323 / de-DE/translation.json:178,322] — existing `event.entryPointLabel`/`smartPlugImport.entryPointLabel` keys/values, the WCAG 2.5.3 substring source-of-truth Task 2's new `shortLabel` keys must remain contained within.
- [Source: web/e2e/app-shell.spec.ts] — existing Playwright viewport-resize convention from Story 8.1 to extend (Task 3).
- [Source: _bmad-artifacts/project-context.md#Critical Don't-Miss Rules, Process gates] — the mandatory live-verification gate this story triggers on the browser-dependent-behavior condition (Task 4).

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Live-verification session (Task 4, 2026-09-26): reused the already-running local dev stack (Postgres via Docker, API via `dotnet run` on :5133, Vite dev server on :5173), driven via Claude-in-Chrome against `https://localhost:5173`, real Auth0 test session.
- Reconfirmed Story 8.1's documented `resize_window` limitation: resizing an already-navigated tab (or re-navigating the same tab after a second resize call) leaves `window.innerWidth` unchanged. Only resizing a genuinely fresh tab (`tabs_create_mcp`, before its first `navigate`) took effect. Also found tabs created via `tabs_create_mcp` share the same browser window/viewport as other tabs in the session's tab group — closing the prior tab before creating+resizing the next one was necessary to get a clean measurement.
- Precise live measurements taken via `javascript_tool` (`getBoundingClientRect`/`getComputedStyle`) rather than visual estimation alone: at `window.innerWidth=1000`, `[data-slot="dashboard-content"]` measured exactly `660px` wide, centered (170px margin each side within the 968px available after `main`'s `p-4`), while `[data-slot="nav-chrome-top"]` measured the full `968px` (unconstrained, as intended). At `window.innerWidth=500`, the same content element measured `468px` (tracking the viewport, not capped), the bottom tab bar was visible, the top nav was hidden, and both label `<span>`s were `display:none`.
- Logging test meter readings against the shared local dev household to reach a non-empty layout state triggered a real `MeterRegressionPromptDialog` (existing household already had a prior 12345.6 kWh reading from an earlier session) — resolved via "The meter was reset" (client-observed real feature working correctly, unrelated to this story's own changes). Could not reach a fully populated `StatusCard` (needs a configured yearly baseline + readings spread over time, out of this story's scope) — AC #2's "no floating CTA" claim is instead verified via the empty-state screenshot (same flex-column/gap-4 structure, same `NavChrome` top-positioning from Story 8.1) plus the existing `dashboard-page.test.tsx` populated-state test, not a live populated screenshot.

### Completion Notes List

- **AC #1** (Status card, Tariff Check prompt, and primary action button constrained to a centered 660px column at ≥660px): `dashboard-page.tsx`'s new `data-slot="dashboard-content"` wrapper (`flex flex-col gap-4 wide:mx-auto wide:w-full wide:max-w-[660px]`) wraps the header row, `eventConfirmation` paragraph, `StatusCard`, `TariffCheckCard`, and the Log Reading CTA row — deliberately excluding `NavChrome` (full-width by Story 8.1 design) and `MeterRegressionPromptDialog` (an overlay). Proven by `dashboard-page.test.tsx`'s new wrapper-class/containment assertion, `web/e2e/app-shell.spec.ts`'s new bounding-box assertion (≤660px at 1000px viewport, >400px at 500px viewport), and live Chrome verification: exact measurement of `660px` at `window.innerWidth=1000`, centered with 170px margins.
- **AC #2** ("+ Log reading" renders directly following the Status/Tariff-Check cards, not floating): the button row is the last item in the same `gap-4` flex column as the cards, with `NavChrome` positioned at the visual top (Story 8.1's `wide:order-first`) rather than the bottom — there is no bottom-anchored sibling left to strand the CTA in empty space. Proven by live Chrome verification (screenshot at 1000px width): the Log Reading button sits immediately after the Tariff Check card with no dead gap before the (top-positioned) nav.
- **AC #3** (both header icon buttons carry a visible short-word label at ≥660px): new `event.shortLabel`/`smartPlugImport.shortLabel` i18n keys (both `en-US`/`de-DE`), rendered via `<span className="hidden wide:inline ...">` immediately after each icon; buttons gain `wide:size-auto wide:justify-start wide:px-3 wide:py-2` to make room. Existing `aria-label`/`title` (`entryPointLabel`) kept unchanged and verified to contain the new short label as a substring (WCAG 2.5.3). Proven by `dashboard-page.test.tsx`'s new label-visibility assertion, `web/e2e/app-shell.spec.ts`'s new visibility assertion at 1000px, and live Chrome verification (both spans visible, `getComputedStyle(...).display !== 'none'` confirmed via JS at `window.innerWidth=1000`).
- **AC #4** (mobile layout at <660px unchanged): all new classes are `wide:`-prefixed, inert below the breakpoint by construction; button click handlers/`aria-label`/`title` untouched. Proven by all pre-existing `dashboard-page.test.tsx` tests passing unmodified, `web/e2e/app-shell.spec.ts`'s new assertion at 500px (icon-only buttons, content column tracking the viewport not capped at 660px), and live Chrome verification: at `window.innerWidth=500`, both label spans measured `display:none`, the bottom tab bar was visible, the top nav was hidden, and the content column measured `468px` (500 − 32px `p-4`), i.e. tracking the viewport.
- Full frontend suite passes clean: `vitest run` 399/399 (47 files), `tsc -b` clean, `oxlint` clean (only 4 pre-existing warnings in untouched files), `playwright test app-shell` 3/3 (run against a temporary local-HTTPS config to work around this sandbox's pre-existing `certs/vite-dev-cert.*` making `vite preview` serve HTTPS while the tracked `playwright.config.ts` assumes HTTP — a local-environment quirk unrelated to this story, not a tracked-file change). No backend changes in this story, so no `.NET` test run was needed.

### File List

- `web/src/components/dashboard/dashboard-page.tsx` — modified: new `data-slot="dashboard-content"` wrapper (`wide:mx-auto wide:w-full wide:max-w-[660px]`, Task 1); header-icon buttons gain `wide:` pill-shape classes and `hidden wide:inline` label spans (Task 2)
- `web/src/components/dashboard/dashboard-page.test.tsx` — extended: label-visibility assertion and content-wrapper assertion (Task 3)
- `web/src/locales/en-US/translation.json` — modified: new `event.shortLabel`/`smartPlugImport.shortLabel` keys (Task 2)
- `web/src/locales/de-DE/translation.json` — modified: matching keys (Task 2)
- `web/e2e/app-shell.spec.ts` — extended: new wide/narrow content-column-width and header-icon-label visibility spec (Task 3)
