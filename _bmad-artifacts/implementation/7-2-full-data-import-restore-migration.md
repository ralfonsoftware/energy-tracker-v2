---
baseline_commit: eac96e37e4c44d982bbdb7bba2c3d4293953991a
---

# Story 7.2: Full Data Import (Restore / Migration)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Household member,
I want to import a previously exported v2 dataset,
so that I can restore my instance after a disaster or move it to new hosting.

## Acceptance Criteria

1. **Given** a previously exported v2 dataset, **when** I import it, **then** the import validates against the documented v2 export format. [Source: epics/epic-7-data-export-import-disaster-recovery.md#Story 7.2; PRD FR-23]
2. **Given** malformed data in the import file, **when** validation runs, **then** it's rejected and reported with what failed — never partially applied. [Source: epic-7...md#Story 7.2; PRD FR-23; UX-DR14]
3. **Given** the import mechanism, **when** used, **then** it only supports v2-to-v2 restore/migration — it does not read or convert v1 data. [Source: epic-7...md#Story 7.2; PRD FR-23]
4. **Given** a Household that already has data, **when** an import is attempted, **then** it's blocked by default, requiring an explicit "replace all data" confirmation step — there is no partial-merge import mode. [Source: epic-7...md#Story 7.2; PRD FR-23]
5. **Given** a successful "replace all data" import, **when** it completes, **then** it's treated as a wholesale replace, not an edit — it does not go through the `IAuditCorrectionRecorder` mechanism used for regular Meter Reading/Tariff edits. [Source: epic-7...md#Story 7.2; AD-11]
6. **Given** the import surface, **when** reached, **then** it's accessible from Settings. [Source: epic-7...md#Story 7.2; UX-DR12]

## Tasks / Subtasks

- [x] **Task 1 — Decide and document the import/restore design** (AC: #1, #2, #4, #5, #6; resolves Epic 6 Retro Action Items #3, #4 — see Dev Notes "Kickoff checks (mandatory)")
  - [x] Decide the upload → validate → confirm → apply flow shape. **Decided as recommended**: two phases — (a) synchronous upload+structural-validation (`POST /api/household-import`, no DB write), (b) explicit confirm (`POST /api/household-import/{token}/confirm`) enqueuing the wholesale delete+insert as an async background job (AD-6).
  - [x] Decide and **disclose**: does restore always require the "replace all data" confirmation, even for a Household with zero existing rows? **Decided as recommended: yes, always** — one code path, no "does this Household have any data" branch.
  - [x] Decide and **disclose** the destructive-write target: restore always targets the **current authenticated session's Household** (`ICurrentHouseholdAccessor.HouseholdId`/`JobHouseholdContext.HouseholdId`) — confirmed and documented in `docs/data-import-restore.md`.
  - [x] Decide and disclose the max upload file size: **250 MB**, documented in `docs/data-import-restore.md`. Kestrel's `MaxRequestBodySize` (~28.6 MB default) and `FormOptions.MultipartBodyLengthLimit` (128 MB default) are both raised to match, once, at the composition root (`Program.cs`) — confirmed a per-endpoint `IHttpMaxRequestBodySizeFeature` override would be too late for a Minimal API `IFormFile`-bound endpoint (the body is already read before the delegate runs), so the raise is global instead (safe: `SmartPlugImportEndpoints` still enforces its own lower 20 MB cap independently). The Vite dev proxy imposes no body-size limit of its own — confirmed, no change needed.
  - [x] New doc `docs/data-import-restore.md` — the validate/confirm/apply contract, the structured validation-failure reporting shape, the restore semantics (delete/insert order, Household-in-place-update exception, session-continuity design, out-of-scope entities), the in-memory-registry trade-off, and the bulk-write safety measures reused from Stories 3.8–3.10. Updated `docs/data-export-format.md`'s own intro sentence to point at it.

- [x] **Task 2 — Application: import validation** (AC: #1, #2, #3)
  - [x] New `EnergyTracker.Application/ValidateHouseholdImport.cs` — deserializes against the same DTO family `ExportHouseholdData.cs` defines (no parallel schema); manually walks the parsed `JsonDocument` field-by-field first (collecting every structural failure) and only calls `JsonSerializer.Deserialize<HouseholdExportResult>` once that passes, so a missing/wrong-type field can never silently produce a null/default value that later code trusts.
  - [x] Rejects anything whose `formatVersion` isn't exactly `"v2"` — reported alone, before any other field is checked (AC #3).
  - [x] Collects every structural failure (missing field, wrong JSON type, unparseable `classification`/`status` enum strings — case-insensitive against the exact lowercase names `ExportHouseholdData.cs` emits) before returning — never fails fast on the first problem (AC #2). Covered by `ValidateHouseholdImportTests.cs` (19 tests), including an explicit "collects every failure, not just the first" test.
  - [x] Zero DB access anywhere in this class — pure JSON parse/validate, satisfying "never partially applied" structurally.

- [x] **Task 3 — Application: `RestoreHouseholdData` use case (wholesale delete + insert)** (AC: #1, #4, #5, #6)
  - [x] New `EnergyTracker.Application/RestoreHouseholdData.cs`, `ExecuteAsync(Guid householdId, RestoreHouseholdDataPayload payload, CancellationToken)` (mirrors `ProcessSmartPlugImport`'s own payload-taking signature/file-I/O-inside-the-use-case shape) — `householdId` is always the current Household from the job envelope, never read from the file.
  - [x] `RestoreHouseholdData`'s only dependency is `IHouseholdRestoreWriter` — there is no `IAuditCorrectionRecorder` reference anywhere in this path to call (AD-11 carve-out), proven structurally by `RestoreHouseholdDataTests`.
  - [x] Household's own row is updated in place (`HouseholdRestoreWriter.UpdateHouseholdSettingsAsync`) — `Id`/`CreatedAtUtc` never touched, `AiPlausibilityBackendConfigured`/`Label` never read back (AD-19).
  - [x] Every other in-scope entity is deleted then reinserted verbatim, `HouseholdId` set explicitly on every reconstructed entity, every internal cross-reference (`PowerPoint.RoomId` etc.) reused byte-for-byte — confirmed by `HouseholdRestoreWriterTests` (dual-provider Testcontainers).
  - [x] `SmartPlugReading.SmartPlugImportId` always `null` on insert.
  - [x] **FK-dependency order confirmed against the real `*Configuration.cs` files** (read during implementation, not assumed) — the story's proposed order was correct as written; every FK between these entity types is `DeleteBehavior.Restrict` (never `Cascade`), and `Event.TaggedEntityId`/`AuditCorrection.EntityId` are confirmed plain polymorphic `Guid` columns with no DB-enforced FK (both configuration files' own comments say so explicitly). **One real, previously-undocumented FK conflict was found and fixed**: `BackgroundJob.QueuedByHouseholdMemberId` → `HouseholdMember` was `DeleteBehavior.Restrict`, which made every restore fail — caught by `HouseholdImportEndpointsTests`' own real-Postgres end-to-end round-trip test, not by unit tests alone — because the confirm endpoint's own `BackgroundJob` row (for the restore job itself) already references the current member before the delete phase runs. Fixed by changing that FK to `SetNull` (mirrors `SmartPlugReading.SmartPlugImportId`'s own precedent) via a new dual-provider migration `BackgroundJobQueuedByMemberSetNull`. **A second, identical-shaped FK conflict was found live during Task 8's own verification** against a real household with historical Smart Plug import data: `SmartPlugImportGap.PowerPointId` → `PowerPoint` was also `DeleteBehavior.Restrict` — fixed the same way, via `SetNull` + migration `SmartPlugImportGapPowerPointSetNull`. Every other out-of-scope table's FK into an in-scope entity was audited afterward (`grep`-level review of every `*Configuration.cs` file) and confirmed clear — these two were the only ones. See Completion Notes.
  - [x] Every delete and insert chunked at 200 rows (`HouseholdRestoreWriter.ChunkSize`, reusing `SmartPlugImportRepository.DeleteBatchSize`'s proven value), `CommandTimeout` bumped to 120s, one outer transaction spans every chunk (delete and insert alike).
  - [x] **AD-2 compliance:** plain EF Core LINQ only (`ExecuteDeleteAsync`/`AddRangeAsync`/`SaveChangesAsync`) — no `EFCore.BulkExtensions`.

- [x] **Task 4 — Backend: async job wiring** (AD-6) (AC: #1, #4)
  - [x] `JobTypes.RestoreHouseholdData` constant added.
  - [x] `RestoreHouseholdDataPayload(string TempFilePath, string OriginalFileName)` record — temp-file path only, never file bytes.
  - [x] `case JobTypes.RestoreHouseholdData:` added to `BackgroundJobProcessor.cs`'s switch, following the existing pattern exactly.
  - [x] `HouseholdImportValidationException` defined (mirrors `SmartPlugImportValidationException`) — used for the one real failure mode reachable at job time (the temp file missing due to the validate→confirm race), never for a second validation pass (structural validation already happened synchronously before the job was ever enqueued). `BackgroundJobProcessor`'s exception-forwarding check extended to include it alongside `SmartPlugImportValidationException`.

- [x] **Task 5 — API: endpoints** (AC: #1, #2, #4, #6)
  - [x] `POST /api/household-import` — multipart upload, runs Task 2's synchronous validation, no DB write. `400 ProblemDetails` with every failure under the `failures` extension property on rejection; `200 OK` with an opaque `Guid` token + entity-count summary on success. Token→temp-file mapping held by a new singleton in-memory registry (`IHouseholdImportUploadRegistry`/`MemoryHouseholdImportUploadRegistry`), not a DB row — decision and trade-off disclosed in `docs/data-import-restore.md`.
  - [x] `POST /api/household-import/{token}/confirm` — atomically consumes the token (verifying it belongs to the current Household; wrong-Household and unknown both 404, closing the IDOR gap), enqueues `RestoreHouseholdData`, returns `202` + `jobId`, deletes the temp file on an enqueue failure.
  - [x] `GET /api/jobs/{id}` — unchanged, confirmed generic enough as-is.
  - [x] `403 ProblemDetails` via the same `TryGetHouseholdId` pattern.
  - [x] 250 MB file-size cap enforced the same way `SmartPlugImportEndpoints.MaxFileSizeBytes` is, plus the Program.cs-level Kestrel/FormOptions bump described in Task 1.

- [x] **Task 6 — Frontend: Settings import UI** (UX-DR12, UX-DR14) (AC: #4, #6)
  - [x] New `web/src/components/data-import/` folder (`data-import-panel.tsx`, `use-household-import-job.ts`, `data-import-panel.test.tsx`), wired into `settings-page.tsx` immediately after `DataExportPanel`.
  - [x] Full flow implemented: file picker → upload+validate → on failure, lists every reported failure (UX-DR14) with a "choose a different file" retry → on success, an explicit "replace all data" confirmation dialog (reusing `settings-page.tsx`'s own `Dialog`/`DialogHeader`/`DialogFooter` Logoff pattern) showing the entity-count summary → confirm → poll `GET /api/jobs/{id}` (`use-household-import-job.ts`, same 2s-interval/tolerate-transient-failures shape as `use-smart-plug-import-job.ts`) → success/failure state.
  - [x] No dedicated mockup existed for this screen (confirmed, same as 7.1's own precedent) — built directly against `GlassCard`/`Button`/`Dialog`.
  - [x] i18next copy keys under `settings.dataImport.*` added to both `en-US` and `de-DE` `translation.json` (including `_one`/`_other` pluralized summary-count keys, matching this codebase's existing plural-key convention).

- [x] **Task 7 — Tests** (all ACs)
  - [x] `EnergyTracker.Application.Tests`: `ValidateHouseholdImportTests.cs` (19 tests: every collected-failure case, case-insensitive enum parsing, "collects every failure" test) + `RestoreHouseholdDataTests.cs` (7 tests: current-Household-id propagation, `HouseholdId` set on every entity, internal cross-references preserved, temp-file cleanup on success/failure, missing-file exception, enum round-trip, structural proof no `IAuditCorrectionRecorder` dependency exists). 383/383 Application tests green.
  - [x] `EnergyTracker.Infrastructure.Tests`: `HouseholdRestoreWriterTests.cs`, dual-provider (Postgres + SqlServer Testcontainers, 10 tests total) — every inserted row carries the current Household's id; a Household with pre-existing data ends up with zero pre-existing rows after restore; another Household's data is never touched (`IgnoreQueryFilters` used correctly to make this real assertion, not filtered-out-by-construction); the session-continuity case (an imported `HouseholdMember` still resolves via issuer+subject afterward); no `AuditCorrection` row is created for the restore itself. 230/230 Infrastructure tests green.
  - [x] `EnergyTracker.Api.Tests`: `HouseholdImportEndpointsTests.cs` (8 tests) — 200+token+summary on a valid file; 400 with every failure listed on a malformed/multi-problem file; 403 with no Household; 404 on an unknown confirm token and on a different-Household's token; **full end-to-end round trip** (create data → export → validate → confirm → poll to `completed` → re-export and assert the data survived, session unbroken); double-confirm of the same token 404s the second time. 218/218 Api tests green.
  - [x] Frontend: `data-import-panel.test.tsx` (5 tests) — validation-failure list rendering, confirm-dialog summary rendering, cancel returns to idle, full confirm→poll→success flow, poll→failure rendering the job's own error message. 384/384 frontend tests green.
  - [x] Full solution build (`dotnet build EnergyTracker.sln`), `tsc -b`, `npm run lint` (oxlint), and `npm run build` (`vite build`) all clean.

- [x] **Task 8 — Live Auth0/Chrome verification** (all ACs — does not defer, hard gate per Epic 6 Retrospective Action Item #1)
  - [x] Ralf's explicit go-ahead obtained before running (AskUserQuestion, this session). Local stack started (`docker compose ... up postgres -d` already running; `scripts/run-api.sh`; `npm --prefix web run dev`), Claude-in-Chrome used against `https://localhost:5173` with the already-authenticated test-user session (`docs/local-development.md`'s dedicated Auth0 test user, not a fresh personal account). **Deviation from the story's literal recommendation, decided with Ralf mid-session:** the real household's own export was 32 MB (117,770 real Smart Plug readings from an earlier story's live import) — the browser-automation file-upload tool has a 10 MB per-call cap, so that exact file could not be fed back through the UI's file picker. With Ralf's explicit choice (AskUserQuestion), restored a copy of the same real export with `smartPlugReadings` trimmed to a 200-row sample and every other entity category kept in full — a real, disclosed, permanent reduction of that one household's Smart Plug reading history, not a synthetic fixture.
  - [x] Confirmed live: the validation-failure UI renders for a deliberately corrupted file (`{"formatVersion":"v1", ...}` → "This file failed validation" / "Unsupported formatVersion 'v1'. Only 'v2' is accepted."); the confirm dialog appeared showing the real entity-count summary (1 Meter Reading, 1 Tariff entry, 3 Events, 1 Room, 1 Power Point, 0 Devices, 200 Smart Plug Readings) and blocked on real existing data; after confirming, the job polled to `completed` and the panel rendered "Restore complete."; **the session was not broken** — Dashboard and Settings both loaded normally immediately after with no forced re-login; re-exporting afterward confirmed the household's data now exactly matches the restored file (32 MB → 57,850 bytes, `smartPlugReadings` count 117,770 → 200, every other category unchanged) — a real, verified wholesale replace, not a silent no-op.
  - [x] Claude-in-Chrome connected without incident this session (no "not connected" pause needed).
  - [x] **Two real, previously-undiscovered FK bugs found live and fixed in this same session** (neither was caught by the automated Testcontainers/Api suites, because no test fixture had ever created a real `SmartPlugImportGap` row via an actual Smart Plug import — this household's did, from earlier stories' own live verifications):
    1. `BackgroundJob.QueuedByHouseholdMemberId` → `HouseholdMember`, `Restrict` — every restore's own confirm-enqueued `BackgroundJob` row already references the current member before the delete phase runs. Fixed via `SetNull` + migration `BackgroundJobQueuedByMemberSetNull` (found first, via the automated Api.Tests round-trip, before Task 8 even started).
    2. `SmartPlugImportGap.PowerPointId` → `PowerPoint`, `Restrict` — this real household had historical Smart Plug import gap rows referencing a Power Point that restore's delete phase tries to remove. Fixed via `SetNull` + migration `SmartPlugImportGapPowerPointSetNull` (found live, during this task, on the first live restore attempt — see Completion Notes for the full trail). **Both migrations must be applied to any existing deployment before this story's restore feature is used against real data with Smart Plug import history** — a fresh/empty database created after these migrations exist needs no separate action.
  - [x] Local dev servers (API, Vite) stopped and synthetic test files removed after verification; the two real `Export data` downloads this session produced remain in `~/Downloads` (the pre-restore 32 MB backup and the post-restore 57 KB one) for Ralf to keep or delete.

### Review Findings

**Pass 1 of 2 (backend scope: Application/Infrastructure/Api C# files, the two real EF migration Up/Down bodies, and all four backend test files).**

- [x] [Review][Patch] No semantic/range validation on numeric fields (negative KwhValue, negative TariffCheckCadenceMonths/ContractPeriodMonths, etc.) [src/EnergyTracker.Application/ValidateHouseholdImport.cs:304] — Fixed: added `RequireNonNegativeDecimal`/`RequireOptionalNonNegativeDecimal`/`RequireNonNegativeInt`/`RequirePositiveInt`, applied to KwhValue, MonthlyBaseFee, PricePerKwh, ContractPeriodMonths (>0), TariffCheckCadenceMonths (>0), LowConfidenceGapDays, TrendingThresholdKwh, YearlyBaselineKwh, DigitCapacityKwh.
- [x] [Review][Patch] No guard against two concurrent confirmed restores for the same Household [src/EnergyTracker.Infrastructure/Adapters/HouseholdRestoreWriter.cs:16] — Fixed: the confirm endpoint now queries `IBackgroundJobRepository.ListByJobTypeAsync` for an existing Queued/Processing `RestoreHouseholdData` job for the Household (DB-backed, holds across replicas — an in-memory lock wouldn't) and returns 409 Conflict if found, checked after token consumption so a rejected confirm doesn't affect the "same token twice → 404" contract.
- [x] [Review][Patch] Global Kestrel/FormOptions body-size raise to 250MB widens `SmartPlugImportEndpoints`' DoS exposure [src/EnergyTracker.Api/Program.cs:360] — Fixed: the Kestrel `MaxRequestBodySize` raise is now route-scoped middleware (`/api/household-import` only, with a small headroom margin) instead of a global `ConfigureKestrel` call; every other route (including SmartPlugImportEndpoints) keeps Kestrel's original ~28.6MB default. `FormOptions.MultipartBodyLengthLimit` stays global since it's harmless once the primary Kestrel gate is scoped.
- [x] [Review][Patch] Malformed-but-structurally-valid input crashes the endpoint with an unhandled exception instead of AC #2's promised structured 400 + failures list [src/EnergyTracker.Application/ValidateHouseholdImport.cs:74] — Fixed: `exportedAtUtc` is now validated; `RequireDateTimeOffset`/`RequireOptionalDateTimeOffset` delegate to `JsonElement.TryGetDateTimeOffset` (the exact parser System.Text.Json's own converter uses, closing the divergence); `RequireDecimal` variants call `TryGetDecimal` after an explicit `ValueKind == Number` check; the final `Deserialize` call is now wrapped in try/catch as defense-in-depth.
- [x] [Review][Patch] `Enum.TryParse`/`Enum.Parse` silently accept numeric strings for undefined enum values [src/EnergyTracker.Application/ValidateHouseholdImport.cs:367] — Fixed: `RequireEnum`/`RequireOptionalEnum` now also require `Enum.IsDefined(value)`.
- [x] [Review][Patch] No cross-entity referential-integrity or duplicate-id validation [src/EnergyTracker.Application/ValidateHouseholdImport.cs:19] — Fixed: `ValidateArray` now detects duplicate ids within each entity array; new `RequireReference`/`RequireMainMeterReference` helpers check `mainMeterId`, `roomId`, `powerPointId`, `meterReadingId`/`previousMeterReadingId` against the file's own collected id sets, threaded through `Execute`'s existing dependency-respecting validation order.
- [x] [Review][Patch] Abandoned/never-confirmed uploads leak their temp file (up to 250MB) and registry entry forever [src/EnergyTracker.Infrastructure/Adapters/MemoryHouseholdImportUploadRegistry.cs:30] — Fixed: `Register` now sweeps expired entries (deleting their orphaned temp file, not just the dictionary entry) before adding the new one — no IHostedService/Timer, piggybacks on the next upload as the natural trigger.
- [x] [Review][Patch] No test verifies the restore transaction actually rolls back on a mid-restore failure [tests/EnergyTracker.Infrastructure.Tests/HouseholdRestoreWriterTests.cs] — Fixed: added `A_failure_partway_through_the_insert_phase_rolls_back_the_entire_transaction`, forcing a real DB-level FK violation mid-insert and asserting pre-existing data survives intact on both providers. Passed on first run — the transaction mechanism itself was already correct, only the test coverage was missing.
- [x] [Review][Patch] `HouseholdImportSummary`/`BuildSummary` omits `meterRegressionPrompts` from the pre-confirmation destructive-action summary [src/EnergyTracker.Api/Endpoints/HouseholdImportEndpoints.cs:146] — Fixed: added `MeterRegressionPrompts` to the record and `BuildSummary`. Note: the frontend (`data-import-panel.tsx`) doesn't yet render this new field — wiring it into the confirm-dialog summary display is Pass 2's responsibility.
- [x] [Review][Patch] File-size-cap friendly error is effectively unreachable at the boundary [src/EnergyTracker.Api/Program.cs:360] — Fixed as part of the same route-scoped middleware change above: the Kestrel limit for `/api/household-import` is now `MaxFileSizeBytes + BodySizeHeadroomBytes` (5MB headroom), so the app-level check can actually fire at the boundary instead of Kestrel rejecting first.
- [x] [Review][Defer] `HouseholdRestoreWriter.UpdateHouseholdSettingsAsync`'s `SingleAsync` throws an unhandled, non-diagnostic exception if the Household row doesn't exist [src/EnergyTracker.Infrastructure/Adapters/HouseholdRestoreWriter.cs:160] — deferred, pre-existing (currently unreachable: no household-deletion capability exists anywhere in this codebase; revisit if one is ever added)
- [x] [Review][Defer] Single outer transaction spanning the full delete+insert doesn't bound transaction duration or lock-hold time for a household with a large history [src/EnergyTracker.Infrastructure/Adapters/HouseholdRestoreWriter.cs:25] — deferred, pre-existing design trade-off (explicitly reasoned through in Dev Notes as required to guarantee "never partially applied" atomicity; worth monitoring on very large households, not actionable without redesigning the atomicity guarantee itself)
- [x] [Review][Defer] No rate limiting / concurrency bound on `/household-import`'s synchronous upload+validate path [src/EnergyTracker.Api/Endpoints/HouseholdImportEndpoints.cs:38] — deferred, pre-existing (no rate-limiting layer exists anywhere in this codebase; not introduced by this story beyond one more large-body endpoint)
- [x] [Review][Defer] `OriginalFileName` (user-supplied, unsanitized) flows into `HouseholdImportValidationException`'s message and then `BackgroundJob.ErrorMessage` [src/EnergyTracker.Application/RestoreHouseholdData.cs:29] — resolved by Pass 2: confirmed safe. `data-import-panel.tsx` renders it as plain JSX text content, never `dangerouslySetInnerHTML` — React's default escaping neutralizes it as an injection vector.

**Pass 2 of 2 (frontend + docs scope: `data-import-panel.tsx` + test, `use-household-import-job.ts`, `household-import-api.ts`, `settings-page.tsx`, both locale files, `data-import-restore.md`, `data-export-format.md`).**

- [x] [Review][Patch] `Response` body read twice on a 400 without a `failures` array, silently discarding the server's real `detail` message [web/src/lib/household-import-api.ts:58] — found independently by all three review layers. Fixed: `uploadHouseholdImportFile` now reads the body exactly once and branches on its shape (also guards the `failures: []` empty-but-truthy edge case). New test: `shows the servers own detail message for a 400 response with no failures array`.
- [x] [Review][Patch] Confirmation dialog summary omits 5 of 12 entity categories (`householdMembers`, `hasMainMeter`, `meterRegressionPrompts`, `statusSnapshots`, `auditCorrections`) [web/src/components/data-import/data-import-panel.tsx:180] — closes the gap Pass 1 explicitly assigned to Pass 2. Fixed: `HouseholdImportSummary` interface extended with `meterRegressionPrompts`; all 12 fields now rendered; new i18n keys added to both locales.
- [x] [Review][Patch] `key={failure}` risks a React duplicate-key warning/misrender if the backend ever emits two identical failure strings [web/src/components/data-import/data-import-panel.tsx:153] — fixed: composite `key={`${index}-${failure}`}`.
- [x] [Review][Patch] No `AbortController` cancels the in-flight upload/confirm fetch on unmount, unlike the established precedent in `use-smart-plug-import-job.ts` [web/src/components/data-import/data-import-panel.tsx:70,109] — fixed: both `handleFileChosen` and `handleConfirm` now create and store an `AbortController`, aborted on unmount; `confirmHouseholdImport` gained an optional `signal` parameter to match `uploadHouseholdImportFile`'s existing one.
- [x] [Review][Patch] The success-screen "start over" button reused the file-picker trigger's exact label but did something different (`resetToIdle`, not opening the file chooser) [web/src/components/data-import/data-import-panel.tsx:168] — fixed: new `successAction` i18n key ("Import another file" / "Weitere Datei importieren").
- [x] [Review][Patch] `docs/data-import-restore.md`'s "Upload size limit" section was stale after Pass 1's own `Program.cs` fix (still described the old global Kestrel/FormOptions raise) [docs/data-import-restore.md:99] — fixed: rewritten to describe the route-scoped middleware + headroom margin actually shipped.
- [x] [Review][Defer] No overall poll timeout / indefinite polling on a repeated 404 in `useHouseholdImportJobPoll` [web/src/components/data-import/use-household-import-job.ts:22] — deferred, pre-existing: faithfully mirrors `use-smart-plug-import-job.ts`'s own established, unchanged-by-this-diff convention — not a regression introduced by this story.
- [x] [Review][Defer] No client-side file-size pre-check or size hint before a long upload that the server may reject at 250MB [web/src/components/data-import/data-import-panel.tsx:130] — deferred, minor UX nicety, not required by any AC.
- [x] [Review][Defer] No `aria-live` region on the error/validation-failure states [web/src/components/data-import/data-import-panel.tsx:142] — deferred, pre-existing systemic gap: `DataExportPanel`'s identical error-rendering pattern (`<p className="text-destructive text-sm">{error}</p>`) has the same gap, unrelated to this story.

**Dismissed as noise/false-positive/handled elsewhere (Pass 2, 6, not shown above):** Dialog Escape/overlay-click during `confirming` not fully guarded — verified false: Radix's `Dialog` `open` prop stays `true` (controlled component) throughout `confirming`, so it cannot actually close early regardless of the `onOpenChange` no-op. 413 not specially handled — resolved as a side effect of the double-read fix above (a bodyless 413 now correctly falls back to `ApiError(413, null)` via the single-read path, same as before but no longer masking a real message when one exists). `HouseholdImportValidationError` not setting `this.name` — consistent with this file's own `ApiError` convention, which also doesn't set it. Editorial ambiguity in the `data-export-format.md` tweak — read the sentence in context: coherent as written (documents/JSON-alone-is-sufficient promise deliberately preserved). `docs/data-import-restore.md`'s "What's out of scope" list allegedly omitting `SmartPlugImport` — verified false: the list at line 303 already reads "`HouseholdInvite`, `BackgroundJob`, `SmartPlugImport`, and `SmartPlugImportGap`".

**Dismissed as noise/handled elsewhere (4, not shown above):** self-lockout when imported members omit the current principal's identity (already explicitly disclosed and reasoned through in `docs/data-import-restore.md`'s "Session continuity across a restore"); cancellation-path temp-file cleanup asymmetry (verified deliberate — `BackgroundJobProcessor` treats `OperationCanceledException` as retryable, preserving the temp file for a resumed attempt); undiscovered-Restrict-FK audit request (independently re-verified via a full grep across every `*Configuration.cs` file — no other out-of-scope entity holds a Restrict FK to an in-scope entity); triple JSON-parsing efficiency (the job-queue re-parse is structurally required by AD-6; the only real duplication is a marginal, low-value micro-optimization within `Execute()`).

## Dev Notes

### Kickoff checks (mandatory — Epic 6 Retrospective Action Items #3 and #4)

The Epic 6 retrospective flagged two items explicitly assigned to story creation for Story 7.2, both resolved by design decisions above, restated here so they aren't lost:

- **Action Item #3** ("apply Stories 3.8–3.10's bulk-write/chunking/command-timeout lessons — a full-household restore is the same 'large bulk write' shape that caused 4 real production incidents"): addressed by Task 3/Task 4's async-job + chunked-transaction design — see "Bulk-write lessons from Stories 3.8–3.10" below.
- **Action Item #4** ("resolve the EF `DateTimeOffset` value-comparer gap ... before it becomes reachable via restore/migration writes"): resolved by design, not by code change — see "Why the DateTimeOffset value-comparer gap does not apply here" below. **Confirm this reasoning holds during implementation**; if the actual implementation ends up doing an in-place `UPDATE` of any existing tracked row's `DateTimeOffset` property to an instant-equal-but-different-offset value anywhere in this path, stop and re-read `spec-datetimeoffset-utc-normalization.md`'s deferred item before proceeding.

### Restore write-target design (critical, read first)

`ICurrentHouseholdAccessor` resolves the request's Household by looking up the authenticated principal's `(ExternalIssuer, ExternalSubjectId)` against the `HouseholdMembers` table (`CurrentHouseholdAccessor.cs`) — **not** by a stored session/cookie Household id. This has a direct consequence for restore:

- Restore must **always** write into the Household the current session already resolves to (`ICurrentHouseholdAccessor.HouseholdId`) — never create a new Household row, never reuse the import file's `household.id` as a real primary key. On "move to new hosting," the very first authenticated visitor already went through FR-26 and got a **freshly created** Household with a new `Id` before they ever reach this Settings screen; restore replaces that fresh Household's *contents*, it does not resurrect the old deployment's Household row under its old id.
- Because `ExportHouseholdData`/`docs/data-export-format.md` deliberately **excludes** `HouseholdId` from every child entity in the file (it's implicit — "the whole document is scoped to exactly one Household"), there is nothing to remap on the way in: every reconstructed entity just gets `HouseholdId = <current Household's id>` set directly, and every entity's *internal* cross-reference (`PowerPoint.RoomId`, `MeterReading.MainMeterId`, etc.) is reused byte-for-byte from the file, since those references were never Household-qualified in the first place and the file's internal graph is already self-consistent.
- `HouseholdMember` rows are deleted and reinserted like everything else. Because `CurrentHouseholdAccessor` resolves by issuer+subject (not by a stable row id), the person performing the restore keeps a working session across the operation **as long as their own `(ExternalIssuer, ExternalSubjectId)` is present among the imported members** — true by construction for the disaster-recovery/self-restore case (same person, same OIDC provider) and for the move-to-new-hosting case (same identity, new deployment). If it is ever *not* present (e.g. restoring someone else's export under a different identity), the current principal loses their Household on the next request and is routed back into FR-26's creation flow — a real, disclosed edge case, not solved by this story, but worth an explicit test (Task 7) proving the *expected* case works, and a code comment noting the edge case exists.
- The restore's DB work runs inside the async background job, which resolves `HouseholdId` from the job envelope (`JobHouseholdContext`), not from an HTTP principal — so the destructive write itself never depends on a live `HouseholdMember` row lookup mid-flight (see `CurrentHouseholdAccessor.Resolve()`'s job-processing branch). Continuity is only a concern for the *next* request after the job completes.

### Bulk-write lessons from Stories 3.8–3.10 (Retro Action Item #3)

`DELETE /api/smart-plug-import-jobs?deleteAll=true` caused **four real production incidents** in Epic 3 before landing on a stable shape — restore's delete-everything-then-insert-everything is structurally the same problem, just across all 12 entity categories instead of one, and adds an insert phase the cleanup story never had. Concrete, proven numbers/patterns to reuse rather than re-derive:

- `SmartPlugImportRepository.DeleteBatchSize = 200` — the one chunk-size value already proven safe against both SQL Server's ~2,100-parameter statement ceiling and per-command log-volume saturation on Azure SQL Basic. Use the same value (or a named constant with the same reasoning) for restore's chunked deletes and inserts, rather than guessing a new number.
- `CommandTimeout` is **per-command, not per-transaction** — `SmartPlugImportRepository` explicitly bumps it (`dbContext.Database.SetCommandTimeout(TimeSpan.FromSeconds(120))`, `180` for a heavier path) before a bulk operation. Chunking bounds each individual command; the explicit bump is still needed because the app-wide default is lower.
- **One outer transaction spans every chunk** (`BeginTransactionAsync`/commit once, loop chunks inside it) — never per-chunk transactions. This is what makes "never partially applied" true for the destructive phase, matching AC #2's spirit even though that AC is worded around the validation phase.
- **Wall-clock ceiling is separate from any single command's timeout.** Story 3.10's round-3 incident happened *after* every individual command was already bounded — total work still exceeded Container Apps' ~240s synchronous HTTP ingress ceiling. This is exactly why Task 1/4 mandate the async-job shape from the start, not as a later fix.
- **A large child-row cascade needs its own explicit chunked detach**, not reliance on an FK's cascade behavior as one unbounded operation (Story 3.10's 4th incident: a `SetNull` cascade alone blew `CommandTimeout` for a 122,158-row detach). Watch for the same shape here: deleting a household's `Room`/`PowerPoint` rows must not implicitly cascade-touch `SmartPlugReading` as one giant operation — `SmartPlugReading` is explicitly deleted as its own chunked step in this story's ordering, before `Room`/`PowerPoint`, for exactly this reason.
- **Explicitly accepted, not mitigated:** SQL Server lock-escalation risk under a large single transaction was raised during the 3.10 incident response and deliberately left unmitigated (`spec-3-10-cleanup-per-import-detach.md`: "a more thorough mitigation ... deserves its own dedicated investigation ... not a same-day addition"). The same acceptance applies here — do not attempt a `LOCK_ESCALATION` schema change as part of this story.

### Why the DateTimeOffset value-comparer gap does not apply here

`spec-datetimeoffset-utc-normalization.md`'s deferred item: EF Core's default `DateTimeOffset` value comparer compares only the UTC instant, so an **`UPDATE`** that reassigns an *already-tracked* row's `DateTimeOffset` property to an instant-equal-but-different-offset value can be silently skipped by change tracking. This restore design never does that:

- Every entity except `Household` is deleted, then **inserted fresh** — an `INSERT` is unaffected by the value comparer (the gap is specifically about `UPDATE`s being skipped as "no-op").
- `Household` **is** updated in place, but none of its updated fields (`Locale`, `Currency`, `YearlyBaselineKwh`, `TrendingThresholdKwh`, `LowConfidenceGapDays`, `TariffCheckCadenceMonths`, `AiPlausibilityEnabled`) is a `DateTimeOffset`. `Household.CreatedAtUtc` (the one `DateTimeOffset` field) is never written on restore.

If implementation ever deviates from this (e.g. an "upsert instead of delete+insert" optimization for performance), re-open this question before shipping it — that is precisely the shape the deferred item warns about.

### Format reuse — do not build a second schema

`docs/data-export-format.md` (Story 7.1) is the authoritative, already-shipped contract. Import's validation and deserialization target must be the **same DTO records** `ExportHouseholdData.cs` already defines (`HouseholdExportResult`, `HouseholdSettingsExportDto`, `HouseholdMemberExportDto`, `MainMeterExportDto`, `MeterReadingExportDto`, `MeterRegressionPromptExportDto`, `TariffExportDto`, `EventExportDto`, `RoomExportDto`, `PowerPointExportDto`, `DeviceExportDto`, `SmartPlugReadingExportDto`, `StatusSnapshotExportDto`, `AuditCorrectionExportDto`) — reusing them (rather than a parallel "import DTO" set) is what guarantees the documented format and the accepted format can never silently drift apart.

### Relevant architecture invariants (do not violate)

- **AD-2 (dual-provider):** plain portable EF Core LINQ only for the delete/insert work — no `EFCore.BulkExtensions`/AD-23 mechanism (see Task 3's explicit carve-out reasoning), no provider-specific SQL.
- **AD-3 (tenant isolation):** every inserted row's `HouseholdId` is set explicitly to the current session's Household id (the DbContext's global query filter governs reads; writes always set the FK explicitly, matching every existing write path in this codebase, e.g. `ProcessSmartPlugImport`).
- **AD-11 (audit correction):** this path never calls `IAuditCorrectionRecorder` — explicit carve-out already named in the AD's own text.
- **AD-10 (historical tag integrity):** restore's delete-then-insert of `Room`/`PowerPoint`/`Device` is a distinct, disclosed exception to FR-28's normal soft-delete-only UX rule — it's a wholesale administrative replace behind an explicit confirmation gate, not a user-facing "delete this Room" action, so AD-10's soft-delete convention doesn't apply to this specific mechanism the way it does to FR-28's own delete endpoint.
- **AD-19 (secrets):** never write `aiPlausibilityBackendConfigured`/`aiPlausibilityBackendLabel` from the import file back into any deployment-level config — those are read-only, informational fields in the export, sourced from this deployment's own `AiPlausibilityBackendOptions` at export time, not settings to restore.
- **AD-6 (async jobs):** payload is a plain JSON-serializable record carrying only a temp-file path, never file bytes (Azure Storage Queue's 64 KB message cap) — mirrors `ProcessSmartPlugImportPayload` exactly.

### Testing standards summary

.NET: `{SubjectClass}Tests` per class, `Snake_case_with_underscores` method names, Shouldly assertions, NSubstitute mocks against ports, `TestContext.Current.CancellationToken` (xUnit v3 MTP). A Testcontainers-backed Infrastructure test is required for the delete/insert-ordering and AD-3-write-target claims — an NSubstitute-mocked Application-layer test cannot verify real FK ordering or that the global query filter still reads consistently afterward. Frontend: Vitest + Testing Library, colocated `*.test.tsx`.

### Project Structure Notes

- Backend: new use case `EnergyTracker.Application/RestoreHouseholdData.cs` and validation logic (Task 2), new port(s)/adapter(s) as needed in `Application/Ports`/`Infrastructure/Adapters` (mirror `IHouseholdExportReader`/`HouseholdExportReader`'s placement); new endpoint file `EnergyTracker.Api/Endpoints/HouseholdImportEndpoints.cs`, registered alongside the other `Map*Endpoints` calls in `Program.cs`; new `JobTypes.RestoreHouseholdData` constant and `BackgroundJobProcessor.cs` switch case.
- New doc: `docs/data-import-restore.md` (flat, same folder as `data-export-format.md`).
- Frontend: new `web/src/components/data-import/` folder, wired into `web/src/components/settings/settings-page.tsx` next to `DataExportPanel`.
- No conflicts detected with existing structure/conventions beyond what's called out above (verify the FK-ordering assumption against real `*Configuration.cs` files per Task 3).

### References

- [Source: _bmad-artifacts/planning/epics/epic-7-data-export-import-disaster-recovery.md#Story 7.2]
- [Source: _bmad-artifacts/planning/prds/prd-energy-tracker-2026-08-08/prd/4-features.md#FR-23]
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-2, AD-3, AD-6, AD-10, AD-11, AD-19]
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/capability-architecture-map.md — "Data Export/Import (FR-22–FR-23) | Application use case over all repositories | AD-2, AD-3"]
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/EXPERIENCE.md — Settings row (data export/import, FR-22/23), State Patterns table row "Import data fails validation" (UX-DR14)]
- [Source: _bmad-artifacts/planning/epics/requirements-inventory.md#UX-DR12, UX-DR14]
- [Source: _bmad-artifacts/implementation/7-1-full-data-export.md — export format/entity-scope decisions this story must stay consistent with]
- [Source: docs/data-export-format.md — the authoritative v2 format this story validates against and reuses DTOs from]
- [Source: _bmad-artifacts/implementation/epic-6-retro-2026-09-21.md — Action Items #3 (bulk-write lessons) and #4 (DateTimeOffset value-comparer gap), both explicitly assigned to this story's creation]
- [Source: _bmad-artifacts/implementation/spec-datetimeoffset-utc-normalization.md — the deferred value-comparer gap resolved by design in this story's Dev Notes]
- [Source: _bmad-artifacts/implementation/spec-3-10-cleanup-batch-delete-fix.md, spec-3-10-cleanup-per-import-detach.md, spec-3-10-cleanup-async-job.md — the 4 real production incidents and their concrete fixes (DeleteBatchSize=200, CommandTimeout bumps, async-job move, chunked detach-before-delete)]
- [Source: src/EnergyTracker.Application/ExportHouseholdData.cs — the DTO family this story's validation/import must reuse]
- [Source: src/EnergyTracker.Infrastructure/Adapters/HouseholdExportReader.cs — read-path precedent for entity scope/AD-3 handling]
- [Source: src/EnergyTracker.Api/Endpoints/HouseholdExportEndpoints.cs, SmartPlugImportEndpoints.cs — endpoint/TryGetHouseholdId/temp-file-upload patterns to mirror]
- [Source: src/EnergyTracker.Infrastructure/Adapters/BackgroundJobProcessor.cs, src/EnergyTracker.Application/JobTypes.cs, src/EnergyTracker.Application/Ports/IBackgroundJobQueue.cs — AD-6 async job wiring to extend]
- [Source: src/EnergyTracker.Infrastructure/Adapters/CurrentHouseholdAccessor.cs — the issuer/subject lookup behind the session-continuity design decision]
- [Source: src/EnergyTracker.Infrastructure/Adapters/SmartPlugImportRepository.cs — DeleteBatchSize/DeleteReadingVolumeThreshold/CommandTimeout/transaction precedents]
- [Source: web/src/components/data-export/data-export-panel.tsx, web/src/components/smart-plug-import/use-smart-plug-import-job.ts — frontend fetch/ApiError and job-polling patterns to mirror]
- [Source: web/src/components/settings/settings-page.tsx — Settings composition pattern, existing Dialog-based confirmation flow (Logoff) to mirror for the "replace all data" confirmation]
- [Source: _bmad-artifacts/project-context.md]

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

None captured to a separate file — the one real defect found (BackgroundJob FK conflict, below) surfaced directly in `dotnet test` output and was root-caused/fixed in the same session.

### Completion Notes List

- Tasks 1–8 complete: design decisions documented (`docs/data-import-restore.md`), validation (`ValidateHouseholdImport`), restore use case + port/adapter (`RestoreHouseholdData`/`IHouseholdRestoreWriter`/`HouseholdRestoreWriter`), async job wiring (`JobTypes.RestoreHouseholdData`, `BackgroundJobProcessor` case, `HouseholdImportValidationException`), two API endpoints (`HouseholdImportEndpoints`), frontend Settings panel (`DataImportPanel`), tests across all four layers (Application/Infrastructure/Api/frontend, all green), and live Auth0/Chrome verification against a real household.
- **Design decision not spelled out in the story, made during implementation**: the token→temp-file mapping (Task 5) uses a plain in-memory `ConcurrentDictionary`-backed singleton (`MemoryHouseholdImportUploadRegistry`), not `IMemoryCache` (avoids a new NuGet package + `Directory.Packages.props` entry for a narrow, already-simple need) and not a DB table (avoids new-entity/migration ceremony for transient state with a 30-minute TTL). Trade-off disclosed in `docs/data-import-restore.md`: a container restart between validate and confirm loses the token, requiring re-upload — judged acceptable given this app's one-process/one-container deployment model (AD-6) and the short real-world window between the two requests.
- **Real defect #1, found and fixed during Task 7 (before live verification)**: `BackgroundJob.QueuedByHouseholdMemberId`'s FK to `HouseholdMember` was `DeleteBehavior.Restrict`. Every restore's own `HouseholdMember` wholesale-delete step therefore violated that FK, because the confirm endpoint had already inserted a `BackgroundJob` row (for the restore job itself, via the existing `IBackgroundJobQueue.EnqueueAsync`/`BackgroundJobEnqueueRecorder` path) referencing the current member before the job's delete phase ever ran — this is not a rare edge case, it is the normal path for every restore. Caught by `HouseholdImportEndpointsTests`' real-Postgres end-to-end round-trip test (an NSubstitute-mocked test could not have caught this — another confirmation of project-context.md's Story 5.1 lesson on needing a real DbContext for this class of bug). Fixed by changing that one FK to `DeleteBehavior.SetNull` via migration `BackgroundJobQueuedByMemberSetNull`.
- **Real defect #2, found live during Task 8 itself, on the first live restore attempt**: `SmartPlugImportGap.PowerPointId`'s FK to `PowerPoint` was also `DeleteBehavior.Restrict`. The specific household used for live verification had real historical Smart Plug import gap rows (from an earlier story's own live-verification import) referencing a `PowerPoint` that restore's delete phase tries to remove — no automated test fixture in this codebase had ever created a real `SmartPlugImportGap` row via an actual Smart Plug import, so this was invisible to every unit/Testcontainers/Api test. Fixed the same way, via `DeleteBehavior.SetNull` + migration `SmartPlugImportGapPowerPointSetNull`. After finding this second instance, audited every remaining `*Configuration.cs` file for the same shape (an out-of-scope table — `HouseholdInvite`/`BackgroundJob`/`SmartPlugImport`/`SmartPlugImportGap` — holding a `Restrict` FK into an in-scope entity) and confirmed these two were the only ones. **Operational note**: both migrations must be applied (`scripts/migrate.sh`, or the equivalent for a deployed environment) before this story's restore feature is exercised against any database that has real Smart Plug import history — this was only caught because the live-verification household had exactly that history; a fresh/empty database needs no separate action since new migrations apply to it automatically.
- Confirmed during implementation (Task 3's own flagged open question): `Event.TaggedEntityId` and `AuditCorrection.EntityId` are plain polymorphic `Guid` columns with no DB-enforced FK — both entities' `*Configuration.cs` files say so explicitly in their own comments — so the delete/insert ordering the story proposed needed no adjustment for them.
- Confirmed during implementation (Task 1's own flagged open question): a per-request `IHttpMaxRequestBodySizeFeature` override inside the `POST /api/household-import` handler would be ineffective, because Minimal API's `IFormFile` model binding reads the entire multipart body (and would already have rejected it under the old, lower ceiling) before the endpoint's own delegate runs. Kestrel's `MaxRequestBodySize` and `FormOptions.MultipartBodyLengthLimit` are instead raised once, globally, at the composition root.
- **Live verification (Task 8) deviated from the story's literal file in one disclosed way**: the real household's own export (32 MB, 117,770 real Smart Plug readings) exceeded the browser-automation upload tool's 10 MB per-call cap, so the exact file could not be fed back through the UI. With Ralf's explicit go-ahead (asked mid-session via AskUserQuestion), restored a copy of that same real export with `smartPlugReadings` trimmed to 200 rows and every other category kept in full — a real, permanent reduction of that one household's Smart Plug history, not a synthetic fixture. Full detail in Task 8's own checklist above. All other verification steps (validation-failure UI, confirm dialog, poll-to-completion, session continuity, actual before/after data comparison) used the real running app with no shortcuts.
- **Code review (Pass 1/backend), 2026-09-22**: 10 patch findings applied — see Review Findings above for the full list. Supersedes the Task 1 note above about the body-size raise: it's no longer global, now scoped to `/api/household-import` via route-conditional middleware in `Program.cs`, closing the DoS-exposure widening the review found on `SmartPlugImportEndpoints`. Full test suite reconfirmed green after all patches: Application.Tests 383/383, Api.Tests 218/218, Infrastructure.Tests 232/232 (dual-provider, including a new forced-rollback test).
- **Code review (Pass 2/frontend+docs), 2026-09-22**: 6 patch findings applied (double-read response-body bug found independently by all three review layers, confirm-dialog summary completeness, duplicate React key, missing unmount-abort, misleading button-label reuse, stale docs section left behind by Pass 1's own `Program.cs` fix), 3 deferred, 6 dismissed as noise/false-positive/handled-elsewhere (two of which — the Dialog-dismissal claim and the docs "out of scope" list claim — were verified false by directly reading the code/doc, not just asserted). Also resolved Pass 1's own deferred `OriginalFileName`/XSS question: confirmed safe, rendered as plain JSX text, never `dangerouslySetInnerHTML`. Full frontend suite green after patches: `tsc -b` clean, `oxlint` clean (no new warnings), Vitest 385/385, `vite build` clean.
- **Live Auth0/Chrome re-verification, 2026-09-22 (post-review, with Ralf's explicit go-ahead)**: both review passes' code changes — including the range/enum/reference/duplicate-id validation, the confirm-dialog full summary, and the corrected success-screen button label — driven live against the same real household/authenticated session Task 8 used. Local stack restarted (Postgres already running; `scripts/run-api.sh`; `npm --prefix web run dev`), Claude-in-Chrome against `https://localhost:5173`. Verified live, with two self-consistent synthetic v2 files built for this session (not the real household's own data): (1) a deliberately-broken file simultaneously triggering all three new Pass-1 validation rules — duplicate room id, undefined-numeric-string `StatusSnapshot.status` ("42"), negative `MeterReading.kwhValue` — all three correctly rejected and reported together, none silently accepted; (2) a fully-populated valid file (all 12 entity categories non-zero) restored successfully end-to-end, with the confirm dialog correctly showing all 12 categories (Pass 2's own fix) and the success screen correctly reading "Import another file" (Pass 2's own fix); session continuity held (Dashboard/Settings loaded normally post-restore, no forced re-login) and the restored `MeterRegressionPrompt` rendered as a real, interactive, AD-12-compliant dialog; a fresh export afterward confirmed the restored data matched the imported file exactly, entity-for-entity. **One real regression caught by this live pass, not by either automated review**: `ValidateMeterReading`'s `kwhValue` had been missed by Pass 1's own range-validation patch — still plain `RequireDecimal` (allowing negative values) despite `SmartPlugReading`'s identical field having been correctly fixed to `RequireNonNegativeDecimal`. Fixed on the spot, plus 4 new regression tests added covering all four Pass-1 validation features that had shipped with zero test coverage (undefined-numeric-enum, duplicate-id, dangling-reference, and this negative-kwhValue case) — Application.Tests now 387/387. API/Vite processes stopped and the household restored back to its pre-verification snapshot (the same export Task 8 last left it at) after verification; the two `Export data` downloads this session produced remain in `~/Downloads` for Ralf to keep or delete, same convention as Task 8.

### File List

**Backend — new:**
- `src/EnergyTracker.Application/Ports/IHouseholdRestoreWriter.cs`
- `src/EnergyTracker.Application/Ports/IHouseholdImportUploadRegistry.cs`
- `src/EnergyTracker.Application/ValidateHouseholdImport.cs`
- `src/EnergyTracker.Application/RestoreHouseholdData.cs`
- `src/EnergyTracker.Infrastructure/Adapters/HouseholdRestoreWriter.cs`
- `src/EnergyTracker.Infrastructure/Adapters/MemoryHouseholdImportUploadRegistry.cs`
- `src/EnergyTracker.Api/Endpoints/HouseholdImportEndpoints.cs`
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/20260922102215_BackgroundJobQueuedByMemberSetNull.cs` (+ `.Designer.cs`)
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/20260922102218_BackgroundJobQueuedByMemberSetNull.cs` (+ `.Designer.cs`)
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/20260922110237_SmartPlugImportGapPowerPointSetNull.cs` (+ `.Designer.cs`)
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/20260922110240_SmartPlugImportGapPowerPointSetNull.cs` (+ `.Designer.cs`)

**Backend — modified:**
- `src/EnergyTracker.Application/JobTypes.cs`
- `src/EnergyTracker.Infrastructure/Adapters/BackgroundJobProcessor.cs`
- `src/EnergyTracker.Infrastructure/Configurations/BackgroundJobConfiguration.cs`
- `src/EnergyTracker.Infrastructure/Configurations/SmartPlugImportGapConfiguration.cs`
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/EnergyTrackerDbContextModelSnapshot.cs`
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/EnergyTrackerDbContextModelSnapshot.cs`
- `src/EnergyTracker.Api/Program.cs`

**Backend — modified (code review, Pass 1, 2026-09-22; `ValidateHouseholdImport.cs` further fixed during live re-verification, same date — see Completion Notes):**
- `src/EnergyTracker.Application/ValidateHouseholdImport.cs` (exportedAtUtc validation, strict ISO-8601/decimal-range checks, undefined-enum rejection, cross-entity reference + duplicate-id checks, guarded final Deserialize)
- `src/EnergyTracker.Infrastructure/Adapters/MemoryHouseholdImportUploadRegistry.cs` (sweep expired/abandoned upload entries + their temp files)
- `src/EnergyTracker.Api/Endpoints/HouseholdImportEndpoints.cs` (per-household in-flight-restore guard, MeterRegressionPrompts in summary, body-size headroom constant)
- `src/EnergyTracker.Api/Program.cs` (body-size raise scoped to `/api/household-import` via route-conditional middleware instead of process-wide)
- `tests/EnergyTracker.Application.Tests/ValidateHouseholdImportTests.cs` (fixture fix for the new cross-reference checks)
- `tests/EnergyTracker.Infrastructure.Tests/HouseholdRestoreWriterTests.cs` (new forced-rollback test)

**Docs — new/modified:**
- `docs/data-import-restore.md` (new)
- `docs/data-export-format.md` (modified — intro sentence)

**Frontend — new:**
- `web/src/lib/household-import-api.ts`
- `web/src/components/data-import/data-import-panel.tsx`
- `web/src/components/data-import/use-household-import-job.ts`
- `web/src/components/data-import/data-import-panel.test.tsx`

**Frontend/docs — modified (code review, Pass 2, 2026-09-22):**
- `web/src/lib/household-import-api.ts` (single-read 400 handling, `meterRegressionPrompts` field, `confirmHouseholdImport` gained a `signal` param)
- `web/src/components/data-import/data-import-panel.tsx` (full 12-field summary, duplicate-key fix, unmount-abort, success-button label)
- `web/src/components/data-import/data-import-panel.test.tsx` (new 400-without-failures test, extended summary assertions)
- `web/src/locales/en-US/translation.json`, `web/src/locales/de-DE/translation.json` (new summary/success-action keys)
- `docs/data-import-restore.md` (Upload size limit section rewritten to match Pass 1's actual `Program.cs` fix)

**Frontend — modified:**
- `web/src/components/settings/settings-page.tsx`
- `web/src/locales/en-US/translation.json`
- `web/src/locales/de-DE/translation.json`

**Tests — new:**
- `tests/EnergyTracker.Application.Tests/ValidateHouseholdImportTests.cs`
- `tests/EnergyTracker.Application.Tests/RestoreHouseholdDataTests.cs`
- `tests/EnergyTracker.Infrastructure.Tests/HouseholdRestoreWriterTests.cs`
- `tests/EnergyTracker.Api.Tests/HouseholdImportEndpointsTests.cs`
