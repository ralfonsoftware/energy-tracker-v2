---
baseline_commit: 52d9e405fc82a1aec717439db994248fb056a87d
---

# Story 2.7: Status Calculation Detail

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Household member,
I want to open a details view from the dashboard Status card,
so that I understand how the "X kWh over/under expected" figure was actually calculated, not just told the number.

## Acceptance Criteria

1. **Given** the dashboard Status card, **when** I open its details view, **then** it shows pace-to-date, baseline-to-date, the elapsed period baseline-to-date covers, the difference between them (the figure already shown on the card), and the household's configured trending threshold (FR-30).
2. **Given** the details view, **when** rendered, **then** it shows only the aggregate figures already computed for Status — never a list of individual contributing Meter Readings (FR-30, confirmed with Ralf: a full reading list would be long with little added value).
3. **Given** the Status card's low-confidence flag (Story 2.4) is active, **when** I open the details view, **then** it explains why — stale last reading, not corroborated by Smart Plug coverage — rather than surfacing the flag with no explanation (FR-30, FR-6).
4. **Given** the details view, **when** rendered, **then** no chart is required — it's legible as labeled figures, same as the Status card itself (FR-30, FR-7).
5. **Given** Status is undefined (fewer than two Readings, or no Yearly Baseline set — Story 2.4/2.5's onboarding empty state), **when** the dashboard is in that state, **then** no details view is offered — there is no calculation yet to explain (FR-30, FR-6).
6. **Given** the existing `/api/status` endpoint, **when** the details data is served, **then** it's exposed via a separate endpoint, never merged into the Status response — consistent with the codebase's existing drill-down convention (e.g. Trend History, FR-8).

## Tasks / Subtasks

- [x] **Task 1 — Extend `GetCurrentStatus`'s result with detail-only figures (AC #1, #3)**
  - [x] `src/EnergyTracker.Application/GetCurrentStatus.cs`: extend the `CurrentStatusResult` record with 4 new fields the computation already derives internally but doesn't currently return: `double ElapsedDays` (from `paceResult.Value.Elapsed.TotalDays`), `decimal TrendingThresholdKwh` (from `household.TrendingThresholdKwh`), `double DaysSinceLastReading` (the existing `(DateTimeOffset.UtcNow - lastReading.ReadingTimestamp).TotalDays` expression — extract it into a named local so it can be both compared against `household.LowConfidenceGapDays` and returned), `int LowConfidenceGapDaysThreshold` (from `household.LowConfidenceGapDays`).
  - [x] Update the single `return new CurrentStatusResult(...)` at the end of `ExecuteAsync` to pass the 4 new values. **No other logic in this method changes** — this is a pure "also return what's already computed" extension, not a new calculation path. AD-7's "one and only place the live computation runs" is preserved exactly: both `/api/status` and the new `/api/status/detail` route (Task 2) call this same method.
  - [x] `CurrentStatusResult` is only constructed in this one file and only consumed via named-property access in `StatusEndpoints.cs` and the test suite (confirmed: `grep -rn "CurrentStatusResult" src/ tests/` returns exactly those 2 call sites plus the test files' property-access assertions) — adding fields is compile-safe, no other file needs touching for this task.

- [x] **Task 2 — `GET /api/status/detail` endpoint (AC #1, #2, #4, #5, #6)**
  - [x] `src/EnergyTracker.Api/Endpoints/StatusEndpoints.cs`: add a second route inside the existing `MapStatusEndpoints` method (not a new file — mirrors Story 2.6's precedent of adding closely-related routes to an existing endpoints file rather than fragmenting one concern across files). Route: `api.MapGet("/status/detail", ...)`, same `TryGetHouseholdId` guard, same `GetCurrentStatus getCurrentStatus` DI parameter (already registered in `Program.cs:304` — **no new DI registration needed**, this route reuses the exact same use-case instance/logic as `/api/status`).
  - [x] `Results.Ok(result is null ? null : ToDetailResponse(result))` — identical 200-with-empty-body-when-undefined shape as `/api/status` (AC #5: Story 2.4/2.5's undefined-Status semantics are the single source of truth for "is there anything to show," reused as-is, not re-implemented).
  - [x] Add `StatusDetailResponse` record next to the existing `StatusResponse`: `(string Status, decimal PaceToDateKwh, decimal BaselineToDateKwh, double ElapsedDays, decimal TrendingThresholdKwh, bool IsLowConfidence, double DaysSinceLastReading, int LowConfidenceGapDaysThreshold)`. Add a `ToDetailResponse(CurrentStatusResult result)` mapping method alongside the existing `ToResponse`, reusing the existing private `ToStatusString` helper for the `Status` field.
  - [x] **Do not touch the existing `ToResponse`/`StatusResponse`/`/status` route at all** (AC #6, Consistency Conventions: "the Dashboard Status endpoint returns only the current Status value and its one headline/supporting sentence — drill-down data is always a separate endpoint, never merged into the Status response"). This task is purely additive.
  - [x] Route naming: `/status/detail` (a sub-resource GET on the existing singleton `/api/status`, not a new top-level plural route) — matches the sub-resource-PUT precedent Story 2.6 established (`/power-points/{id}/room`) over inventing a differently-shaped convention; Trend History/per-plug view (the other "drill-down" examples cited in Consistency Conventions) don't exist yet to compare against directly.

- [x] **Task 3 — Frontend API client: `fetchStatusDetail` (AC #1)**
  - [x] `web/src/lib/status-api.ts`: add `export interface StatusDetailDto { status: StatusValue; paceToDateKwh: number; baselineToDateKwh: number; elapsedDays: number; trendingThresholdKwh: number; isLowConfidence: boolean; daysSinceLastReading: number; lowConfidenceGapDaysThreshold: number }` and `export async function fetchStatusDetail(): Promise<StatusDetailDto | null>` calling `GET /api/status/detail`. **Replicate `fetchCurrentStatus`'s exact empty-body-as-null / `ApiError` pattern verbatim** (read as text first, `JSON.parse` only if non-empty) — same file, same established precedent, do not diverge.

- [x] **Task 4 — Extract shared pace/baseline difference logic (AC #1)**
  - [x] Create `web/src/lib/status-difference.ts` exporting `computeStatusDifference(paceToDateKwh: number, baselineToDateKwh: number): { sign: 'under' | 'over' | 'on'; roundedMagnitude: number; rawDifference: number }`. Extract this **verbatim** from `status-card.tsx`'s existing inline logic (lines ~96-106: `rawDifference`, `roundedMagnitude`, and the `rawDifference === 0 / < 0 / else` branching) — this is the exact calculation a Story 2.5 code-review finding already fixed once (the `(-0.5, 0)` kWh boundary contradiction between the rounded copy and the unrounded status badge). The details view (Task 5) needs to render the identical "X kWh over/under pace" figure the card already shows (AC #1) — re-deriving it independently in a second file would risk silently reintroducing that exact bug class if the two copies ever drift.
  - [x] Update `status-card.tsx` to import and call `computeStatusDifference` instead of its inline calculation, keeping its own `t('dashboard.status.body.onPace'|'underPace'|'overPace', ...)` copy composition unchanged (only the number-crunching moves, not the JSX/copy). Existing `status-card.test.tsx` cases (tie-at-zero, under/over-pace) must still pass unmodified — this is a pure refactor, not a behavior change.
  - [x] Add `web/src/lib/status-difference.test.ts` with the 3 branch cases plus the near-zero-boundary regression case (a raw difference like `-0.3` must report `sign: 'under'`, not `'on'`, with `roundedMagnitude: 0` — matching the exact fixed behavior from Story 2.5's review).

- [x] **Task 5 — `StatusDetailDialog` component (AC #1, #2, #3, #4)**
  - [x] Create `web/src/components/dashboard/status-detail-dialog.tsx`. Props: `{ trigger: ReactNode; open: boolean; onOpenChange: (open: boolean) => void; locale: string }` — controlled from `DashboardPage` (Task 6), matching `LogReadingSheet`'s existing controlled-open-state shape, not an internally-managed `open`.
  - [x] Reuse the shadcn `Dialog`/`DialogTrigger`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter` primitives with `className={GLASS_MODAL_CLASSNAME}` on `DialogContent` (`@/lib/glass-classnames`) — the exact same modal shell `tagging-scaffold-manager.tsx`'s dialogs and the Meter Regression prompt already use. **Do not build a bespoke sheet/overlay** — no mockup exists for this story (added post-UX-freeze, 2026-08-23), so the established `Dialog` + `GLASS_MODAL_CLASSNAME` combination is the binding precedent, not a new visual language.
  - [x] Fetch-on-open: a `useEffect` keyed on `open` calls `fetchStatusDetail()` when `open` transitions to `true`, storing `detail: StatusDetailDto | null`, `loading: boolean`, `error: boolean` in local state; resets all three back to their initial values when `open` transitions to `false` (so a re-open always shows a fresh fetch, never stale data from a previous open). On fetch failure (thrown `ApiError` or network error), set `error: true` — render a plain-text error line, no retry-loop machinery (this is a read-only, re-openable drill-down; the user can simply re-open it).
  - [x] Body content (AC #1, #4 — no chart, labeled figures only, mirrors the Tariff Radar card's "each row a label/value pair with `{typography.status-figure}` (tabular-nums) on the value" convention from `DESIGN/components.md`, the closest existing precedent for a labeled-figures drill-down since no Status-Detail-specific design token exists):
    - Pace to date: `Intl.NumberFormat(locale, { maximumFractionDigits: 0 }).format(detail.paceToDateKwh)` kWh, `tabular-nums`.
    - Baseline to date: same formatting for `detail.baselineToDateKwh` kWh, with the elapsed period alongside (`Math.round(detail.elapsedDays)` days) — this single row satisfies AC #1's "baseline-to-date... the elapsed period baseline-to-date covers" (they're presented together, not as two disconnected figures, since the elapsed period is what the baseline-to-date figure was prorated over).
    - Difference: reuse `computeStatusDifference` (Task 4) on `detail.paceToDateKwh`/`detail.baselineToDateKwh`, then render via the **existing** `t('dashboard.status.body.onPace'|'underPace'|'overPace', { kwh })` keys — the same copy the Status card itself already shows (AC #1: "the difference between them (the figure already shown on the card)"). Do not invent new copy keys for this row.
    - Trending threshold: `Intl.NumberFormat(locale, { maximumFractionDigits: 0 }).format(detail.trendingThresholdKwh)` kWh.
  - [x] Low-confidence explanation (AC #3): rendered only when `detail.isLowConfidence` is true, below the figure rows. One sentence composed via i18next interpolation from `detail.daysSinceLastReading` (rounded) and `detail.lowConfidenceGapDaysThreshold` — concrete named numbers, not vague copy, matching the voice/tone discipline (UX-DR17) already established for the Status card's own headline/supporting-sentence copy (Story 2.5 Task 4). Since `IsLowConfidence` can only be `true` when both the gap-exceeded-threshold check *and* the no-corroborating-Smart-Plug-coverage check hold (see `GetCurrentStatus.cs`'s `if (isLowConfidence) { ...hasCorroboratingCoverage... }` branch — it only ever flips `true`→`false`, never the reverse), a single sentence naming both conditions is always accurate, never a guess.
  - [x] Cold-load state while `loading`: reuse the shadcn `Skeleton` primitive (`@/components/ui/skeleton`, already scaffolded by Story 2.5) for 4 placeholder bars matching the row count — same "no chart, no reflow surprise" discipline the Status card's own skeleton follows, not a bespoke spinner.
  - [x] `DialogFooter` with a single `Button variant="outline"` closing the dialog (`onOpenChange(false)`) — matches every existing `Dialog` in this codebase's Cancel/Close footer shape.
  - [x] **Do not render a list of individual Meter Readings anywhere in this dialog** (AC #2) — the backend response (`StatusDetailDto`) structurally cannot supply one (it's 8 scalar aggregate fields, not a collection), but this is also a deliberate, explicit product decision ("confirmed with Ralf: a full reading list would be long with little added value," epic file verbatim) — do not add one as a perceived enhancement. That surface is Story 2.8 (Meter Reading History View)'s job, not this one's.

- [x] **Task 6 — Wire the trigger into `StatusCard`/`DashboardPage` (AC #1, #5)**
  - [x] `web/src/components/dashboard/status-card.tsx`: add a `detailTrigger?: ReactNode` prop to `StatusCardProps`, rendered **only** in the populated branch (after the existing `aria-live` block, inside the `GlassCard`) — never in the `loading` or `!status` (empty) branches. This mirrors the existing `emptyStateAction` slot pattern exactly (a compositional slot, not new dialog-awareness inside `StatusCard` itself — `StatusCard` stays a pure function of its props, consistent with its existing design). AC #5 falls out for free: the empty/loading branches simply never receive or render this prop.
  - [x] `web/src/components/dashboard/dashboard-page.tsx`: add local state `const [detailDialogOpen, setDetailDialogOpen] = useState(false)`. Render `<StatusDetailDialog open={detailDialogOpen} onOpenChange={setDetailDialogOpen} locale={household.locale} trigger={<button type="button" className="mt-3 text-xs font-medium text-muted-foreground underline underline-offset-4 hover:text-foreground">{t('dashboard.statusDetail.trigger')}</button>} />`, and pass it as `<StatusCard ... detailTrigger={showPopulated ? <StatusDetailDialog .../> : undefined} />` (only construct/pass it when `showPopulated` — same guard `dashboard-page.tsx` already uses for the primary Log Reading button placement). **The trigger is real, visible text — not an icon-only control** (per `review-accessibility-2026-08-16.md`'s explicit finding against icon-only controls relying only on `title`; this story's trigger has its own visible label, sidestepping that class of issue entirely rather than needing a supplementary `aria-label`).
  - [x] One-level-deep modal stacking (UX-DR13, cited at Epic 2's header level, not restated in this story's own AC text since this story predates/postdates the UX-DR citation discipline the earlier Story 2.1-2.6 ACs used — see Dev Notes): add a `useEffect` in `dashboard-page.tsx` that calls `setDetailDialogOpen(false)` whenever `openRegressionPrompt` transitions from `null` to non-`null` — the same "a higher-priority modal supersedes rather than stacks" rule Story 2.3 already applies to the Log Reading sheet, applied here to this new read-only drill-down dialog for consistency.

- [x] **Task 7 — i18n copy (AD-18)**
  - [x] Add a `statusDetail` block to both `web/src/locales/en-US/translation.json` and `de-DE/translation.json`'s `dashboard` namespace (mirrored keys, parity discipline unchanged): `trigger` (e.g. en: `"How was this calculated?"`, de: `"Wie wurde das berechnet?"`), `title` (e.g. en: `"Status calculation"`, de: `"Statusberechnung"`), `paceLabel` (e.g. en: `"Pace to date"`, de: `"Tempo bis heute"`), `baselineLabel` (e.g. en: `"Baseline to date"`, de: `"Basiswert bis heute"`), `elapsedDays` (e.g. en: `"over {{days}} days"`, de: `"über {{days}} Tage"`), `differenceLabel` (e.g. en: `"Difference"`, de: `"Differenz"`), `thresholdLabel` (e.g. en: `"Trending threshold"`, de: `"Schwellenwert für Anstieg"`), `lowConfidenceExplanation` (e.g. en: `"It's been {{days}} days since your last reading — more than the household's {{threshold}}-day threshold, and no Smart Plug data covers the gap."`, de: analogous translation), `loadError` (e.g. en: `"Couldn't load the calculation detail — try again."`, de: analogous), `close` (reuse an existing generic close/cancel key if one already exists in this namespace before adding a new one — check `taggingScaffold.cancel`-style precedent first). **Reuse the existing `dashboard.status.body.onPace`/`underPace`/`overPace` keys for the difference row** (Task 5) — do not duplicate them under `statusDetail`.

- [x] **Task 8 — Tests**
  - [x] `GetCurrentStatusTests.cs`: extend the existing `WithinRange`/low-confidence test cases (don't need new methods) with assertions on the 4 new `CurrentStatusResult` fields — `ElapsedDays`, `TrendingThresholdKwh`, `DaysSinceLastReading`, `LowConfidenceGapDaysThreshold` — following the same `Snake_case_with_underscores`/Shouldly pattern already in this file.
  - [x] `StatusEndpointsTests.cs`: add `GET_status_detail_...` cases mirroring the existing `GET_status_...` cases 1:1 — null body when undefined (no baseline; fewer than two readings), a populated response asserting `ElapsedDays`/`TrendingThresholdKwh`/`DaysSinceLastReading`/`LowConfidenceGapDaysThreshold` alongside the already-covered `Status`/`PaceToDateKwh`/`BaselineToDateKwh`, cross-Household isolation (reuse `A_households_Status_is_never_affected_by_another_households_readings_or_baseline`'s fixture shape), and the `403` no-Household case. Add `StatusDetailResponse` to this test file's imports (it's a public record in `StatusEndpoints.cs`, same as `StatusResponse` today).
  - [x] `status-api.test.ts`: add `fetchStatusDetail` cases mirroring the existing `fetchCurrentStatus` cases (null-on-empty-body, parsed DTO on 2xx-with-body, `ApiError` on non-2xx).
  - [x] `status-difference.test.ts` (new, Task 4): 4 cases — on-pace tie, under-pace, over-pace, and the near-zero-boundary regression case.
  - [x] `status-card.test.tsx`: extend with a case asserting `detailTrigger` renders only when populated (absent during `loading` and the empty state) — mirrors the existing `emptyStateAction`-slot test precedent. Existing under/over/on-pace cases continue to pass unmodified post-refactor (Task 4).
  - [x] `status-detail-dialog.test.tsx` (new): opening the trigger fetches and renders the 4 figure rows with correct locale-aware formatting; loading skeleton shown before resolution; low-confidence explanation present/absent per `isLowConfidence`; fetch failure renders the error line; re-opening after a close re-fetches (no stale data carried over); a `500`/thrown `ApiError` is handled without an unhandled rejection.
  - [x] `dashboard-page.test.tsx`: extend with a case asserting the detail dialog closes when `openRegressionPrompt` transitions from `null` to non-`null` while it's open (UX-DR13 supersession, Task 6).

- [x] **Task 9 — Documentation**
  - [x] No `docs/*.md` changes expected — no new operator-facing configuration, migration, or adapter/env var (same conclusion as Story 2.6's Task 7 and Story 2.5's equivalent check). Confirm once implementation is done; update only if something unexpected surfaces.

### Review Findings

_Adversarial code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor), 2026-08-23. All 3 layers completed. Diff artifact initially omitted the 4 new untracked files (`status-detail-dialog.tsx`/test, `status-difference.ts`/test) — regenerated with `git add -N` before triage; all findings below reflect the complete diff._

- [x] [Review][Patch] Low-confidence explanation can read self-contradictory at the rounding boundary [web/src/components/dashboard/status-detail-dialog.tsx:132-135] — `isLowConfidence` is decided on the raw `daysSinceLastReading > threshold` comparison, but the dialog displays `Math.round(daysSinceLastReading)`; a raw value like 45.4 (threshold 45) renders "It's been 45 days... more than the household's 45-day threshold." Fixed: switched to `Math.ceil` for this field (guaranteed > threshold since threshold is an integer and raw is strictly greater), added a boundary-value regression test.
- [x] [Review][Patch] Dialog's async loading→loaded/error transition has no `aria-live` region [web/src/components/dashboard/status-detail-dialog.tsx:95-139] — breaks the established codebase precedent for this exact class of transition (`status-card.tsx`'s `aria-live="polite"` wrapper, explicit AC #7 citation). Fixed: wrapped the loading/error/populated content in a persistent `aria-live="polite"` region, mirroring `status-card.tsx` exactly.
- [x] [Review][Patch] `StatusDetailDialog` can be force-unmounted mid-view and silently reopen [web/src/components/dashboard/dashboard-page.tsx:79-96] — `detailDialogOpen` state lives in `DashboardPage` and isn't reset when `showPopulated` transitions to `false` (e.g. a transient background status-refresh failure while the dialog is open). Fixed: added a `useEffect` closing the dialog whenever `showPopulated` goes false, mirroring the existing `openRegressionPrompt` supersession effect; added a regression test covering the null-then-repopulate sequence.
- [x] [Review][Patch] Day figures bypass `Intl.NumberFormat`, contra AD-18 [web/src/components/dashboard/status-detail-dialog.tsx:117,133-134] — `elapsedDays` and the low-confidence explanation's `days`/`threshold` are interpolated as raw `Math.round(...)`/int values, never passed through the component's own `numberFormat.format(...)`, unlike the kWh figures in the same file. Fixed: all three now go through `numberFormat.format(...)`.
- [x] [Review][Patch] `elapsedDays` rounding can overstate the "over X days" claim [web/src/components/dashboard/status-detail-dialog.tsx:117] — `Math.round(182.5)` → 183 renders "over 183 days" when only 182.5 have actually elapsed. Fixed: switched to `Math.floor`; updated the existing test assertion (182.5 → "over 182 days").
- [x] [Review][Patch] `CurrentStatusResult`/`StatusDetailResponse` 8-arg positional construction risks silent transposition [src/EnergyTracker.Application/GetCurrentStatus.cs:81-89] — several adjacent same-typed fields (`decimal`/`decimal`, `double`/`double`); a future edit swapping two of them would compile cleanly and only surface as wrong numbers on screen. Fixed: switched to named arguments at both construction call sites (`GetCurrentStatus.cs` and `StatusEndpoints.cs`'s `ToDetailResponse`).
- [x] [Review][Patch] Two separate `DateTimeOffset.UtcNow` reads within one request [src/EnergyTracker.Application/GetCurrentStatus.cs:64,74] — `daysSinceLastReading`'s calculation and the coverage-window `to` bound each call `UtcNow` independently. Fixed: captured once into a local `now`.
- [x] [Review][Patch] Cross-household isolation test for `/api/status/detail` only checks one direction [tests/EnergyTracker.Api.Tests/StatusEndpointsTests.cs:174-194] — `A_households_Status_detail_is_never_affected_by_another_households_readings_or_baseline` creates `otherClient`/`otherHouseholdId` but never fetches/asserts the other household's own `/api/status/detail` response. Fixed: added the symmetric assertion — which caught a genuine pre-existing bug in the test's own fixture data (see note below).
- [x] [Review][Defer] Duplicate "Close" accessible name inside the dialog [web/src/components/dashboard/status-detail-dialog.tsx:141-145] — deferred, pre-existing: the footer `Close` button and shadcn `DialogContent`'s built-in "X" button share an accessible name; the same pattern already exists in every `DialogFooter` usage in `tagging-scaffold-manager.tsx`, not introduced by this diff.

_Dismissed as noise (4): `DialogDescription` restating the trigger's own label (cosmetic, sr-only, no functional impact) · "Difference: Right on pace." phrasing (explicit story direction — Task 5 mandates reusing exactly these copy keys) · modal-stacking "already-open" case relying on Radix's own focus trap rather than the cited effect (structurally already covered, not actionable) · sprint-status log entry's "purely additive" framing being narrower than literal truth (documentation nuance, not a code defect)._

_Applying the cross-household-isolation patch surfaced a genuine pre-existing bug in the test's own fixture: `otherClient`'s baseline reading used `kwhValue: 0m`, which `CreateMeterReading.cs:48` rejects (`kwhValue <= 0`) — that POST silently failed (its response was never checked), leaving `otherClient` with only one persisted reading and therefore an undefined Status, invisible until this patch's new assertion tried to read the (correctly empty) response body. Fixed by changing the fixture to `1m`._

_All patches verified: 319 backend tests green (166 Application + 116 Api.Tests via Testcontainers Postgres+SqlServer + 34 Infrastructure + 3 Architecture, 2 new: the boundary-fixture repair and the symmetric isolation assertion), `dotnet build` clean in Debug and Release. 168 frontend tests green (2 new: the rounding-boundary regression, the dialog-does-not-silently-reopen regression; 1 existing assertion updated for the floor-vs-round change), `tsc -b` clean, `oxlint` clean (pre-existing warnings only, none touching this story's files), `vite build` clean._

## Dev Notes

### Architecture compliance (binding, not optional)

- **AD-7 — reuse, don't re-derive.** `GetCurrentStatus.ExecuteAsync` is already "the single place the live computation runs" per its own file comment (`src/EnergyTracker.Application/GetCurrentStatus.cs:16-18`), consumed today by both `GET /api/status` and `IStatusRecomputeService`'s snapshot-writing path. This story adds a **third** consumer (`GET /api/status/detail`) of the exact same method — it must not spawn a second calculation path, a new use-case class, or a locally-adjusted copy of the pace/baseline/low-confidence logic. Task 1 only widens the *return value* of the existing method; the computation itself is untouched.
- **AD-14 — no new exposure risk.** The 4 new fields (`ElapsedDays`, `TrendingThresholdKwh`, `DaysSinceLastReading`, `LowConfidenceGapDaysThreshold`) are all scalars already computed from `MeterReading`/`Household` data inside the AD-14-guarded `GetCurrentStatus.ExecuteAsync` — none of them are `SmartPlugReading`/`Event` data, so `PatternDetectiveDoesNotReferenceSmartPlugOrEventDataTests` (which guards this exact file) needs no changes and should still pass unmodified.
- **AD-15 — no new config surface.** `TrendingThresholdKwh` and `LowConfidenceGapDays` are Story 2.4's existing `Household`-scoped columns (`src/EnergyTracker.Domain/Household.cs:23,29`) — this story only *reads and returns* them, it does not add a new tunable or a new column.
- **AD-18 — locale-driven formatting only.** All new frontend figures (kWh, days) use `Intl.NumberFormat(household.locale, ...)`, the same discipline Story 2.5's `status-card.tsx` already established — never a hardcoded format or a raw JS number-to-string conversion.
- **Consistency Conventions — drill-down endpoint discipline.** "The Dashboard Status endpoint returns only the current Status value and its one headline/supporting sentence — drill-down data is always a separate endpoint, never merged into the Status response." This story is the concrete case that convention was written for. `StatusResponse`/`/api/status`'s `ToResponse` mapping must not change at all in this story's diff — only additive code (a new record, a new mapping method, a new route) is expected in `StatusEndpoints.cs`.
- **UX-DR13 (one-level-deep modal stacking).** Cited at Epic 2's header level, and already implemented once for the Log Reading sheet / Meter Regression prompt pair (Story 2.3). This story's `StatusDetailDialog` is a second, independent modal on the same Dashboard surface — Task 6's supersession `useEffect` extends the same discipline to it, even though Story 2.7's own AC text (unlike Stories 2.1-2.6) cites no UX-DR numbers at all.

### Why this story has no UX-DR citations or dedicated mockup (read before assuming something was missed)

Story 2.7 was added to Epic 2 on 2026-08-23, **after** the UX design pass (`ux-energy-tracker-2026-08-08/`) was frozen — confirmed via `sprint-status.yaml`'s own changelog comment and via a full `grep` across `mockups/`, `EXPERIENCE.md`, and `DESIGN/components.md` finding no Status-Detail-specific mockup, component spec, or UX-DR reference anywhere. Unlike Stories 2.1-2.6, this story's AC text (epic file, verbatim) cites zero `UX-DR*` tags. This is not a gap to fill — it means the story has no bespoke visual spec to follow, so it must instead extend the **existing, already-shipped** Status card / Dashboard visual language: `GlassCard`, `GLASS_MODAL_CLASSNAME`, the shadcn `Dialog` primitives, and the tabular-nums/label-value figure convention `DESIGN/components.md` documents for the closest analogous component (the Tariff Radar summary card). Do not invent a new visual treatment or go looking for a mockup that doesn't exist.

### Existing code this story builds on (read before writing anything)

- `src/EnergyTracker.Application/GetCurrentStatus.cs` — the entire computation this story exposes more of. `CurrentStatusResult` (currently 4 fields) is the record Task 1 extends. Read the full method — every new field Task 1 needs is already a local value computed somewhere in this method's body, nothing new needs computing.
- `src/EnergyTracker.Api/Endpoints/StatusEndpoints.cs` — the file Task 2 extends. Copy `TryGetHouseholdId`'s existing usage pattern, `ToStatusString`'s existing switch, and the existing `Results.Ok(result is null ? null : ToResponse(result))` shape verbatim for the new route.
- `src/EnergyTracker.Domain/Calculations/PatternDetectiveCalculator.cs` — read `ComputePaceToDate`'s `PaceToDateResult.Elapsed` and `ResolveStatus`'s exact tie-breaking rule (`difference > threshold` → Trending, else `< 0` → BelowBaseline, else WithinRange) — Task 5's difference-row copy must stay consistent with this exact boundary logic (this is exactly why Task 4 extracts and reuses `computeStatusDifference` rather than re-deriving the sign independently).
- `web/src/components/dashboard/status-card.tsx` — the component both Task 4 (refactor) and Task 6 (new prop) touch. Its `rawDifference`/`roundedMagnitude` branching (lines ~96-106) is the exact logic Task 4 extracts; its `emptyStateAction` prop (already in `StatusCardProps`) is the exact pattern Task 6's `detailTrigger` prop mirrors.
- `web/src/components/dashboard/dashboard-page.tsx` — owns `showEmptyState`/`showPopulated` derivation already; Task 6 reuses `showPopulated` as the same guard for constructing/passing the new dialog, and adds the new `detailDialogOpen` state + supersession effect alongside the existing `openRegressionPrompt`/`logSheetOpen` wiring already in this file.
- `web/src/lib/status-api.ts` — the file Task 3 extends; replicate its existing `ApiError`/`toApiError`/empty-body-as-null pattern exactly, do not diverge.
- `web/src/lib/glass-classnames.ts` — `GLASS_MODAL_CLASSNAME`, reused as-is by `StatusDetailDialog` (Task 5), the same constant `tagging-scaffold-manager.tsx`'s dialogs and the Meter Regression prompt dialog already use.
- `web/src/components/ui/dialog.tsx`, `web/src/components/ui/skeleton.tsx`, `web/src/components/ui/button.tsx` — standard shadcn primitives reused unmodified (per `DESIGN/components.md`: "Standard shadcn components (Dialog, Input, Button..., `details`/accordion, Table) are used unmodified... The brand-layer discipline is: don't customize what doesn't need customizing").
- `web/src/App.tsx` — **not touched by this story.** `openRegressionPrompt` is already threaded into `DashboardPage` as a prop; Task 6's supersession effect consumes it there, no new prop needs to flow through `App.tsx`.

### File structure / conventions to follow exactly

- New dashboard-specific component lives under `web/src/components/dashboard/` (existing folder, Story 2.5's convention) — `status-detail-dialog.tsx`, colocated `status-detail-dialog.test.tsx`.
- New shared pure-logic module lives flat under `web/src/lib/` (existing convention, alongside `status-api.ts`) — `status-difference.ts`, colocated `status-difference.test.ts`.
- `verbatimModuleSyntax: true` — `import type { StatusDetailDto, StatusValue }` wherever type-only.
- Both `en-US` and `de-DE` `translation.json` updated together (Task 7) — never one locale alone; verify key-set parity (Story 2.5/2.6's established discipline).
- .NET: one use-case class per file stays true — this story adds **zero** new use-case classes (Task 1 only extends an existing record; Task 2 only extends an existing endpoints file), consistent with the reuse discipline Stories 2.4-2.6 all followed.
- Test naming: `.NET` — `Snake_case_with_underscores`, Shouldly, mirrored 1:1 into the existing test files (`GetCurrentStatusTests.cs`, `StatusEndpointsTests.cs`) rather than new files, since no new production class is being added on the backend. Frontend — colocated, Vitest + Testing Library, `vi.stubGlobal('fetch', ...)` mocking per `status-api.test.ts`'s existing pattern.

### Testing standards summary

.NET: xunit.v3.mtp-v2, Shouldly, NSubstitute, `TestContext.Current.CancellationToken`. API integration tests use `EnergyTrackerApiFactory` + real Postgres via Testcontainers (`StatusEndpointsTests.cs`'s existing pattern — reuse its `CreateHouseholdAsync`/`PostReadingAsync`/`SetYearlyBaselineAsync` helpers directly, don't re-write them). Frontend: Vitest + `@testing-library/react`, `jsdom`, globals on, i18next real-catalog (no test-provider wrapper needed, per Story 2.5's established precedent).

### Previous story intelligence (Stories 2.4, 2.5, 2.6)

- Story 2.4 built `GetCurrentStatus`/`GET /api/status` specifically as a single, reusable live-computation seam — this story is the first to add a *second* consumer beyond Story 2.5's Dashboard fetch, confirming the seam was designed correctly for exactly this kind of extension.
- Story 2.5's own code review fixed a real bug in the pace/baseline difference sign logic at the `(-0.5, 0)` kWh boundary — Task 4 exists specifically to make that fix un-re-breakable by construction (single source of truth, not copy-paste).
- Story 2.6 established the precedent of adding new routes to an *existing* endpoints file (`TaggingScaffoldEndpoints.cs`) rather than fragmenting closely-related concerns across new files, and of reusing an existing generic `Dialog`/`GLASS_MODAL_CLASSNAME` shell for a new drill-down UI rather than building bespoke sheet markup, even when there's a mockup that suggests otherwise (there isn't one here at all). Both precedents are followed directly by this story's Task 2 and Task 5.
- `review-accessibility-2026-08-16.md` flagged icon-only controls relying solely on `title` as a **high**-severity accessibility gap elsewhere in the UX set — this story's trigger sidesteps that class of issue by using visible text from the start (Task 6), not an icon needing a supplementary `aria-label` retrofit.

### Project Structure Notes

- Alignment: follows the exact `web/src/components/{feature}` + colocated-test convention and the flat `web/src/lib/` convention every prior frontend story already established. No deviation.
- No migration, no new DI registration, no new use-case class — this is the smallest-surface-area story in Epic 2 so far by design: every piece of data the details view needs already exists somewhere in `GetCurrentStatus.ExecuteAsync`'s local scope, `Household`'s existing columns, or the Status card's existing copy keys.

### References

- [Source: `_bmad-artifacts/planning/epics/epic-2-meter-reading-pattern-detective-status-core.md#Story 2.7`] — story statement + AC source (verbatim).
- [Source: `_bmad-artifacts/planning/prds/prd-energy-tracker-2026-08-08/prd/4-features.md#FR-30`] — FR consequences, exact wording this story's ACs mirror.
- [Source: `_bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/consistency-conventions.md`] — "Dashboard Status endpoint" drill-down convention, the direct basis for AC #6 / Task 2's route design.
- [Source: `_bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-7, #AD-14, #AD-15, #AD-18`] — exact AD rule text this story must not violate.
- [Source: `_bmad-artifacts/planning/epics/requirements-inventory.md#UX-DR13`] — "one-level-deep modal/sheet stacking system-wide," basis for Task 6's supersession effect.
- [Source: `_bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/DESIGN/components.md`] — label/value figure-row convention (Tariff Radar summary card section) used as the closest analogous precedent; "standard shadcn components... used unmodified" discipline.
- [Source: `_bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/review-accessibility-2026-08-16.md`] — icon-only-control finding, basis for Task 6's visible-text-trigger decision.
- [Source: `_bmad-artifacts/implementation/2-4-gap-tolerant-rolling-baseline-status-computation.md`] — previous story intelligence: `GetCurrentStatus`/`PatternDetectiveCalculator`/`StatusEndpoints` implementation and its own code-review findings (rollover/reset correction, 365-day windowing) this story's Task 1 must not disturb.
- [Source: `_bmad-artifacts/implementation/2-5-dashboard-status-display.md`] — previous story intelligence: `status-card.tsx`'s difference-sign bug fix (basis for Task 4), `dashboard-page.tsx`'s composition pattern, the `dashboard.status.*` i18n keys this story reuses for the difference row.
- [Source: `_bmad-artifacts/implementation/2-6-room-power-point-device-re-parenting.md`] — previous story intelligence: additive-route-in-existing-file precedent (Task 2), generic-`Dialog`-over-bespoke-sheet precedent (Task 5), icon-vs-visible-label `aria-label` discipline (Task 6).
- [Source: `src/EnergyTracker.Application/GetCurrentStatus.cs`, `src/EnergyTracker.Domain/Calculations/PatternDetectiveCalculator.cs`, `src/EnergyTracker.Domain/Household.cs`, `src/EnergyTracker.Api/Endpoints/StatusEndpoints.cs`, `src/EnergyTracker.Application/Ports/ISmartPlugCoverageSignal.cs`] — existing backend code this story extends read-only except for the two additive changes in Tasks 1-2.
- [Source: `web/src/components/dashboard/status-card.tsx`, `web/src/components/dashboard/dashboard-page.tsx`, `web/src/lib/status-api.ts`, `web/src/lib/glass-classnames.ts`, `web/src/App.tsx`] — existing frontend code this story extends.
- [Source: `web/src/locales/en-US/translation.json`, `de-DE/translation.json`] — existing `dashboard.status.*` key set this story extends and partially reuses (Task 7).

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

None — no debugging sessions required; implementation matched the story's Dev Notes on the first pass (backend and frontend suites both green before any fix-up beyond two test-authoring issues caught by the frontend test run itself, see Completion Notes).

### Completion Notes List

- Task 1/2 (backend): `CurrentStatusResult` extended with `ElapsedDays`, `TrendingThresholdKwh`, `DaysSinceLastReading`, `LowConfidenceGapDaysThreshold` — all four already computed inside `GetCurrentStatus.ExecuteAsync`, no new calculation logic added (AD-7 preserved: `/api/status` and the new `/api/status/detail` both call the same method). New `GET /api/status/detail` route added to the existing `MapStatusEndpoints`, purely additive — `/api/status`'s `ToResponse`/`StatusResponse` untouched.
- Task 4 (frontend refactor): `computeStatusDifference` extracted verbatim from `status-card.tsx` into `web/src/lib/status-difference.ts`; `status-card.tsx` now calls the shared function. All pre-existing `status-card.test.tsx` cases pass unmodified, confirming the refactor is behavior-preserving.
- Task 5 (dialog): `StatusDetailDialog` built on the existing `Dialog` + `GLASS_MODAL_CLASSNAME` shell (no bespoke visual language, per the story's "no mockup exists" Dev Note). Fetch-on-open/reset-on-close implemented with a cancellation flag to avoid a stale-closure state update after a fast open/close/open sequence.
- Task 6 (wiring): `detailTrigger` slot added to `StatusCardProps`, rendered only in the populated branch. `DashboardPage` owns `detailDialogOpen` state and a `useEffect` that force-closes it when `openRegressionPrompt` transitions to non-null (UX-DR13 supersession, mirroring the Log Reading sheet's existing behavior in `App.tsx`).
- Task 7 (i18n): `dashboard.statusDetail.*` keys added to both `en-US` and `de-DE`, verified byte-for-byte key-set parity via a script (no drift). Reused the existing `dashboard.status.body.onPace`/`underPace`/`overPace` keys for the difference row rather than duplicating copy.
- Test-authoring fixes found by the frontend suite itself (not separate debugging — caught and fixed inline before considering the task done): (1) an initial `DialogDescription` duplicated the dialog title's exact text, which made `getByText('Status calculation')` ambiguous in tests — changed the description's copy to differ from the title; (2) `getByRole('button', { name: 'Close' })` was ambiguous against the shadcn `DialogContent`'s built-in "X" close button (also accessibly named "Close") — disambiguated in the one test that clicks the footer Close button specifically.
- All 8 ACs verified: AC #1 (pace/baseline/elapsed/difference/threshold all rendered), AC #2 (no Meter Reading list — structurally impossible, `StatusDetailDto` is 8 scalars), AC #3 (low-confidence explanation with concrete day counts), AC #4 (no chart, labeled figures only), AC #5 (`detailTrigger` only passed when `showPopulated`, so undefined Status never offers the trigger), AC #6 (separate `/api/status/detail` endpoint, `/api/status` unchanged).
- Full regression: 319 backend tests green (166 Application + 34 Infrastructure + 3 Architecture + 116 Api.Tests, all via `dotnet test EnergyTracker.sln`), `dotnet build` clean in Debug and Release. 166 frontend tests green (16 new: 4 `status-difference`, 3 `status-api`, 1 `status-card`, 7 `status-detail-dialog`, 1 `dashboard-page`), `tsc -b` clean, `oxlint` clean (pre-existing warnings only, none touching this story's files), `vite build` clean.
- No `docs/*.md` changes — confirmed no new operator-facing configuration, migration, or adapter/env var (Task 9).

### File List

**Backend:**
- `src/EnergyTracker.Application/GetCurrentStatus.cs` (modified)
- `src/EnergyTracker.Api/Endpoints/StatusEndpoints.cs` (modified)
- `tests/EnergyTracker.Application.Tests/GetCurrentStatusTests.cs` (modified)
- `tests/EnergyTracker.Api.Tests/StatusEndpointsTests.cs` (modified)

**Frontend:**
- `web/src/lib/status-api.ts` (modified)
- `web/src/lib/status-api.test.ts` (modified)
- `web/src/lib/status-difference.ts` (new)
- `web/src/lib/status-difference.test.ts` (new)
- `web/src/components/dashboard/status-card.tsx` (modified)
- `web/src/components/dashboard/status-card.test.tsx` (modified)
- `web/src/components/dashboard/status-detail-dialog.tsx` (new)
- `web/src/components/dashboard/status-detail-dialog.test.tsx` (new)
- `web/src/components/dashboard/dashboard-page.tsx` (modified)
- `web/src/components/dashboard/dashboard-page.test.tsx` (modified)
- `web/src/locales/en-US/translation.json` (modified)
- `web/src/locales/de-DE/translation.json` (modified)
