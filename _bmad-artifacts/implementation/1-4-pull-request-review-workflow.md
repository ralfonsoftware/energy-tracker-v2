---
baseline_commit: 24a53b1eb0361225ff15302cc9398ef1cc5afcdb
---

# Story 1.4: Pull Request Review Workflow

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a contributor,
I want a GitHub Actions workflow that runs on every pull request,
so that build failures, test failures, or infrastructure drift are caught before merge, without deploying anything.

## Acceptance Criteria

1. **Given** a pull request opened or updated against `main`, **when** the workflow runs, **then** it restores/builds the .NET solution and `web/` frontend, runs the full test suite, and reports pass/fail as a PR status check.
2. **Given** the same PR, **when** linting is configured for the frontend/backend, **then** lint failures are also reported as a status check.
3. **Given** the PR workflow needs to validate infrastructure changes, **when** `infra/**` Bicep files changed in the PR, **then** it authenticates via OIDC federated-credential login scoped to the `pull_request` subject — distinct from the `main`-branch credential used for real deploys — and runs a read-only `what-if`/validate operation only, never an actual deployment.
4. **Given** a PR opened from a fork, **when** the workflow runs, **then** no Azure login step executes with deploy-capable permissions, since the federated credential's trust policy does not cover fork-originated runs — avoiding a credential-leak/privilege-escalation path.
5. **Given** the workflow's checks, **when** all pass, **then** the PR is unblocked for merge per branch protection; when any check fails, the PR is blocked.

## Tasks / Subtasks

- [x] Task 1: Author the CI build/test/lint job (AC #1, #2)
  - [x] New workflow file `.github/workflows/pr-review.yml`, trigger: `pull_request: { branches: [main] }` (no `paths:` filter at the workflow level — AC #1 requires build/test to run for **every** PR against `main` regardless of which files changed; contrast with `infra-deploy.yml`'s `infra/**` filter, which doesn't apply here since this workflow's build/test job isn't infra-specific)
  - [x] `permissions: { contents: read }` at the workflow level for this job — no `id-token: write` here (that's scoped to the `validate-infra` job only, Task 2, keeping the least-privilege boundary explicit per-job)
  - [x] Concurrency: `group: pr-review-${{ github.event.pull_request.number }}`, `cancel-in-progress: true` — set once at the **workflow level** (top-level `concurrency:` key, sibling of `on:`/`permissions:`), not per-job. This workflow has two jobs (`build-test-lint` here, `validate-infra` in Task 2); a workflow-level `concurrency:` block applies the same cancel-on-resupersede behavior to both, so a rapid sequence of pushes to the same PR doesn't leave stale `what-if` runs piling up in `validate-infra` while only `build-test-lint` gets canceled. `app-deploy.yml`/`infra-deploy.yml` only ever had one job each, so there's no existing precedent to copy for this two-job case — don't default to a job-level block by analogy with those files. This is **deliberately the opposite** of `app-deploy.yml`/`infra-deploy.yml`'s `cancel-in-progress: false`: those must never race concurrent deploys against the same live resource, while a PR check has no such shared-mutable-state risk, and canceling a superseded run on `synchronize` (new push to the PR) saves CI minutes instead of running two checks for a commit nobody cares about anymore.
  - [x] Job `build-test-lint`, `runs-on: ubuntu-latest`, `timeout-minutes: 30` (same budget as `app-deploy.yml`'s job, for the same reason — bound a hung step)
  - [x] `actions/checkout@v7` (default shallow clone — this job never diffs against `base`, unlike Task 2's job)
  - [x] `actions/setup-dotnet@v6` with `global-json-file: global.json` (picks up SDK `10.0.100`/`rollForward: latestFeature` — do not hardcode a `dotnet-version`, exactly as `app-deploy.yml` already does)
  - [x] `dotnet restore EnergyTracker.sln`, then `dotnet build EnergyTracker.sln --no-restore --configuration Release` — **Release**, not the default Debug, to match what `app-deploy.yml` gates and what `Dockerfile` actually publishes (this exact Debug/Release mismatch was a Story 1.3 review finding — don't reintroduce it here)
  - [x] `dotnet test EnergyTracker.sln --no-restore --configuration Release` — runs all three test projects (`Api.Tests`, `Infrastructure.Tests`, `Architecture.Tests`) via the `Microsoft.Testing.Platform` runner (`global.json`). `Infrastructure.Tests` uses Testcontainers, which drives Docker directly — `ubuntu-latest` ships Docker pre-installed, so no `services:` block is needed, exactly as `app-deploy.yml` already established. A non-zero exit here must fail the job (AC #1) — do not add `continue-on-error:` or `if: always()` anywhere in this sequence.
  - [x] `actions/setup-node@v7` with `node-version: '22'` — matches `Dockerfile`'s `node:22-alpine` build stage exactly, same reasoning `app-deploy.yml` documents (don't follow `web/package.json`'s `@types/node ^24`, that's a type-checking version only)
  - [x] `npm --prefix web ci` (reproducible, matches `Dockerfile`'s frontend stage — never `npm install`), `npm --prefix web run build`, `npm --prefix web run test` (Vitest, single run via `vitest run`)
  - [x] Lint step (AC #2): `npm --prefix web run lint` (this repo's frontend lint is `oxlint`, already configured in `web/package.json`'s `lint` script — add it as its own step so a lint failure is individually visible in the PR checks UI, not buried inside the build step). **No backend/.NET linting is currently configured anywhere in this repo** (no `.editorconfig`, no `EnableNETAnalyzers`/`AnalysisLevel`/`TreatWarningsAsErrors` in any `.csproj`/`Directory.Build.props` — verified by search at story-creation time). AC #2's condition ("when linting is configured...") is therefore **not yet true for the backend** — do not invent a backend lint gate (e.g. `dotnet format --verify-no-changes`) as part of this story; that would silently expand scope beyond what's configured today. If backend analyzers get configured in a later story, this workflow's frontend-lint step is the pattern to extend, not something to redesign.
  - [x] Playwright `test:e2e` is **not** included in this gate, for the same reasoning `app-deploy.yml`/Story 1.3 already established and documented (epic AC only names "the test suite" without specifying e2e; browser-install + server-spinup cost/flakiness isn't justified here). Keep this consistent rather than re-litigating it — note the decision in Completion Notes rather than silently omitting it.

- [x] Task 2: Author the conditional, fork-safe infra validation job (AC #3, #4)
  - [x] Same workflow file, second job: `validate-infra`, `runs-on: ubuntu-latest`, `timeout-minutes: 15`, independent of `build-test-lint` (no `needs:` — both should start immediately in parallel; infra validation failing should not block seeing .NET/frontend results and vice versa, and neither result depends on the other)
  - [x] `permissions: { id-token: write, contents: read }` on this job only — `build-test-lint` must **not** carry `id-token: write` (least-privilege boundary, and matches AC #4's spirit of not handing OIDC capability to a job that never needs it)
  - [x] `actions/checkout@v7` with **`fetch-depth: 0`** (full history) — this job (unlike `build-test-lint`) needs to diff the PR's changed files against `base`, and a PR's auto-generated merge-commit ref (`refs/pull/N/merge`, what `pull_request` events check out by default) has its parent commits unavailable in a depth-1 shallow clone, so a bare `git diff HEAD^1 HEAD` would fail with "unknown revision" on a shallow checkout
  - [x] Step "Check for infra changes" (`id: changes`): `git diff --name-only ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }} | grep -q '^infra/' && echo "infra_changed=true" >> "$GITHUB_OUTPUT" || echo "infra_changed=false" >> "$GITHUB_OUTPUT"` — a plain `git diff`/`grep`, no new third-party action (`dorny/paths-filter` etc. would work too, but this repo has so far used only first-party/`docker`/`azure` actions; keep that pattern rather than introducing a new dependency for one boolean check)
  - [x] Step "Check if PR is from a fork" (`id: origin`): `echo "is_fork=${{ github.event.pull_request.head.repo.full_name != github.repository }}" >> "$GITHUB_OUTPUT"` — this is the explicit guard AC #4 requires. Do not rely solely on GitHub's built-in behavior of withholding secrets/OIDC tokens from fork-triggered `pull_request` runs (real, but an *implicit* safety net — if relied on alone, a fork PR that touches `infra/**` would hit `azure/login` and fail with an opaque credential error instead of a clear, intentional skip). Making the fork check an explicit, named condition is what makes AC #4 verifiable by reading the workflow, not just true by accident of platform behavior.
  - [x] All remaining steps in this job gated on **both** `steps.changes.outputs.infra_changed == 'true'` **and** `steps.origin.outputs.is_fork == 'false'` via `if:`
  - [x] Azure login (OIDC): `azure/login@v3` with `client-id: ${{ secrets.AZURE_CLIENT_ID }}`, `tenant-id: ${{ secrets.AZURE_TENANT_ID }}`, `subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}` — **reuse the same three repository secrets** `app-deploy.yml`/`infra-deploy.yml` already use (same managed identity). This relies on the existing identity's **second federated credential**, already provisioned for the `pull_request` subject (see Task 3) — no second identity, and no new credential to create. The AC's "distinct... credential" refers to the federated-credential trust entry (subject `pull_request` vs. `main`-branch), not a distinct Azure identity/secret set.
  - [x] Resolve the resource group's what-if target the same way `infra-deploy.yml` does: `${{ vars.AZURE_RESOURCE_GROUP_NAME }}` (repo variable, not a secret — do not hardcode or reintroduce a second name)
  - [x] Run **only** `az deployment group what-if` (never `az deployment group create`) against `infra/main.bicep` / `infra/main.bicepparam`, mirroring `infra-deploy.yml`'s existing `What-if (manual runs only)` step verbatim in mechanism — including passing `DATABASE_ADMIN_PASSWORD` from `secrets.DATABASE_ADMIN_PASSWORD` as an env var (the `@secure()` bicepparam parameter needs it to resolve even for a dry-run; `what-if` does not persist or expose this value)
  - [x] Add a clearly-worded step (no `if:` restriction, always runs) that echoes a one-line notice when infra changed but validation was skipped because the PR is from a fork (`steps.changes.outputs.infra_changed == 'true' && steps.origin.outputs.is_fork == 'true'`) — e.g. `::warning::infra/** changed in this fork PR; what-if validation was skipped (fork PRs never get Azure credentials) — a maintainer should check out the branch locally and run 'az deployment group what-if' before merging.` This keeps AC #4's silent-skip invisible-to-reviewers risk from becoming its own gap — the job still reports success (no false-red block on a legitimate contribution), but the PR page isn't silently missing information a reviewer needs.

- [x] Task 3: Verify the existing `pull_request` federated credential (prerequisite for Task 2 to function; not itself expressible in Bicep/workflow YAML)
  - [x] **No credential needs to be created by this story.** The user-assigned managed identity `infra-deploy.yml`/`app-deploy.yml` already authenticate as — `energy-tracker-devops-uami` — already has **two** federated credentials configured: one trusting the `main`-branch push subject (pre-existing, documented in `infra/README.md`'s "One-time identity bootstrap" section) and one trusting the `pull_request` subject (already provisioned ahead of this story, outside of it). Task 2's `azure/login@v3` step in `validate-infra` can rely on the `pull_request`-subject credential already being in place — do not re-run `az identity federated-credential create` for it.
  - [x] Confirm the existing `pull_request` credential's subject matches GitHub's documented OIDC subject-claim format for the `pull_request` event, `repo:ralfonsoftware/energy-tracker-v2:pull_request` — verify with `az identity federated-credential list --identity-name energy-tracker-devops-uami --resource-group <bootstrap-resource-group>` if live Azure CLI access is available; if not available, state in Completion Notes that the subject was taken on the user's word rather than independently verified, and that Task 2 should be watched for an `AADSTS70021`-shaped failure on first real PR run if the subject turns out to differ.
  - [x] No new GitHub repository secrets are needed — `AZURE_CLIENT_ID`/`AZURE_TENANT_ID`/`AZURE_SUBSCRIPTION_ID` are identical across all three workflows (same identity, now three federated-credential trust entries total: `main`-branch push [shared by `infra-deploy.yml` and `app-deploy.yml`] and `pull_request` [this story])
  - [x] Update `infra/README.md`'s "One-time identity bootstrap (already done — reference only)" section to note the identity now carries **two** federated credentials rather than one — add a reference-only entry for the `pull_request`-subject credential alongside the existing `main`-branch one, in the same style (a documentation record of what already exists, not a re-run instruction)

- [x] Task 4: Branch protection (manual GitHub repo-settings step, not expressible in workflow YAML or Bicep) (AC #5)
  - [x] Document (in `infra/README.md` or a comparable ops doc) that `main` needs GitHub branch protection with required status checks matching this workflow's two job names (`build-test-lint`, `validate-infra`) enabled via Settings → Branches → Branch protection rules, or `gh api repos/ralfonsoftware/energy-tracker-v2/branches/main/protection` — this is what actually makes AC #5's "blocks merge on failure" true; the workflow YAML itself only produces pass/fail status checks, it cannot make GitHub *require* them
  - [x] Note: `validate-infra` reports success even when its steps are skipped (no infra changes, or fork-origin skip per Task 2) — a required status check on a job with conditional steps still reports an overall pass when the job itself doesn't fail, so making it a required check does not block non-infra PRs. Confirm this understanding is correct by reading GitHub's docs on required status checks with conditional jobs, or note it as unverified if not confirmed live.
  - [x] If enabling branch protection is done via `gh api`/`gh ruleset`, note the exact command run in Completion Notes; if done manually via the Settings UI (no CLI trace), say so explicitly rather than fabricating a command that wasn't actually run

- [x] Task 5: Verify against every AC
  - [x] AC #1: verify structurally (workflow YAML has no `paths:` filter, `dotnet test`/`npm run test` steps have no `continue-on-error`) and, if a real PR can be opened against this branch, live — confirm the `build-test-lint` check appears on the PR and fails when a test is deliberately broken (inject a failing test locally, confirm non-zero exit, revert — same non-disruptive technique Story 1.3 used, applied to a PR branch instead of `main` this time so it's safe to actually push)
  - [x] AC #2: confirm `npm --prefix web run lint` fails on a deliberately-introduced lint violation (e.g. an unused variable) locally before reverting; confirm the backend has no equivalent gate and that this is a documented, deliberate scope boundary (not an oversight)
  - [x] AC #3: confirm `validate-infra`'s Azure-login/what-if steps only execute when `infra/**` changed (test with a PR that touches only `src/**` — steps should show as skipped) and confirm the what-if step's `run:` never contains `deployment group create`
  - [x] AC #4: confirm the fork-check step's logic reads correctly for both cases (`github.event.pull_request.head.repo.full_name == github.repository` → same-repo PR → `is_fork=false`; a genuinely different `head.repo.full_name` → `is_fork=true`) — live verification of the fork case requires an actual fork PR, which may not be practical to set up; if not verified live, say so explicitly and document that the logic was verified by reading GitHub's documented event-payload shape for `pull_request` instead
  - [x] AC #5: verify Task 4's branch-protection configuration is actually in place (`gh api repos/ralfonsoftware/energy-tracker-v2/branches/main/protection` should list `build-test-lint`/`validate-infra` as required checks) — if not configured live, document this as an outstanding manual step rather than claiming it's done

## Dev Notes

- **This story adds no new Azure resources and changes no Bicep templates, and no new federated credential either.** The `pull_request`-subject federated credential on the existing identity (`energy-tracker-devops-uami`) was already provisioned ahead of this story — Task 3 is a verification/documentation step, not a creation step. This story's only artifacts are one new GitHub Actions workflow file and a documentation update to `infra/README.md`. Cost impact: zero (NFR2/NFR14) — `what-if` is a read-only ARM API call with no billable resource created.
- **Two workflows already establish the patterns this story must reuse, not reinvent:** `.github/workflows/app-deploy.yml` (build/test steps for .NET + web — copy the restore/build/test/setup-node/npm sequence nearly verbatim, Release configuration and all) and `.github/workflows/infra-deploy.yml` (the OIDC login shape, the `what-if` step's exact `az deployment group what-if` invocation and its `DATABASE_ADMIN_PASSWORD` env-var handling, and the `vars.AZURE_RESOURCE_GROUP_NAME` repo-variable convention). Read both files in full before writing `pr-review.yml` — this story is assembling already-proven pieces into a new trigger shape, not designing new CI mechanics from scratch.
- **Concurrency semantics deliberately differ from the other two workflows** — see Task 1's `cancel-in-progress: true` note. Don't copy `app-deploy.yml`/`infra-deploy.yml`'s `cancel-in-progress: false` by reflex; the rationale (avoid racing a shared live resource) doesn't apply to a PR check, and canceling superseded runs is the better default here.
- **The fork-safety mechanism (AC #4) has two layers, and Task 2 implements the outer one explicitly rather than relying on the inner one alone:**
  1. GitHub's platform-level behavior: `pull_request`-triggered workflow runs from a forked repository do not receive repository secrets or an OIDC token, regardless of `permissions: id-token: write` in the workflow file. This is real and is the actual security backstop.
  2. This story's explicit `is_fork` check (Task 2) makes that backstop *visible and intentional* in the workflow's own logic, so a reviewer can confirm AC #4 by reading the YAML rather than trusting undocumented platform behavior — and so a fork PR that touches `infra/**` gets a clear skip notice instead of an opaque `azure/login` failure.
- **Subject claim for the existing `pull_request` federated credential is expected to be `repo:ralfonsoftware/energy-tracker-v2:pull_request`** (constant string per GitHub's OIDC subject-claim format for the `pull_request` event — same value whether the PR originates from a branch in this repo or, hypothetically, a fork; the fork exclusion comes from GitHub withholding the token entirely for fork runs, not from the subject claim differing). This credential was already provisioned ahead of this story on `energy-tracker-devops-uami`; Task 3 confirms rather than assumes this subject. Verify this subject format against GitHub's current OIDC docs at implementation time rather than trusting it blindly — federated-credential subject formats have historically been an easy-to-get-subtly-wrong detail.
- **No backend lint gate exists yet, and this story does not create one** — see Task 1's lint step notes. Don't let AC #2's phrasing ("when linting is configured for the frontend/backend") be misread as "make sure both are configured"; it's a conditional describing what to gate *if* configured, and only the frontend currently is.
- **`fetch-depth: 0` is scoped to the `validate-infra` job only** (Task 2) — `build-test-lint` doesn't need full history and should stay on the default shallow checkout for speed; don't apply `fetch-depth: 0` to both jobs out of consistency-for-its-own-sake.
- **Two jobs, not one**, so GitHub's branch-protection required-status-checks can gate on `build-test-lint` and `validate-infra` independently (Task 4) — a hypothetical single combined job would make it impossible to distinguish "the app is broken" from "the infra plan is invalid" from the PR checks list alone.
- **This workflow must never run `az deployment group create`** — that's `infra-deploy.yml`'s exclusive job, gated to `push` on `main` only. Re-read AC #3's "read-only what-if/validate operation only, never an actual deployment" as a hard constraint, not a suggestion — accidentally wiring `create` into a PR-triggered workflow would let an unmerged PR branch modify live Azure infrastructure, which is exactly the class of mistake OIDC-subject-scoping (AC #3) and this AC together exist to prevent.
- **Task 4 is the piece of this story that cannot be verified by reading/running code alone** — it needs live GitHub repo-admin access. Task 3 no longer has this constraint: the `pull_request` federated credential already exists, so Task 3 is a lightweight verification (confirm the subject claim, update documentation) rather than a live-Azure-CLI-dependent creation step. Follow the same honesty discipline Story 1.3's Dev Notes established: state plainly in Completion Notes which ACs were verified live vs. only structurally, rather than implying full verification that didn't happen.
- **Constraints that still apply, unchanged**: AD-19 (secrets via env vars/repo secrets only, never committed or baked into anything — this story introduces zero new secret values and zero new trust relationships, since the `pull_request` federated credential already exists), NFR2/NFR14 (no new billable resources).

### Project Structure Notes

New/modified files this story introduces:

```text
energy-tracker-v2/
  .github/
    workflows/
      pr-review.yml            # new — pull_request-triggered, two jobs: build-test-lint (unconditional) and validate-infra (conditional on infra/** changes, fork-guarded)
      app-deploy.yml            # unchanged — do not modify or trigger from pr-review.yml
      infra-deploy.yml          # unchanged — do not modify or trigger from pr-review.yml
  infra/
    README.md                   # modified — document the already-existing pull_request federated credential (Task 3) and the branch-protection note (Task 4) in existing documentation sections
  web/
    .oxlintrc.json               # modified — no-unused-vars set to error so lint failures actually fail the check (AC #2); a config-only change, not application code
```

No changes to `src/`, `web/`'s application code, or `infra/*.bicep` — this is a CI-only story, same category as Story 1.2/1.3 but scoped to pull requests instead of pushes to `main`. The one exception is `web/.oxlintrc.json`, lint configuration rather than application code, needed to make AC #2 actually true (see Completion Notes).

### References

- [Source: _bmad-artifacts/planning/epics/epic-1-foundation-deployment-household-access.md#Story 1.4] — story statement and acceptance criteria (verbatim origin)
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE.md#AD-19] — secrets via env vars/repo secrets, never committed; unchanged by this story
- [Source: .github/workflows/app-deploy.yml] — established build/test step sequence (`actions/setup-dotnet@v6` with `global-json-file`, `dotnet restore`/`build --configuration Release`/`test --configuration Release`, `actions/setup-node@v7` node 22, `npm --prefix web ci`/`build`/`test`) this story's `build-test-lint` job reuses directly
- [Source: .github/workflows/infra-deploy.yml] — established OIDC login shape (`azure/login@v3`, the three `secrets.AZURE_*` names, `vars.AZURE_RESOURCE_GROUP_NAME`) and the exact `what-if` step (including `DATABASE_ADMIN_PASSWORD` env-var handling) this story's `validate-infra` job reuses; also the precedent comment already left there ("Story 1.4 owns a separate, read-only what-if-only workflow scoped to its own federated credential subject")
- [Source: infra/README.md#One-time identity bootstrap] — existing bootstrap documentation pattern (identity creation, role assignment, federated-credential creation, secret-setting) this story's Task 3 extends with a record of the already-existing second (`pull_request`-subject) federated credential on the same identity, `energy-tracker-devops-uami`
- [Source: _bmad-artifacts/implementation/1-3-ci-build-test-cd-deploy-pipeline-app-to-azure.md] — immediately preceding story; established the Debug-vs-Release CI/deploy-parity lesson (review finding, fixed), the `timeout-minutes: 30` job-level safeguard, the "don't disrupt `main` to prove a negative" verification technique, and explicitly deferred e2e/lint gating decisions to this story
- [Source: web/package.json] — frontend script names (`build`, `test`, `lint`) confirming `lint` (`oxlint`) already exists and just needs wiring into CI; `@types/node` version note (not the runtime Node version, see app-deploy.yml precedent)
- [Source: global.json] — `.NET` SDK `10.0.100`/`rollForward: latestFeature`, `Microsoft.Testing.Platform` test runner selection `dotnet test` depends on
- [Source: git remote — ralfonsoftware/energy-tracker-v2] — the exact repo slug for the existing `pull_request` federated credential's `subject: repo:ralfonsoftware/energy-tracker-v2:pull_request` claim
- [Source: https://docs.github.com/en/actions/deployment/security-hardening-your-deployments/about-security-hardening-with-openid-connect] — OIDC subject-claim format for the `pull_request` event, and GitHub's documented behavior of withholding secrets/OIDC tokens from fork-triggered `pull_request` runs — verify current at implementation time
- [Source: https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/deploy-what-if] — `az deployment group what-if` as the current, documented read-only validation mechanism this story's `validate-infra` job relies on

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

None — no persistent debug log was needed; all verification was interactive (local commands, live GitHub Actions runs on a throwaway PR, `az`/`gh` CLI queries).

### Completion Notes List

- **AC #1 (build/test/lint on every PR) — verified both structurally and live.** `pr-review.yml` has no `paths:` filter on the `pull_request` trigger. Locally: `dotnet restore/build/test` (Release) and `npm --prefix web ci/build/test` all pass with zero regressions. Live: opened throwaway PR #1 (branch `test/pr-review-verification`) against `main` with a deliberately broken `HealthEndpointTests` assertion — `build-test-lint` failed (non-zero exit, no `continue-on-error`/`if: always()` anywhere in the sequence) and GitHub reported `mergeStateStatus: BLOCKED`. Reverted the break, pushed again — check passed, `mergeStateStatus` moved to `CLEAN`. PR closed and branch deleted without merging (throwaway verification only, no application code changes intended to land).
- **AC #2 (lint failures reported as a status check) — a real gap was found and fixed, not just wired up.** `web/package.json`'s pre-existing `lint` script (`oxlint`, no flags) returns exit 0 even when it reports violations, because oxlint's eslint-compat rules default to `warn` severity and warnings alone don't fail the process without `--deny-warnings`. Verified locally: injected an unused-variable violation in `App.tsx` — `npm run lint` printed the warning but exited 0. Considered `oxlint --deny-warnings` globally, but that also flags the pre-existing, deliberate `react/only-export-components` warning in the shadcn-generated `button.tsx` (mixing component + `cva` export is the standard shadcn pattern) — turning that into a build-breaker would require touching application UI code, which is out of scope for this CI-only story per Dev Notes ("No changes to `src/`, `web/`, ... application code"). Instead, scoped the fix to `web/.oxlintrc.json`: added `"no-unused-vars": "error"` (same explicit-severity style already used for `react/rules-of-hooks`). Re-verified: clean tree exits 0 (pre-existing warning stays non-blocking), injected violation now exits 1. Backend has no lint gate configured (confirmed: no `.editorconfig`, no analyzer settings in any `.csproj`) — this is the documented, deliberate scope boundary from Dev Notes, not an oversight.
- **AC #3 (infra what-if only, never deploy) — verified structurally and live.** `validate-infra`'s only `az` invocation is `az deployment group what-if`; no `deployment group create` appears anywhere in the workflow. Live: PR #1 touched only `tests/**`, and `validate-infra` completed successfully with its Azure-login/what-if steps' `if:` conditions evaluating false (confirmed via `gh run view --log`, `infra_changed=false`) — the job reports overall success without ever authenticating to Azure.
- **AC #4 (fork PRs never get deploy-capable credentials) — partially live-verified, partially verified by reading GitHub's documented event shape.** The same-repo (`is_fork=false`) branch of the fork-check logic was confirmed live via PR #1's run log: `echo "is_fork=false"` executed as expected for a same-repo branch. The `is_fork=true` branch (an actual external fork PR) was not exercised live — doing so would require a second GitHub account/fork, judged not practical for this story. Verified instead by reading the fork-check expression (`github.event.pull_request.head.repo.full_name != github.repository`) against GitHub's documented `pull_request` event payload shape, which is the standard idiom GitHub's own docs recommend for this check.
- **AC #5 (passing checks unblock merge, failing checks block it) — fully live-verified, and a real bug was caught and fixed in the process.** Enabled branch protection on `main` via `gh api` (`required_status_checks.strict: true`, contexts `build-test-lint`/`validate-infra`, `enforce_admins: false`). First live test run surfaced a genuine defect: both jobs originally had a `name:` field (`"Build, test, and lint"` / `"Validate infra changes"`) that produced check-run contexts different from the job ids the branch-protection rule required — so even after both checks passed, `mergeStateStatus` stayed `BLOCKED` forever (GitHub never saw a check reporting under the exact required context string). Fixed by removing both `name:` overrides so the reported context defaults to the job id, matching branch protection exactly. Re-verified end-to-end: failing check → `BLOCKED`; passing checks + branch behind base → `BEHIND` (expected `strict: true` behavior, unrelated to the check-name bug); passing checks + branch updated → `CLEAN`. This full BLOCKED → BEHIND → CLEAN progression is captured in the throwaway PR #1's history (closed, not merged).
- **Task 3 (federated credential) — verified live, and the story's assumed subject format was superseded.** `az identity federated-credential list` against `energy-tracker-devops-uami` showed the `pull_request`-subject credential's subject as `repo:ralfonsoftware@121377414/energy-tracker-v2@1327052942:pull_request`, not the plain `repo:ralfonsoftware/energy-tracker-v2:pull_request` the story's Dev Notes assumed. Cross-checked the numeric IDs against `gh api repos/ralfonsoftware/energy-tracker-v2` (`owner.id`/`id`) — they match exactly. This is GitHub's newer **immutable subject-claim format** (owner/repo database IDs appended after `@`), which applies by default to repositories created after GitHub's July 15, 2026 cutover; this repo was created 2026-08-07, after that cutover. The existing credential is correctly configured for this repo's actual OIDC token shape — documented in `infra/README.md` along with the reference-only bootstrap command for recreating it elsewhere.
- **Task 4 (branch protection) — applied live via `gh api`, not just documented**, per explicit user confirmation before making a shared-repo-settings change. Exact command recorded in `infra/README.md`.
- **Playwright `test:e2e` intentionally excluded** from `build-test-lint`, consistent with the Story 1.3 precedent already established in `app-deploy.yml` (epic AC only names "the test suite"; e2e browser-install/server-spinup cost isn't justified in this gate).
- **No Bicep/infra resources changed** — this story's only artifacts are `.github/workflows/pr-review.yml`, `infra/README.md`, and the `web/.oxlintrc.json` fix required to make AC #2 actually true.

### File List

- `.github/workflows/pr-review.yml` (new)
- `web/.oxlintrc.json` (modified — `no-unused-vars` set to `error` so lint failures actually fail the check, per AC #2)
- `infra/README.md` (modified — documents the second `pull_request`-subject federated credential with its actual immutable-ID subject format, and the branch-protection configuration/command for AC #5)
- `_bmad-artifacts/implementation/sprint-status.yaml` (modified — status tracking)

### Change Log

- 2026-08-13: Story 1.4 implementation complete. `.github/workflows/pr-review.yml` added (`build-test-lint` + `validate-infra` jobs). Branch protection enabled on `main` requiring both checks. Fixed a job-`name:`/required-status-check-context mismatch that left merges permanently blocked even after checks passed (caught via live PR verification, not structural review alone). Fixed `web/.oxlintrc.json` so `no-unused-vars` violations actually fail CI (oxlint's default `warn` severity doesn't fail the process). Verified the existing `pull_request` federated credential live and documented its actual (immutable-ID) subject format, which differs from what the story's Dev Notes assumed.

### Review Findings

- [x] [Review][Patch] `grep '^infra/'` change-detection both over- and under-triggers — no `.bicep`/`.bicepparam` extension filter (any file under `infra/`, e.g. `infra/README.md`, triggers a full Azure login + `what-if` run — this very diff's own `infra/README.md` change would trigger it) and, separately, `git diff --name-only`'s default rename detection drops the old path entirely when a file is moved *out of* `infra/`, so that removal is invisible to the check [.github/workflows/pr-review.yml:107-110] — fixed: added `--no-renames` and an extension-filtered `grep -E`
- [x] [Review][Patch] `git diff` failure in the "Check for infra changes" step is silently coerced to `infra_changed=false` — the `&&`/`||` chain evaluates `grep`'s exit code, not `git diff`'s, and no `set -o pipefail` is set, so a broken/failing diff invocation makes a required security-relevant check silently skip validation instead of failing loud [.github/workflows/pr-review.yml:107-110] — fixed: `git diff` now writes to a file first so its own failure is distinct from grep's match result
- [x] [Review][Patch] "Notice — infra changed but validation skipped (fork PR)" step is gated with a step-level `if:`, contradicting Task 2's explicit instruction ("no `if:` restriction, always runs") — the step now shows as "skipped" for the vast majority of PRs instead of always running with the condition embedded in the shell script [.github/workflows/pr-review.yml:120-122] — fixed: step now uses `if: always()` with the condition moved into the shell script
- [x] [Review][Patch] Story's own Project Structure Notes ("No changes to `src/`, `web/`, ... application code") and file-tree enumeration omit `web/.oxlintrc.json`, which the diff does modify (and the File List correctly discloses) — the constraint text was never reconciled with the actual scope [_bmad-artifacts/implementation/1-4-pull-request-review-workflow.md — Project Structure Notes] — fixed: file-tree and constraint text updated to disclose the config-only exception
- [x] [Review][Patch] `infra/README.md`'s new federated-credential bootstrap block for `repo-pr` (step 4b) omits the immutable-subject-format caveat that the pre-existing `repo-branch-main` block (step 4) has, despite Task 3 asking for "the same style" [infra/README.md — step 4b, ~L110-120] — fixed: added the equivalent caveat to step 4b
- [x] [Review][Patch] `pr-review.yml`'s comment says the reported context must match `required_status_checks.contexts`, but the actual `gh api` payload documented in `infra/README.md` uses the current `checks` array format — stale/imprecise terminology, not a functional bug [.github/workflows/pr-review.yml:30-34] — fixed: comment reworded to `required_status_checks.checks[].context`
- [x] [Review][Patch] `validate-infra`'s `actions/checkout@v7` step has no explicit `ref:`, so it silently defaults to the PR's synthetic merge-commit (`refs/pull/N/merge`) rather than the PR head branch — a reasonable default for a what-if validation, but undocumented, and it means a PR with a real merge conflict against `main` fails required checks for reasons unrelated to the PR's own content [.github/workflows/pr-review.yml:99-105] — fixed: added an explanatory comment (behavior unchanged, documented as intentional)
- [x] [Review][Defer] `web/.oxlintrc.json`'s new `"no-unused-vars": "error"` has no ignore pattern for intentionally-unused vars (e.g. `_`-prefixed args) — repo is clean today, but the first legitimate unused-arg case becomes an unexplained CI blocker with no documented escape hatch [web/.oxlintrc.json] — deferred: no current violation, revisit if it starts blocking legitimate code
- [x] [Review][Defer] Branch-protection `required_status_checks.checks` entries omit `app_id`, so GitHub matches the required check by context string from any reporting source, not only this workflow's job [infra/README.md — branch protection `gh api` payload] — deferred: low practical risk for this repo's trust model, hardening improvement not a defect
- [x] [Review][Defer] GitHub's "require approval to run workflows for first-time/outside contributors" setting (if enabled at the org/repo level) could leave a fork PR's Actions run never starting, leaving both required checks perpetually pending and merge blocked indefinitely — not addressable in workflow YAML [.github/workflows/pr-review.yml — fork-handling design] — deferred: platform-level setting outside this diff's control, worth a doc note in a future pass
- [x] [Review][Defer] All actions pinned by mutable major-version tags (`@v7`, `@v6`, `@v3`) rather than SHA, propagating the existing `app-deploy.yml`/`infra-deploy.yml` convention into a third workflow [.github/workflows/pr-review.yml] — deferred, pre-existing convention from Story 1.2/1.3, not introduced by this diff
- [x] [Review][Defer] "Notice" step's fork-skip condition relies on no earlier step being able to fail on that path — fine today, but fragile if a future edit inserts an unconditional failing step before it without adding `if: always()` [.github/workflows/pr-review.yml:120-122] — deferred: no current defect, note for future editors
