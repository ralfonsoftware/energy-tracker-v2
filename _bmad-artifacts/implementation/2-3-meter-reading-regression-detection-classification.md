---
baseline_commit: 820130cf1afa51c73450178a1cfb0e85a96885a2
---

# Story 2.3: Meter Reading Regression Detection & Classification

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Household member,
I want a Meter Reading lower than the previous one to be flagged and classified rather than silently breaking my baseline,
so that a meter swap or digit rollover doesn't corrupt my Status.

## Acceptance Criteria

1. **Given** a new Meter Reading lower than the Reading immediately preceding it by timestamp (not entry order) **When** it's saved **Then** a classification prompt opens asking to confirm *reset* or *rollover*, and no negative consumption rate is computed (FR-25)
2. **Given** the classification prompt **When** I confirm *reset* **Then** a new baseline-computation sequence starts going forward without discarding prior Reading history (FR-25)
3. **Given** the classification prompt **When** I confirm *rollover* **Then** the interval's consumption is computed as (meter's known digit capacity − previous Reading) + new Reading (FR-25)
4. **Given** a backfilled Reading **When** compared for regression **Then** "immediately preceding" is determined by timestamp order, never entry order (FR-25)
5. **Given** an unconfirmed regression prompt **When** any later reading or Status computation runs **Then** the flagged Reading is excluded from baseline computation until resolved, and the prompt never silently expires or defaults to either classification (FR-25)
6. **Given** a second lower-than-previous Reading arrives while an earlier regression prompt is still open **When** it's detected **Then** it queues behind the first rather than opening a second, conflicting prompt — at most one open `MeterRegressionPrompt` exists per Main Meter (FR-25, AD-12)
7. **Given** the regression prompt triggers while a Log Reading sheet happens to be open **When** it opens **Then** it supersedes the Log Reading sheet rather than stacking on top of it (UX-DR13)
8. **Given** the regression prompt UI **When** rendered **Then** it uses the neutral/informational glass treatment, never destructive/error styling — this is a normal classification step, not a system error (UX-DR4, UX-DR18)
9. **Given** the regression prompt **When** it opens **Then** it traps focus while open and returns focus to the triggering control on close, and is announced on open (role + label) for screen readers (UX-DR16)

## Scope Boundary — What This Story Does NOT Do

**No live baseline/pace computation exists yet.** Story 2.4 (Gap-Tolerant Rolling Baseline & Status Computation) is what actually turns Meter Readings into a consumption pace/Status. AC #2 and #3's "a new baseline-computation sequence starts" / "the interval's consumption is computed as..." describe the *eventual* effect once 2.4 exists — there is no Status endpoint, no pace calculation, and nothing to display a computed kWh figure to yet. This story's job is to **detect the regression, classify it, and persist everything 2.4 will need** (the classification itself, and — for rollover — the digit capacity used) so 2.4's baseline math can consume it correctly. Do not build a consumption-computation feature here; verify AC #2/#3 via persisted state (`MeterRegressionPrompt.Classification`, `.DigitCapacityKwh`), not a displayed/returned kWh figure.

**Two design decisions this story makes that aren't explicitly spelled out anywhere in the PRD/architecture/UX docs** (flagged again at the end as open questions for confirmation, but a concrete default is needed to implement):

1. **Where the meter's "known digit capacity" comes from.** No prior story, PRD FR, or architecture doc defines a field for this. Per AD-15 (no hardcoded household-specific values), this story adds a nullable `MainMeter.DigitCapacityKwh` column. The classification prompt's *rollover* action captures it inline the first time (a lightweight numeric input alongside the rollover confirmation — not a separate settings flow), then persists it onto `MainMeter` so future rollovers on the same meter pre-fill it and stay a one-tap confirmation, matching the UX spec's "household resolves them one at a time, in one tap" framing.
2. **AD-11's audit-correction mechanism does not exist in the codebase yet** (no `AuditCorrection` table, no `IAuditCorrectionRecorder` — grep confirms nothing has built it as of Story 2.2). A prior architecture review (`reviews/review-adversarial-divergence.md`, finding #8) flagged that it's undefined whether resolving a `MeterRegressionPrompt` should route through AD-11. Since the mechanism doesn't exist, this story **does not** attempt to wire one — resolving a prompt is not treated as an "edit" for AD-11 purposes. Revisit if/when a later story (4.3's correction flow is the likely candidate) builds `AuditCorrection`.

## Tasks / Subtasks

- [x] Task 1: Domain — `MeterRegressionPrompt` entity, `MeterRegressionClassification` enum, `MainMeter.DigitCapacityKwh` (AC: #1, #2, #3, #4, #6)
  - [x] Create `src/EnergyTracker.Domain/MeterRegressionClassification.cs`: `public enum MeterRegressionClassification { Reset, Rollover }` — this is the **first enum in the codebase**; no `JsonStringEnumConverter` is registered anywhere yet. Do not add one globally. Map to/from a plain `string` ("reset"/"rollover") explicitly in the API endpoint DTOs (Task 3), the same way every other endpoint in this codebase hand-maps Domain ↔ response records — keeps this story's blast radius to its own files.
  - [x] Create `src/EnergyTracker.Domain/MeterRegressionPrompt.cs`: `Id` (Guid), `HouseholdId` (Guid, denormalized — AD-3 pattern, same as `MeterReading`), `MainMeterId` (Guid), `MeterReadingId` (Guid — the flagged/lower reading), `PreviousMeterReadingId` (Guid — the reading it regressed against), `CreatedAtUtc` (DateTimeOffset), `ResolvedAtUtc` (DateTimeOffset?), `Classification` (`MeterRegressionClassification?`), `DigitCapacityKwh` (decimal?, only ever set when `Classification == Rollover`). No navigation properties — this codebase's entities (`MeterReading`, `MainMeter`) never use them, only FK-shaped Guid properties + `HasOne<T>().WithMany()` in EF configuration.
  - [x] Add `public decimal? DigitCapacityKwh { get; set; }` to `src/EnergyTracker.Domain/MainMeter.cs` (mutable, like `Household.YearlyBaselineKwh` — a config value that starts unset and gets set later, not an `init`-only field).
  - [x] Add `MeterRegressionPromptConfiguration.cs` under `src/EnergyTracker.Infrastructure/Configurations/`, mirroring `MeterReadingConfiguration.cs`'s shape: `ToTable("MeterRegressionPrompts")`, `HasKey(Id)`, `.HasPrecision(18, 2)` on `DigitCapacityKwh`, `HasOne<Household>()`/`HasOne<MainMeter>()`/`HasOne<MeterReading>()` (twice — once for `MeterReadingId`, once for `PreviousMeterReadingId`, both `OnDelete(DeleteBehavior.Restrict)`, both needing an explicit `.HasForeignKey(...)` + a distinct EF-generated FK constraint name since there are two FKs to the same target table — give each relationship an explicit name via `.HasConstraintName(...)` if EF's auto-naming collides), `HasIndex(HouseholdId)`. **Also add `HasIndex(p => p.MeterReadingId).IsUnique()`** — this is the race-safety guard for Task 2's duplicate-prompt scenario below, mirroring `MeterReadingConfiguration`'s `IdempotencyKey` unique index.
  - [x] Update `MainMeterConfiguration.cs`: add `.Property(m => m.DigitCapacityKwh).HasPrecision(18, 2)` (nullable, no `.IsRequired()`).
  - [x] Add `modelBuilder.Entity<MeterRegressionPrompt>().HasQueryFilter(e => e.HouseholdId == CurrentHouseholdId);` to `EnergyTrackerDbContext.OnModelCreating`, and `public DbSet<MeterRegressionPrompt> MeterRegressionPrompts => Set<MeterRegressionPrompt>();`.
  - [x] Generate the migration via `scripts/add-migration.sh AddMeterRegressionPrompt` (covers both the new table and the new `MainMeter.DigitCapacityKwh` column in one migration, added to both provider projects — AD-2).

- [x] Task 2: Application — regression detection wired into `CreateMeterReading` (AC: #1, #4, #6)
  - [x] Extend `src/EnergyTracker.Application/Ports/IMeterReadingRepository.cs`: add `Task<MeterReading?> FindImmediatelyPrecedingAsync(Guid mainMeterId, DateTimeOffset readingTimestamp, CancellationToken cancellationToken)` — the reading with the greatest `ReadingTimestamp` strictly less than the given timestamp, for the same `MainMeterId`. Also add `Task<MeterReading?> FindByIdAsync(Guid readingId, CancellationToken cancellationToken)` (needed by Task 4's enrichment).
  - [x] Add `src/EnergyTracker.Application/Ports/IMeterRegressionPromptRepository.cs`: `Task<MeterRegressionPrompt> AddAsync(MeterRegressionPrompt prompt, CancellationToken ct)`, `Task<MeterRegressionPrompt?> GetOpenForHouseholdAsync(Guid householdId, CancellationToken ct)` (the unresolved prompt with the **earliest** `MeterReading.ReadingTimestamp` for that Household's Main Meter — computing "open" this way, rather than persisting an explicit open/queued flag, is what makes AD-12's "at most one open, ordered by timestamp" true by construction instead of by a second piece of state that could drift out of sync), `Task<MeterRegressionPrompt?> FindByIdAsync(Guid householdId, Guid promptId, CancellationToken ct)`, `Task<MeterRegressionPrompt> ResolveAsync(MeterRegressionPrompt prompt, CancellationToken ct)` (persists a prompt whose `Classification`/`DigitCapacityKwh`/`ResolvedAtUtc` the caller has already set), `Task<decimal?> GetMainMeterDigitCapacityAsync(Guid mainMeterId, CancellationToken ct)`, `Task SetMainMeterDigitCapacityIfUnsetAsync(Guid mainMeterId, decimal digitCapacityKwh, CancellationToken ct)` (a conditional `UPDATE ... WHERE DigitCapacityKwh IS NULL` — never overwrites an already-confirmed capacity from a later resolution).
  - [x] Implement both in `src/EnergyTracker.Infrastructure/Adapters/`: extend `MeterReadingRepository.cs` for the two new `IMeterReadingRepository` methods; add `MeterRegressionPromptRepository.cs` for the new port. **`AddAsync` needs the same race-safety discipline as `MeterReadingRepository.AddAsync`** (Story 2.2's established pattern): catch `DbUpdateException` on the `MeterReadingId` unique-index violation, detach, re-query by `MeterReadingId`, and return the winner — this is reachable because `CreateMeterReading.ExecuteAsync`'s existing `AddAsync(reading, ct)` call can itself return a different (already-persisted) reading than the caller's local variable on a concurrent idempotency-key race (see `MeterReadingRepository.AddAsync`'s existing catch block), so two concurrent requests can both reach the regression-detection step for the *same* winning reading and both attempt to insert its prompt.
  - [x] Modify `src/EnergyTracker.Application/CreateMeterReading.cs`: inject `IMeterRegressionPromptRepository` alongside the existing `IMeterReadingRepository` (primary-ctor DI, two params now). **Only run detection after a genuinely fresh insert** — the existing `FindByIdempotencyKeyAsync` early-return path must stay untouched; detection runs on the `MeterReading` actually returned by `repository.AddAsync(...)` (capture it in a variable — don't detect against the pre-insert local `reading`, since a concurrent-race requery can substitute a different persisted instance as described above). Detection: call `FindImmediatelyPrecedingAsync(mainMeter.Id, persistedReading.ReadingTimestamp, ct)`; if a preceding reading exists and `persistedReading.KwhValue < preceding.KwhValue`, construct and `AddAsync` a new `MeterRegressionPrompt` (`MeterReadingId = persistedReading.Id`, `PreviousMeterReadingId = preceding.Id`, `Classification = null`, `ResolvedAtUtc = null`). **Do not compare against or re-evaluate any reading chronologically *after* the new one** — only the immediately-preceding comparison is in scope; a backfill is never retroactively re-checked against its chronological successor (not required by any AC here, and doing it anyway would need its own queuing/superseding rules this story doesn't define).
  - [x] `CreateMeterReading.ExecuteAsync`'s **return type and signature stay unchanged** (`Task<MeterReading>` — regression detection is a side effect, not part of the return value). The frontend/API discovers a newly-raised or still-open prompt via Task 4's dedicated `GET` endpoint, not via the create-reading response — this mirrors the documented "Dashboard Status endpoint returns only the current Status ... drill-down data is always a separate endpoint" convention (`consistency-conventions.md`) and avoids a second response shape for the common no-regression case.
  - [x] Register `IMeterRegressionPromptRepository`/`MeterRegressionPromptRepository` in `Program.cs` next to the existing `IMeterReadingRepository` registration.

- [x] Task 3: Application — resolving a prompt (AC: #2, #3, #5, #6)
  - [x] Add `src/EnergyTracker.Application/MeterRegressionPromptNotFoundException.cs` (mirror `HouseholdInviteNotFoundException.cs`'s shape — constructor takes the prompt id).
  - [x] Add `src/EnergyTracker.Application/MeterRegressionPromptNotOpenException.cs` — thrown when the targeted prompt is already resolved, or is not currently the earliest-unresolved (open) one for its Main Meter. Message should explain both cases plainly (e.g. "already resolved" vs. "another regression must be resolved first").
  - [x] Add `src/EnergyTracker.Application/MeterRegressionValidationException.cs` (mirror `MeterReadingValidationException.cs`'s shape) — thrown when confirming *rollover* with no digit capacity available (neither passed in nor already stored on `MainMeter`) or a non-positive one.
  - [x] Add `src/EnergyTracker.Application/ResolveMeterRegressionPrompt.cs`: `ExecuteAsync(Guid householdId, Guid promptId, MeterRegressionClassification classification, decimal? digitCapacityKwh, CancellationToken ct)`. Steps: `FindByIdAsync` → `MeterRegressionPromptNotFoundException` if null; if `prompt.ResolvedAtUtc is not null` → `MeterRegressionPromptNotOpenException` ("already resolved"); `GetOpenForHouseholdAsync` → if the open prompt's `Id` isn't this one → `MeterRegressionPromptNotOpenException` ("resolve the earlier one first") — this is what makes AC #6's "resolves one at a time, in order" a hard guarantee rather than a UI-only convention (the UI should never let this be reachable in practice, since it only ever surfaces the open one — see Task 5 — but the API must not trust that). If `classification == Rollover`: resolve the effective capacity as `digitCapacityKwh ?? await repository.GetMainMeterDigitCapacityAsync(prompt.MainMeterId, ct)`; if that's null or `<= 0`, throw `MeterRegressionValidationException`; set `prompt.DigitCapacityKwh = effectiveCapacity`; call `SetMainMeterDigitCapacityIfUnsetAsync(prompt.MainMeterId, effectiveCapacity, ct)` (only takes effect the first time — see Scope Boundary above). Set `prompt.Classification = classification` and `prompt.ResolvedAtUtc = DateTimeOffset.UtcNow` regardless of classification, then `await repository.ResolveAsync(prompt, ct)`.
  - [x] Register `ResolveMeterRegressionPrompt` in `Program.cs`.

- [x] Task 4: Application — reading the open prompt (AC: #1, #6, #7)
  - [x] Add `src/EnergyTracker.Application/GetOpenMeterRegressionPrompt.cs`: injects both `IMeterRegressionPromptRepository` and `IMeterReadingRepository`. `ExecuteAsync(Guid householdId, CancellationToken ct)` → `GetOpenForHouseholdAsync`; if null, return null; otherwise look up both `MeterReadingId` and `PreviousMeterReadingId` via `FindByIdAsync` and return a small Application-layer record `OpenMeterRegressionPromptDetails(MeterRegressionPrompt Prompt, MeterReading Reading, MeterReading PreviousReading, decimal? MainMeterDigitCapacityKwh)` (the last field via `GetMainMeterDigitCapacityAsync`, so the API/frontend can pre-fill an already-known digit capacity instead of asking again — Scope Boundary decision #1).
  - [x] Register in `Program.cs`.

- [x] Task 5: API endpoints (AC: #1, #2, #3, #5, #6)
  - [x] Add `src/EnergyTracker.Api/Endpoints/MeterRegressionPromptEndpoints.cs`. Reuse the `TryGetHouseholdId` 403-helper pattern copied into `MeterReadingEndpoints.cs` (copy again here, not shared across files — established precedent).
    - `GET /api/meter-regression-prompts/open` → `Results.Ok(response)` where `response` is a nullable DTO (200 with a `null` body when nothing's open — simpler for the frontend than distinguishing 204, and consistent with this being a "is there one?" poll-style read, not a resource fetch that should 404 when absent). Shape: `MeterRegressionPromptResponse(Guid Id, Guid MeterReadingId, decimal ReadingKwhValue, DateTimeOffset ReadingTimestamp, Guid PreviousMeterReadingId, decimal PreviousReadingKwhValue, DateTimeOffset PreviousReadingTimestamp, decimal? MainMeterDigitCapacityKwh)`.
    - `POST /api/meter-regression-prompts/{id}/resolve`, request DTO `ResolveMeterRegressionPromptRequest(string Classification, decimal? DigitCapacityKwh)`. Parse `Classification` case-insensitively against `"reset"`/`"rollover"`; an unrecognized value → `Results.Problem(detail: ..., statusCode: 400)` (do this parse in the endpoint, not by trusting a `JsonStringEnumConverter` — see Task 1). Catch `MeterRegressionValidationException` → 400, `MeterRegressionPromptNotFoundException` → 404, `MeterRegressionPromptNotOpenException` → 409 (same RFC 7807 `Results.Problem` shape as every other endpoint; a 409 here is a reasonable extension of `consistency-conventions.md`'s "concurrency conflicts return 409" — this is a different kind of conflict (ordering, not optimistic-concurrency), but the same wire contract fits: current server state -ish info in the detail message, client should reconcile). Response: `ResolveMeterRegressionPromptResponse(Guid Id, string Classification, DateTimeOffset ResolvedAtUtc)`.
  - [x] Register both endpoints (`api.MapMeterRegressionPromptEndpoints();`) in `Program.cs`, next to `api.MapMeterReadingEndpoints();`.

- [x] Task 6: Frontend — regression prompt dialog + wiring (AC: #1, #2, #3, #7, #8, #9)
  - [x] Add `web/src/lib/meter-regression-api.ts`: `fetchOpenMeterRegressionPrompt()` (GET, returns the DTO or `null`) and `resolveMeterRegressionPrompt(id, classification, digitCapacityKwh?)` (POST), reusing the `ApiError`/`toApiError` pattern already established in `meter-reading-sync.ts`/`tagging-scaffold-manager.tsx` for error responses.
  - [x] Add `web/src/components/meter-reading/meter-regression-prompt-dialog.tsx`: shadcn `Dialog` (not `Sheet` — the UX spec explicitly calls for a Dialog here), receiving the open prompt (or `null`) and a resolve callback as controlled props from `App.tsx` (see below), not managing its own open/fetch state — this is what makes the "supersede the Log Reading sheet" requirement (AC #7) tractable: a single source of truth in `App.tsx` decides what's open. **Glass-panel tokens are wired as of Story 2.2b** — apply `GLASS_MODAL_CLASSNAME` (exported from `web/src/lib/glass-classnames.ts`) to `DialogContent` instead of leaving it unstyled; this is the same centered-modal glass shell Room Management's Delete-confirm dialog already consumes, and it matches `mockups/key-log-reading-flow.html` Frame 2's rendered reference (`.modal-card`/`.modal-icon`/`.modal-title`/`.modal-body`) exactly. The plain glass shell alone already satisfies "neutral/informational, never destructive" (AC #8) — it carries no status-triad or destructive color, so no extra variant work is needed to make it "not an error." Content: plain-language explanation of the two readings (kWh values + timestamps) from the DTO, two `Button` actions styled as the mock's `.choice-btn` plain-bordered rows (default `Button` variant — **not** `glass-primary`/`glass-confirm`, since neither choice is a destructive or single "primary" action; per the UX spec's "no status triad or destructive color anywhere in this component"): "Meter was reset/replaced" (→ resolves as `Reset` immediately, no extra input) and "Meter rolled over" (→ reveals a digit-capacity field using the shared `UnitInput` component from Story 2.2b, `unit="kWh"` — pre-filled from `MainMeterDigitCapacityKwh` when present and still editable, empty and required when absent — with its own "Confirm" action, `variant="glass-primary"` since this step *is* a real form submission, same as Log Reading's/Yearly Baseline's Save). Radix `Dialog` provides focus trap and return-focus-on-close natively (AC #9's first half, same as `Sheet`/`Log Reading` in Story 2.2); use `DialogTitle`/`DialogDescription` for the open-announcement half.
  - [x] **Wire into `App.tsx`:** lift the currently-open regression prompt into `App.tsx` state, fetched on mount (`fetchOpenMeterRegressionPrompt()`) and re-fetched immediately after any successful `LogReadingSheet` save. Change `LogReadingSheet` from internally-managed `open` state to a controlled component (`open`/`onOpenChange` props from `App.tsx`, plus an `onSaved` callback fired after a successful save/offline-enqueue) — this is the concrete mechanism for AC #7: when `App.tsx`'s open-prompt state becomes non-null, force `logSheetOpen` to `false` in the same state update, so the regression dialog visually supersedes rather than stacks (Radix Dialogs already stack by z-index/portal order if both were left open simultaneously — the fix here is behavioral, not CSS). Render `<MeterRegressionPromptDialog>` unconditionally in `App.tsx` alongside the dashboard placeholder, open whenever the fetched prompt is non-null. After a successful resolve, re-call `fetchOpenMeterRegressionPrompt()` — if another prompt is now open (AC #6's queued case), the dialog stays open with the new prompt's data instead of closing, satisfying "resolves one at a time, in order" without a page reload.
  - [x] Add a `meterRegression.*` i18n key block (dialog title/description, reset/rollover action labels, digit-capacity field label, confirm action, generic error) to both `web/src/locales/en-US/translation.json` and `de-DE/translation.json`.

- [x] Task 7: Tests (AC: all)
  - [x] `tests/EnergyTracker.Application.Tests/CreateMeterReadingTests.cs` — add cases (NSubstitute against the now-two-dependency constructor): a reading lower than its immediately-preceding one creates a `MeterRegressionPrompt` via `regressionPromptRepository.Received(1).AddAsync(...)`; a reading *higher* than or equal to its preceding one does not; the very first reading for a Main Meter (no preceding reading found) does not; an idempotency-key replay does not re-run detection (`FindImmediatelyPrecedingAsync` never called).
  - [x] `tests/EnergyTracker.Application.Tests/ResolveMeterRegressionPromptTests.cs` (new) — reset resolves cleanly with no capacity involved; rollover with an explicit `digitCapacityKwh` persists it on the prompt and calls `SetMainMeterDigitCapacityIfUnsetAsync`; rollover with no explicit capacity but an existing `MainMeter.DigitCapacityKwh` succeeds using the stored value; rollover with neither throws `MeterRegressionValidationException`; a non-positive `digitCapacityKwh` throws; resolving an already-resolved prompt throws `MeterRegressionPromptNotOpenException`; resolving a prompt that isn't the current open one (another is earlier by timestamp) throws `MeterRegressionPromptNotOpenException`; a nonexistent prompt id throws `MeterRegressionPromptNotFoundException`.
  - [x] `tests/EnergyTracker.Application.Tests/GetOpenMeterRegressionPromptTests.cs` (new) — returns null when nothing's open; returns the enriched details (both readings' data + `MainMeterDigitCapacityKwh`) when one is.
  - [x] `tests/EnergyTracker.Api.Tests/MeterRegressionPromptEndpointsTests.cs` (new), `EnergyTrackerApiFactory`-based, real Postgres via Testcontainers (mirror `MeterReadingEndpointsTests.cs`). Cover, end-to-end through `POST /api/meter-readings` then this story's endpoints: posting a reading lower than the previous one, then `GET open` returns it; resolving as reset returns 200 and a subsequent `GET open` returns null (or the next queued one, in the multi-prompt case below); resolving as rollover with a capacity value persists it and updates `MainMeter`'s stored value (verify via a second rollover on a fresh regression that omits `digitCapacityKwh` and still succeeds); a second lower-than-previous reading arriving while the first prompt is unresolved does **not** change what `GET open` returns (still the first, by timestamp) — post readings out of insertion order to also cover AC #4's timestamp-vs-entry-order requirement; attempting to resolve the second (queued, non-open) prompt directly by id returns 409; resolving twice returns 409 on the second call; cross-Household 403/isolation (mirror Story 2.1/2.2's IDOR test).
  - [x] `web/src/components/meter-reading/meter-regression-prompt-dialog.test.tsx` (new) — Vitest + Testing Library. Cover: renders the two readings' values/timestamps from a supplied prompt; "Meter was reset/replaced" resolves immediately with no extra input; "Meter rolled over" reveals the capacity field, pre-filled when `mainMeterDigitCapacityKwh` is provided, required when it's `null`; traps focus/announces on open (role + accessible name via Testing Library's accessibility queries).
  - [x] Update `web/src/components/meter-reading/log-reading-sheet.test.tsx` for the new controlled `open`/`onOpenChange`/`onSaved` props (no behavior change to the save flow itself, just how openness is driven).
  - [x] Update `web/src/App.test.tsx` (if it currently exercises the dashboard placeholder/sheet trigger) for the lifted state; add a case confirming a fetched open prompt on mount renders the regression dialog, and that opening `LogReadingSheet` while a prompt is open is prevented / that a newly-arriving open prompt closes an open sheet (AC #7 — mock `fetchOpenMeterRegressionPrompt` to control timing).

## Dev Notes

- **This story extends `CreateMeterReading`, not just adding new files** — Story 2.2's own Dev Notes explicitly scoped regression detection out and flagged it as "Story 2.3's entire job... do not add a comparison-against-previous-reading check, even defensively." That means Task 2 is a real modification to an existing, tested, reviewed use case (constructor signature changes from one dependency to two) — re-run and update `CreateMeterReadingTests.cs`'s existing cases for the new constructor shape, don't just add new test methods alongside stale ones that no longer compile.
- **"Open" is computed, not stored.** No `IsOpen`/`Status` column on `MeterRegressionPrompt` — "the open one" is always the earliest-by-`ReadingTimestamp` unresolved prompt for a Household (equivalently, its one Main Meter — v2 is single-Main-Meter-per-Household, `deferred.md`). This is deliberate: a persisted queue-position/open-flag is a second piece of state that has to be kept in sync with resolution order and reading timestamps, which is exactly the kind of drift AD-12 exists to prevent. Don't add one.
- **Digit capacity is genuinely undefined upstream** — see "Scope Boundary" above for the full reasoning; this is flagged again as an open question at the end of this document. The `MainMeter.DigitCapacityKwh` field, and capturing it inline on first rollover, is this story's own design decision to make FR-25's rollover math implementable at all, not something copied from an existing spec. If Ralf's answer to the end-of-document question changes the intended UX (e.g., a dedicated Settings field instead), this task list's Task 6 dialog behavior is the part to revisit — the backend shape (nullable `MainMeter` column, captured-once-then-reused) still holds either way.
- **Race-safety is not optional here, same lesson as Story 2.2.** `CreateMeterReading`'s existing `AddAsync` can return a *different* persisted `MeterReading` than the one constructed locally (idempotency-key race, existing behavior) — Task 2 must detect against that returned instance, and the new `MeterRegressionPromptRepository.AddAsync` needs its own catch-detach-requery on the `MeterReadingId` unique index for the same reason `MeterReadingRepository` needed one for `IdempotencyKey`/`HouseholdId`. Skipping this reproduces Story 2.2's review-found race class in a new place.
- **AD-11 does not apply here** — see Scope Boundary decision #2. Do not add an `AuditCorrection` row or invent a recorder call; the mechanism doesn't exist in this codebase yet.
- **Glass-panel tokens are wired as of Story 2.2b** (Design System Foundation, `done`) — `web/src/index.css` now defines the full component-token layer, `web/src/lib/glass-classnames.ts` exports `GLASS_MODAL_CLASSNAME`/`GLASS_SHEET_CLASSNAME`, and `Button` has `glass-primary`/`glass-confirm` variants plus a shared `UnitInput` component. Build the regression prompt against these shared pieces (`GLASS_MODAL_CLASSNAME` on `DialogContent`, `UnitInput` for the digit-capacity field, `glass-primary` on its Confirm action) rather than a bare shadcn `Dialog` — see `mockups/key-log-reading-flow.html` Frame 2 for the exact rendered reference. No `StatusBadge` exists yet (2.2b's Scope Boundary #1 deferred it to Story 2.5 — there's no Dashboard/nav consumer for it yet); don't reach for it here.
- **This is the first enum in the codebase.** Do not register a global `JsonStringEnumConverter` in `Program.cs` for it — map `MeterRegressionClassification` to/from a plain `string` explicitly in the endpoint DTOs, matching how every other endpoint here hand-maps between Domain types and response records (e.g. `MeterReadingEndpoints.ToResponse`).
- **Dashboard/global wiring, not a Story-2.5 boundary issue this time.** Unlike the Log Reading trigger button (Story 2.2 → Story 2.5 handoff), the regression prompt dialog is not a Dashboard-specific surface — FR-25 can trigger from *any* reading entry, and EXPERIENCE.md's component table says it's "triggered inline, wherever a Reading is entered." Render it at the `App.tsx` level (sibling to the current dashboard placeholder), not nested inside dashboard-specific markup, so it isn't accidentally scoped to a view that Story 2.5 later restructures.

### Project Structure Notes

- `MeterRegressionPrompt`/`MeterRegressionClassification` are new Domain types; `MeterRegressionPromptRepository`/`MeterRegressionPromptConfiguration`/`MeterRegressionPromptEndpoints` are new Infrastructure/Api files — same structural pattern as `MeterReading`'s own introduction in Story 2.2. No new project or layer type.
- `CreateMeterReading.cs` and `IMeterReadingRepository.cs`/`MeterReadingRepository.cs` are **modified**, not just extended-by-addition — expect constructor/interface signature changes, not purely additive changes.
- `web/src/lib/meter-regression-api.ts` follows the existing `web/src/lib/meter-reading-sync.ts` shape (thin fetch wrapper + `ApiError`). `web/src/components/meter-reading/meter-regression-prompt-dialog.tsx` joins `log-reading-sheet.tsx` in the existing `meter-reading/` feature folder — no new folder needed.
- No conflicts with the unified project structure otherwise.

### References

- [Source: _bmad-artifacts/planning/epics/epic-2-meter-reading-pattern-detective-status-core.md#Story-2.3] — canonical Story 2.3 statement and AC text (reproduced above verbatim).
- [Source: _bmad-artifacts/planning/prds/prd-energy-tracker-2026-08-08/prd/4-features.md#FR-25] — FR-25 full text and its six testable consequences.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-12] — the regression-queue rule this story implements: at most one open `MeterRegressionPrompt` per Main Meter, ordered by reading timestamp not entry order, excluding the flagged reading (and everything chronologically after it) from baseline computation until resolved.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-11] — audit-correction mechanism this story deliberately does not wire into (doesn't exist yet).
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-15] — basis for `MainMeter.DigitCapacityKwh` being a config column, never a hardcoded constant.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/invariants-rules.md#AD-7] — Status/history computation model; confirms this story only *feeds* Story 2.4's computation, doesn't perform it.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/structural-seed.md] — ERD: `MainMeter ||--o{ MeterRegressionPrompt : "may raise"`.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/ARCHITECTURE-SPINE/consistency-conventions.md] — RFC 7807 error shape, kebab-case route naming, "drill-down data is always a separate endpoint" precedent used to justify keeping regression state out of the create-reading response.
- [Source: _bmad-artifacts/planning/architecture/architecture-energy-tracker-2026-08-09/reviews/review-adversarial-divergence.md] — finding #8, the unresolved AD-11-vs-AD-12 question this story's Scope Boundary decision #2 addresses.
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/EXPERIENCE.md#Micro-Flow] — the 7-step Meter Reading Regression Classification walkthrough (UX-DR18), the exact sequence Tasks 2/3/6 implement.
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/EXPERIENCE.md#Accessibility-Floor] — focus-trap/return-focus/ARIA-announce requirement (AC #9, UX-DR16), same discipline as the Log Reading sheet.
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/DESIGN/components.md#Meter-regression-prompt] — neutral/informational Dialog treatment, two plain-labeled actions, no status triad or destructive color (UX-DR4).
- [Source: _bmad-artifacts/planning/ux-designs/ux-energy-tracker-2026-08-08/mockups/key-log-reading-flow.html#Frame-2] — rendered reference for the Meter regression prompt's exact glass-modal treatment (Frame 1 is the Log Reading sheet this story's dialog supersedes, per AC #7).
- [Source: _bmad-artifacts/implementation/2-2b-design-system-foundation.md] — the shared `GLASS_MODAL_CLASSNAME`, `Button` `glass-primary`/`glass-confirm` variants, and `UnitInput` component this story now builds against instead of a bare `Dialog`; done, baseline for this amendment.
- [Source: _bmad-artifacts/planning/epics/requirements-inventory.md] — UX-DR4/UX-DR13/UX-DR16/UX-DR18 one-line definitions.
- [Source: _bmad-artifacts/implementation/2-2-meter-reading-entry-with-offline-queueing.md] — the explicit scope hand-off ("FR-25... is Story 2.3's entire job"), the `AddAsync` race-safety pattern this story's `MeterRegressionPromptRepository` mirrors, the `ApiError`/`toApiError` pattern, and the confirmed absence of wired glass-panel tokens.
- [Source: src/EnergyTracker.Domain/MeterReading.cs], [Source: src/EnergyTracker.Domain/MainMeter.cs] — existing entities this story extends/reads; `CreatedAtUtc` (server insert order) vs. `ReadingTimestamp` (chronological order) distinction, already called out in `MeterReading.cs`'s own comment as "the 'entry order' signal Story 2.3's regression detection needs to distinguish from `ReadingTimestamp` order."
- [Source: src/EnergyTracker.Application/CreateMeterReading.cs], [Source: src/EnergyTracker.Application/Ports/IMeterReadingRepository.cs] — the exact use case/port this story modifies.
- [Source: src/EnergyTracker.Infrastructure/Adapters/MeterReadingRepository.cs] — the `GetOrCreateMainMeterAsync`/`AddAsync` catch-detach-requery race-safety pattern to mirror in `MeterRegressionPromptRepository`.
- [Source: src/EnergyTracker.Application/SetYearlyBaseline.cs], [Source: src/EnergyTracker.Application/HouseholdConcurrencyConflictException.cs] — AD-4-style exception-to-409-mapping precedent, the basis for `MeterRegressionPromptNotOpenException` → 409.
- [Source: src/EnergyTracker.Api/Endpoints/MeterReadingEndpoints.cs] — `TryGetHouseholdId` 403-helper pattern to copy again; request/response DTO record shape.
- [Source: src/EnergyTracker.Api/Program.cs] — DI registration and `api.Map*Endpoints()` composition-root block to extend.
- [Source: web/src/components/meter-reading/log-reading-sheet.tsx] — component this story converts from uncontrolled to controlled `open` state; existing `SAVE_BUTTON_CLASSNAME`/i18n/`ApiError` conventions.
- [Source: web/src/lib/meter-reading-sync.ts] — `ApiError`/`toApiError` pattern `meter-regression-api.ts` reuses.
- [Source: web/src/App.tsx] — current dashboard placeholder and `LogReadingSheet` mounting point; insertion point for lifted state and the new dialog.
- [Source: web/src/index.css] — confirms no `surface-glass`/backdrop-blur tokens are wired yet (grep returned no matches).

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- `dotnet build` (solution-wide): clean after every task; 0 errors throughout (only pre-existing `SSH.NET` NU1903 advisory warnings, unrelated to this story).
- `./scripts/add-migration.sh AddMeterRegressionPrompt`: succeeded on both providers on the first attempt — one migration covers the new `MeterRegressionPrompts` table and `MainMeter.DigitCapacityKwh`.
- `dotnet test` (full solution, Docker/Testcontainers available in this environment): 172/172 passed (Domain/Infrastructure/Api/Architecture/Application projects) — includes the new `ResolveMeterRegressionPromptTests` (9), `GetOpenMeterRegressionPromptTests` (2), the 3 new `CreateMeterReadingTests` regression cases, and the 11 new `MeterRegressionPromptEndpointsTests` (real Postgres via Testcontainers).
- Found and fixed a real bug during API-integration testing: `Results.Ok(null)` writes an **empty** response body, not the JSON literal `"null"` — the original Task 5 wording ("200 with a null body") reads naturally as literal JSON `null`, but ASP.NET Core's actual behavior is an empty body. `web/src/lib/meter-regression-api.ts`'s `fetchOpenMeterRegressionPrompt` reads the body as text and treats empty the same as `null` rather than calling `response.json()` directly (which throws `SyntaxError` on an empty body) — this would have been a real "nothing open" bug in production, not just a test-assertion mismatch.
- `npm test -- --run` (web/): 11 files, 63 tests passed — new: 6 in `meter-regression-prompt-dialog.test.tsx` (new file), 1 in `log-reading-sheet.test.tsx` (`onSaved`), 2 in `App.test.tsx` (Meter Regression prompt describe block). `npx tsc -b`: clean. `npm run lint` (oxlint): clean except the same 3 pre-existing `react/only-export-components` warnings Story 2.2b's own notes already flagged as non-build-breaking. `npm run build`: clean production build.
- Browser-based visual verification was not performed in this session (no interactive browser tool invoked) — verification relied on the full Vitest + Testing Library suite (renders the actual `MeterRegressionPromptDialog`/`App` JSX and asserts on real roles/accessible names, including the aria-hidden/inert-background assertion for AC #7), `tsc -b`, `oxlint`, `npm run build`, and the real-Postgres API integration suite.

### Completion Notes List

- Task 1: `MeterRegressionClassification` (first enum in the codebase, no `JsonStringEnumConverter`), `MeterRegressionPrompt` entity, and `MainMeter.DigitCapacityKwh` added exactly as scoped. `MeterRegressionPromptConfiguration` gives the two FKs to `MeterReading` explicit constraint names to avoid an auto-naming collision, and adds the `MeterReadingId` unique index Task 2 relies on for race-safety. Migration `AddMeterRegressionPrompt` covers both providers in one commit (AD-2).
- Task 2: `IMeterReadingRepository` gained `FindImmediatelyPrecedingAsync`/`FindByIdAsync`; new `IMeterRegressionPromptRepository` + `MeterRegressionPromptRepository` (catch-detach-requery on the `MeterReadingId` unique index, mirroring `MeterReadingRepository.AddAsync`'s established pattern). `CreateMeterReading` now takes two dependencies and detects regression against the *persisted* reading (not the pre-insert local variable), per the race-safety discipline the story's Dev Notes called out. Two pre-existing `CreateMeterReadingTests` methods (`Two_distinct_idempotency_keys...`, `GetOrCreateMainMeterAsync_is_called...`) had never configured `AddAsync`'s return value — harmless before this story since the return value was unused by the caller, but now needed (`persistedReading.ReadingTimestamp` is dereferenced) and threw `NullReferenceException` until both tests were given an explicit `AddAsync` stub.
- Task 3: `ResolveMeterRegressionPrompt` implements the ordering guard (`GetOpenForHouseholdAsync` must match the target prompt's id) exactly as specified, including the "resolve the earlier one first" 409 case.
- Task 4: `GetOpenMeterRegressionPrompt` returns `null` when nothing's open, otherwise the enriched `OpenMeterRegressionPromptDetails` record with both readings + the Main Meter's current digit capacity.
- Task 5: `MeterRegressionPromptEndpoints` added; case-insensitive `Classification` string parsing in the endpoint (not a `JsonStringEnumConverter`, per Task 1's note); 400/404/409 mapped from the three new exception types. **Found and fixed during testing:** `Results.Ok(null)` returns an empty body, not literal `"null"` — see Debug Log; fixed in the frontend fetch wrapper, not the endpoint (an empty 200 body is a legitimate, simpler wire contract than forcing literal `null` JSON, and matches what "don't need to distinguish 204" was going for).
- Task 6: `MeterRegressionPromptDialog` built against `GLASS_MODAL_CLASSNAME` (Story 2.2b) with the mock's plain-bordered choice-row treatment for reset/rollover and `glass-primary` only on the rollover Confirm submission, per the story's Task 6 amendment (sprint-change-proposal-2026-08-16.md §4.2). The dialog is deliberately **not dismissible** — no `onOpenChange` is wired (so Radix's Close/Escape/outside-click handlers are no-ops against the controlled `open` prop) and the built-in close (X) button is hidden via a Tailwind child-selector, matching AC #5's "never silently expires or defaults to either classification." `LogReadingSheet` converted to a fully controlled component (`open`/`onOpenChange`/`onSaved`), `App.tsx` lifts both the open-prompt and log-sheet-open state and forces the sheet closed whenever a prompt becomes open (AC #7).
- Task 7: Full test coverage added at every layer — see Debug Log for pass counts. The API integration suite's cross-Household test asserts `404 NotFound` (not `403`), matching the established AD-3 query-filter pattern already used by `TaggingScaffoldEndpointsTests` (cross-Household access is indistinguishable from a nonexistent resource; `403` is reserved for "no Household at all") — the story's own Task 7 wording ("cross-Household 403/isolation") is satisfied by this existing, correct convention rather than a literal 403. `App.test.tsx`'s `mockSession` helper was tightened to route by URL (previously a blanket any-URL mock) — needed because it would otherwise echo the `/api/session` response body back for the new `/api/meter-regression-prompts/open` mount-time fetch too, masking whether the regression-prompt wiring was actually exercised correctly.

### File List

- `src/EnergyTracker.Domain/MeterRegressionClassification.cs` (new)
- `src/EnergyTracker.Domain/MeterRegressionPrompt.cs` (new)
- `src/EnergyTracker.Domain/MainMeter.cs` (modified — `DigitCapacityKwh`)
- `src/EnergyTracker.Infrastructure/Configurations/MeterRegressionPromptConfiguration.cs` (new)
- `src/EnergyTracker.Infrastructure/Configurations/MainMeterConfiguration.cs` (modified)
- `src/EnergyTracker.Infrastructure/EnergyTrackerDbContext.cs` (modified)
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/20260816151421_AddMeterRegressionPrompt.cs` (new)
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/20260816151421_AddMeterRegressionPrompt.Designer.cs` (new)
- `src/EnergyTracker.Infrastructure.Migrations.Postgres/Migrations/EnergyTrackerDbContextModelSnapshot.cs` (modified)
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/20260816151425_AddMeterRegressionPrompt.cs` (new)
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/20260816151425_AddMeterRegressionPrompt.Designer.cs` (new)
- `src/EnergyTracker.Infrastructure.Migrations.SqlServer/Migrations/EnergyTrackerDbContextModelSnapshot.cs` (modified)
- `src/EnergyTracker.Application/Ports/IMeterReadingRepository.cs` (modified — `FindImmediatelyPrecedingAsync`, `FindByIdAsync`)
- `src/EnergyTracker.Application/Ports/IMeterRegressionPromptRepository.cs` (new)
- `src/EnergyTracker.Infrastructure/Adapters/MeterReadingRepository.cs` (modified)
- `src/EnergyTracker.Infrastructure/Adapters/MeterRegressionPromptRepository.cs` (new)
- `src/EnergyTracker.Application/CreateMeterReading.cs` (modified — two-dependency constructor, regression detection)
- `tests/EnergyTracker.Application.Tests/CreateMeterReadingTests.cs` (modified — new constructor shape, regression-detection cases)
- `src/EnergyTracker.Application/MeterRegressionPromptNotFoundException.cs` (new)
- `src/EnergyTracker.Application/MeterRegressionPromptNotOpenException.cs` (new)
- `src/EnergyTracker.Application/MeterRegressionValidationException.cs` (new)
- `src/EnergyTracker.Application/ResolveMeterRegressionPrompt.cs` (new)
- `tests/EnergyTracker.Application.Tests/ResolveMeterRegressionPromptTests.cs` (new)
- `src/EnergyTracker.Application/GetOpenMeterRegressionPrompt.cs` (new — also defines `OpenMeterRegressionPromptDetails`)
- `tests/EnergyTracker.Application.Tests/GetOpenMeterRegressionPromptTests.cs` (new)
- `src/EnergyTracker.Api/Program.cs` (modified — `ResolveMeterRegressionPrompt`/`GetOpenMeterRegressionPrompt` DI, endpoint mapping)
- `src/EnergyTracker.Api/Endpoints/MeterRegressionPromptEndpoints.cs` (new)
- `web/src/lib/meter-regression-api.ts` (new)
- `web/src/components/meter-reading/meter-regression-prompt-dialog.tsx` (new)
- `web/src/components/meter-reading/log-reading-sheet.tsx` (modified — controlled `open`/`onOpenChange`/`onSaved`)
- `web/src/components/meter-reading/log-reading-sheet.test.tsx` (modified — controlled-prop wrapper, new `onSaved` case)
- `web/src/App.tsx` (modified — lifted open-prompt state, wiring)
- `web/src/locales/en-US/translation.json` (modified — `meterRegression.*` keys)
- `web/src/locales/de-DE/translation.json` (modified — `meterRegression.*` keys)
- `tests/EnergyTracker.Api.Tests/MeterRegressionPromptEndpointsTests.cs` (new)
- `web/src/components/meter-reading/meter-regression-prompt-dialog.test.tsx` (new)
- `web/src/App.test.tsx` (modified — `mockSession` now routes by URL; new Meter Regression prompt test cases)
## Open Questions for Ralf (raised during story creation, not blocking — see Scope Boundary for the defaults this story ships with)

1. **Digit capacity source.** This story adds it as a `MainMeter`-scoped value captured inline the first time a rollover is confirmed (no dedicated Settings UI). Is that the right long-term home for it, or should a Settings field for "meter digit capacity" be added as its own small story later (e.g. for a household that wants to set it proactively, before ever hitting a rollover)?
2. **AD-11 vs. regression resolution.** Confirmed the mechanism doesn't exist yet, so this story doesn't wire it — but should resolving a `MeterRegressionPrompt` (especially *rollover*, which effectively reinterprets a reading's meaning) count as an "edit" once Story 4.3 builds `AuditCorrection`? Worth a one-line decision in AD-11 or AD-12 at that point (per `review-adversarial-divergence.md` finding #8) so it isn't re-litigated then.
3. **Out-of-order resolve → 409.** This story blocks resolving a queued (non-open) prompt directly via the API with a 409, even though the UI never exposes that path in normal use. Reasonable defensive default, or should the API instead silently reorder / allow it? (No AC requires either; 409 was chosen as the safer default.)
