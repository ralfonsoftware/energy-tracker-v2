import { Button } from '@/components/ui/button'

function App() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-4">
      <h1 className="text-2xl font-semibold">Energy Tracker</h1>
      <Button>Skeleton build</Button>
    </main>
  )
}

export default App
